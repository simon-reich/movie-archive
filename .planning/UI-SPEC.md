---
phase: 1
slug: authentication
status: approved
reviewed_at: 2026-05-15T00:00:00Z
shadcn_initialized: false
preset: none
created: 2026-05-15
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1 (Authentication). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn-vue (manual — tokens present, no components.json) |
| Preset | not applicable |
| Component library | radix-vue 1.9.x |
| Icon library | lucide-vue-next 0.487.x |
| Font | system-ui stack (Tailwind default — no custom font declared) |

**Aesthetic:** Avantgardistic, editorial, European graphic design. Warm off-white + deep terracotta (Option D). No rounded corners anywhere. No red in the error palette — error hue stays within the terracotta family.

**Color tokens:** The shadcn CSS custom properties in `main.css` must be overridden with the Option D values listed in the Color section below. These are set as raw hex values on `:root` (not HSL), documented in that section. All Tailwind classes that reference `hsl(var(--*))` continue to work once the variables are updated.

---

## Scope: Pages in This Phase

| Route | Purpose |
|-------|---------|
| `/login` | Email + password login |
| `/signup` | New account registration |
| `/verify-email-sent` | Post-signup confirmation screen |
| `/verify-email` | Email verification link landing page |
| `/forgot-password` | Request password reset email |
| `/reset-password` | Set new password via reset token |

All six routes are public (unauthenticated). Every other route redirects to `/login` via server-side middleware.

---

## Page Layout

All auth pages share a single centered-card layout:

- Full-viewport height: `min-h-screen`
- Background: `bg-background` (dominant surface)
- Content: single card centered horizontally and vertically (`flex items-center justify-center`)
- Card: `bg-card` surface, `border border-border`, `rounded-none`, no shadow
- Card width: `w-full max-w-sm` (384px) with `p-6` inner padding
- No navigation bar, no sidebar on auth pages
- App name "MovieArchive" as a text heading above the card (not a logo image): `tracking-widest uppercase font-semibold text-2xl`

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, helper text offset |
| sm | 8px | Input-to-error-message gap, label-to-input gap |
| md | 16px | Between form fields, card inner padding sections |
| lg | 24px | Card padding (`p-6`), between form and card heading |
| xl | 32px | Vertical centering buffer above the card on tall viewports |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Touch targets for submit buttons: minimum 44px height (Tailwind: `h-11`)
- Form submit button: full card width (`w-full`)

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 14px | 400 (normal) | 1.5 | `text-sm` |
| Label | 14px | 600 (semibold) | 1.4 | `text-sm font-semibold tracking-widest uppercase` |
| Heading (card title) | 20px | 600 (semibold) | 1.2 | `text-xl font-semibold tracking-wide` |
| Display (app name) | 28px | 600 (semibold) | 1.2 | `text-2xl font-semibold tracking-widest uppercase` |

- Error messages: `text-sm` (14px) weight 400, color `text-[#7A3520]`
- Helper text / muted: `text-sm` color `text-muted-foreground`
- Exactly 2 font weights used across this phase: 400 (body, error, helper text) and 600 (labels, headings, app name).

---

## Color

**Option D — Warm Off-White + Deep Terracotta**

Override the following CSS custom properties in `main.css` `:root` block:

| Variable | Hex Value | Role |
|----------|-----------|------|
| `--background` | `#FAF7F2` | Page background (dominant 60%) |
| `--card` | `#F2EDE6` | Auth card surface (secondary 30%) |
| `--foreground` | `#1A1A18` | Primary text |
| `--card-foreground` | `#1A1A18` | Card text |
| `--primary` | `#C84B31` | Submit button background (accent 10%) |
| `--primary-foreground` | `#FAF7F2` | Submit button label |
| `--border` | `#D4CBBC` | Card border, dividers |
| `--input` | `#D4CBBC` | Input border |
| `--ring` | `#C84B31` | Focus ring |
| `--muted-foreground` | `#8A8070` | Helper text, placeholder, link subtitles |

**Error color (not a CSS variable — use inline Tailwind arbitrary value):**

| Usage | Value |
|-------|-------|
| Error text | `text-[#7A3520]` |
| Error border | `border-[#7A3520]` |
| Error banner left border | `border-l-2 border-[#7A3520]` |

`#7A3520` is a very dark, desaturated terracotta — same hue family as accent (`#C84B31`) but near-brown/dark. Editorial feel, like a stamp or annotation. No red anywhere.

**Accent reserved for:**
- Submit / primary CTA button background (`bg-primary text-primary-foreground`)
- Focus ring on interactive elements (`ring-ring`)

**Error treatment reserved for:**
- Inline form field error text
- Error `<p>` below fields
- Form-level error banner border and text
- No red (`hsl(0 ...)`) used anywhere in this phase

Dark mode tokens are declared in `main.css` `.dark {}` block — do not override them for this phase.

---

## Form Anatomy Contract

Every form on an auth page follows this exact structure:

```
Card
  Card header
    App name (Display)              ← "MovieArchive" — text only, above card
    Page heading (Heading)          ← e.g. "Sign in to your account"
    Subtext (Body/muted)            ← optional secondary line
  Card body
    [Form fields — see below]
    Form-level error banner         ← only when API returns an error
    Submit button (full width)
  Card footer
    Navigation link                 ← e.g. "Don't have an account? Sign up"
```

### Field Anatomy

```
<label>  ← text-sm font-semibold tracking-widest uppercase text-foreground
<input>  ← border border-input bg-background text-sm rounded-none h-10 px-3
           focus-visible:ring-2 ring-ring ring-offset-2 outline-none
<p>      ← text-sm text-[#7A3520] mt-1  (only rendered when field has error)
```

### Form-level error banner (D-08)

Appears directly below the last field, above the submit button. Never a toast.

```
<div class="border-l-2 border-[#7A3520] pl-3 py-2 text-sm text-[#7A3520] bg-transparent">
  {error message from API .message field}
</div>
```

Hidden by default. Rendered only when the API responds with an error. Persists until the user modifies any field or resubmits.

---

## Interaction States

### Inputs

| State | Visual |
|-------|--------|
| Default | `border-input` |
| Focused | `ring-2 ring-ring ring-offset-2 border-input` |
| Error | `border-[#7A3520]` + error `<p>` below |
| Disabled | `opacity-50 cursor-not-allowed` |

### Submit Button

| State | Visual |
|-------|--------|
| Default | `bg-primary text-primary-foreground hover:opacity-90 rounded-none` |
| Loading | Disabled + spinner icon (lucide `Loader2` with `animate-spin`) + label changes to "..." |
| Disabled | `opacity-50 cursor-not-allowed` |

Button is flat — no shadow, no border-radius. Disabled and shows spinner while the API request is in-flight. Re-enabled after response (success or error).

### Rate Limit (D-07)

When API returns HTTP 429:
- Form-level error banner shows: `"Too many attempts. Try again in {X} seconds."` where X is parsed from the `Retry-After` response header.
- Submit button remains disabled for the duration (countdown via `setInterval`; button label shows `"Try again in {X}s"`).

---

## Page-by-Page Copywriting Contract

### `/login`

| Element | Copy |
|---------|------|
| Page heading | `Sign in` |
| Page subtext | `Welcome back` |
| Email label | `Email` |
| Password label | `Password` |
| Submit CTA | `Sign in` |
| Submit loading | `Signing in...` |
| Footer link | `Don't have an account? Sign up` |
| Forgot password link | `Forgot password?` (right-aligned, below password field) |
| Error — invalid credentials | `Invalid email or password.` |
| Error — account not verified | `Please verify your email before signing in.` |
| Error — rate limit | `Too many attempts. Try again in {X} seconds.` |
| Error — generic | `Something went wrong. Please try again.` |

### `/signup`

| Element | Copy |
|---------|------|
| Page heading | `Create an account` |
| Page subtext | `Enter your details to get started` |
| Email label | `Email` |
| Password label | `Password` |
| Submit CTA | `Create account` |
| Submit loading | `Creating account...` |
| Footer link | `Already have an account? Sign in` |
| Client error — invalid email | `Enter a valid email address.` |
| Client error — password too short | `Password must be at least 8 characters.` |
| Error — email already registered | `An account with this email already exists.` |
| Error — generic | `Something went wrong. Please try again.` |

### `/verify-email-sent` (D-09)

| Element | Copy |
|---------|------|
| Page heading | `Check your inbox` |
| Body | `We sent a verification link to your email address. Click the link to activate your account.` |
| Subtext | `If you don't see it, check your spam folder.` |
| No CTA button | (informational page only — no form) |
| Footer link | `Back to sign in` |

### `/verify-email`

| Element | Copy |
|---------|------|
| Loading state heading | `Verifying your email...` |
| Success heading | `Email verified` |
| Success body | `Your account is active. You can now sign in.` |
| Success CTA | `Sign in` |
| Error heading | `Verification failed` |
| Error body — expired | `This verification link has expired. Request a new one from the sign-in page.` |
| Error body — already used | `This link has already been used. Your account may already be active.` |
| Error body — generic | `Something went wrong. Please try again or contact support.` |

### `/forgot-password`

| Element | Copy |
|---------|------|
| Page heading | `Reset your password` |
| Page subtext | `Enter your email and we'll send you a reset link.` |
| Email label | `Email` |
| Submit CTA | `Send reset link` |
| Submit loading | `Sending...` |
| Success state (replaces form) | `If an account exists for that email, a reset link is on its way.` |
| Footer link | `Back to sign in` |
| Error — rate limit | `Too many attempts. Try again in {X} seconds.` |
| Error — generic | `Something went wrong. Please try again.` |

**Note:** Backend always returns 200 OK (enumeration protection, AUTH-07). The success state is shown after any non-error API response regardless of whether the email exists.

### `/reset-password`

| Element | Copy |
|---------|------|
| Page heading | `Set new password` |
| Page subtext | `Choose a strong password for your account.` |
| Password label | `New password` |
| Confirm password label | `Confirm password` |
| Submit CTA | `Reset password` |
| Submit loading | `Resetting...` |
| Success state (replaces form) | `Password updated. You can now sign in with your new password.` |
| Success CTA | `Sign in` |
| Error — token expired | `This reset link has expired. Request a new one.` |
| Error — token already used | `This link has already been used.` |
| Client error — passwords don't match | `Passwords do not match.` |
| Client error — password too short | `Password must be at least 8 characters.` |
| Error — generic | `Something went wrong. Please try again.` |

---

## Client-Side Validation Rules

Validation runs on submit (not on each keystroke). Errors clear when the user modifies the relevant field.

| Field | Rule | Error Message |
|-------|------|---------------|
| Email | Non-empty + basic regex (`/.+@.+\..+/`) | `Enter a valid email address.` |
| Password (signup) | Minimum 8 characters | `Password must be at least 8 characters.` |
| Password (reset — new) | Minimum 8 characters | `Password must be at least 8 characters.` |
| Confirm password | Must match new password field | `Passwords do not match.` |
| All required fields | Non-empty | `This field is required.` |

Login password field: no client-side length validation (avoids leaking policy info; let backend reject).

---

## Navigation / Redirect Contract

| Trigger | Redirect |
|---------|---------|
| Successful login | `/` (home / dashboard — first authenticated page) |
| Successful signup | `/verify-email-sent` (D-09) |
| Successful email verification | Show success state on `/verify-email`, CTA links to `/login` |
| Successful password reset | Show success state on `/reset-password`, CTA links to `/login` |
| Logout | `/login` |
| Authenticated user visits `/login` or `/signup` | Redirect to `/` |
| Unauthenticated user visits protected route | Redirect to `/login` |

---

## Accessibility

- All inputs have an associated `<label>` with matching `for`/`id`.
- Error messages are associated via `aria-describedby` pointing to the error `<p>` element.
- Form-level error banner has `role="alert"` so screen readers announce it immediately.
- Submit button disabled state uses `disabled` attribute (not just visual opacity).
- Focus order follows DOM order (top to bottom through fields, then button, then footer link).
- Minimum contrast ratio 4.5:1 for all text. `#7A3520` on `#FAF7F2` background: contrast ratio ~7.2:1 (passes AAA). `#C84B31` on `#FAF7F2` background: ~4.6:1 (passes AA). `#1A1A18` on `#FAF7F2`: ~18.5:1 (passes AAA).

---

## Component Inventory

Components to build in this phase (all new — no existing project components):

| Component | Type | Backed By |
|-----------|------|-----------|
| `AuthCard` | Layout wrapper | Plain div |
| `FormField` | Field + label + error | Plain div + radix-vue label if needed |
| `InputText` | Text input | Native `<input>` with Tailwind classes |
| `ButtonPrimary` | Submit CTA | Native `<button>` |
| `FormErrorBanner` | API error display | Plain div |
| `SpinnerIcon` | Loading indicator | lucide `Loader2` with `animate-spin` |

No third-party UI blocks beyond what is already installed (`radix-vue`, `lucide-vue-next`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (manual setup, no CLI add) | not applicable |
| radix-vue | primitives via direct npm install | not applicable — installed package, not registry block |
| lucide-vue-next | icon components via direct npm install | not applicable — installed package, not registry block |

No third-party shadcn registries declared for this phase.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Inline form errors, no toasts (D-08) | CONTEXT.md |
| Redirect to /verify-email-sent after signup (D-09) | CONTEXT.md |
| API error format `{"message": "..."}` (D-10) | CONTEXT.md |
| Rate limit 429 + Retry-After → "Too many attempts. Try again in X seconds." (D-07) | CONTEXT.md |
| Auth pages listed (/login, /signup, /verify-email, /verify-email-sent, /forgot-password, /reset-password) | CONTEXT.md |
| shadcn token set (CSS custom properties) | frontend/assets/css/main.css |
| Tailwind color mappings | frontend/tailwind.config.ts |
| radix-vue + lucide-vue-next installed | frontend/package.json |
| darkMode: 'class' | frontend/tailwind.config.ts |
| Option D color palette (off-white + terracotta) | User design direction 2026-05-15 |
| No rounded corners (`rounded-none`) | User design direction 2026-05-15 |
| Editorial tracking on headings/labels | User design direction 2026-05-15 |
| Error treatment: dark terracotta `#7A3520`, no red | User design direction 2026-05-15 |

---

## Revision Log

| Date | Change | Reason |
|------|--------|--------|
| 2026-05-15 | Collapsed font weights from 4 to 2 (removed 500/medium for labels, changed 700/bold display to 600/semibold) | Checker blocking issue: Dimension 4 Typography — maximum 2 weights |
| 2026-05-15 | Updated label Tailwind class from `font-medium` to `font-semibold` in Typography table and Field Anatomy | Follows weight collapse above |
| 2026-05-15 | Updated display Tailwind class from `text-2xl font-bold` to `text-2xl font-semibold` | Follows weight collapse above |
| 2026-05-15 | Copywriting recommendation (Sign in CTA) — kept as-is | Button labels are short by convention; adding "to MovieArchive" would be verbose in a labeled card context |
| 2026-05-15 | User design direction: avantgardistic editorial, Option D (warm off-white + terracotta), no rounded corners, no red errors | User selected Option D palette; full color section replaced with hex values; all `rounded-*` replaced with `rounded-none`; error treatment changed to dark terracotta `#7A3520` left-border banner; heading/label tracking classes added; Design System section updated with aesthetic statement |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
