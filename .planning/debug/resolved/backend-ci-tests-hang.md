---
status: resolved
trigger: "CI job stuck on GitHub Actions run 33266244052 (push to main, commit 1cdaa77 'docs: update retrospective for v1.1') — Backend CI 'Run tests' step running far longer than normal"
created: 2026-08-29
updated: 2026-08-29
---

## Symptoms

- **Expected behavior**: Backend CI's "Run tests" step (Gradle test task, full backend suite: JUnit 5 + Testcontainers postgres:16-alpine + OpenSearch 2.x) normally completes within a few minutes, same as every prior successful Backend CI run on this repo (see `gh run list` history for `feat(search): add relevance sort` etc. — all completed in 1-3 minutes total job time historically, though those were Frontend CI runs; Backend CI has historically also been fast per project docs).
- **Actual behavior**: As of last check, the job (run 33266244052, job 99136703154) had been in the "Run tests" step for 58+ minutes without completing, failing, or timing out. Steps before it (Set up job, Checkout, Set up Java 25, Make Gradle wrapper executable, Compile (lint)) all completed normally (green checkmarks). Steps after it (Generate JaCoCo report, Enforce coverage thresholds, Upload coverage report, Build Spring Boot JAR, Upload JAR artifact) have not started.
- **Error messages**: None yet — no failure, just apparent non-completion. `gh run view --log --job=99136703154` returns "job ... is still in progress; logs will be available when it is complete" — no way to tail live logs via the `gh` CLI as used so far.
- **Timeline**: First observed 2026-08-29, same push as the E2E flake (see `.planning/debug/e2e-login-redirect-flake.md` — separate, unrelated symptom, tracked in its own session). This is the first real CI run against `origin/main` in a long time (previous successful runs are all from 2026-05-19/20, before the entire v1.1 milestone's local-only work). STATE.md/PROJECT.md document a KNOWN pre-existing issue: "full-suite cross-class test isolation flakiness in `./gradlew check`" — deferred tech debt from Phase 15/16, described as causing flaky failures, not explicitly described as causing a multi-hour hang. Worth checking whether this is the same root cause manifesting differently in CI (e.g. Testcontainers reuse/port contention, thread pool exhaustion, or a genuine deadlock) vs. a new, distinct problem introduced by v1.1's async enrichment/bulk-import additions (new executors, SSE emitters, `Thread.sleep`-based pacing in `WikipediaClient`/`WikiReloadService`/`BulkImportService`).
- **Reproduction**: Not yet reproduced locally — orchestrator has not yet run `./gradlew test` locally to see if it hangs the same way outside CI. GitHub Actions runners are more resource-constrained (2-core standard runners) than a typical local dev machine, which could matter if the hang is resource-contention-related (e.g. Testcontainers spinning up Postgres + OpenSearch containers under memory/CPU pressure).

## Current Focus

- **bug_class**: Bohrbug — CONFIRMED deterministic (reproduced identically on a local 10-core machine at the exact same test method; not resource/timing-dependent)
- **status**: root cause confirmed, proceeding to fix

```yaml
reasoning_checkpoint:
  hypothesis: "WikiReloadControllerTest.shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete() hangs the entire CI test step because it calls wikiReloadProgressService.complete(userId) expecting it to trigger the servlet's real ASYNC completion (via emitter.complete()), but the 2026-08-28 wiki-reload-progress-blind-window fix removed that call from complete() — so mockMvc.perform(asyncDispatch(mvcResult))'s underlying CountDownLatch.await() never counts down and blocks forever, and Gradle's single-threaded test executor means this one stuck test blocks the whole ./gradlew test task indefinitely."
  confirming_evidence:
    - "Two jstack thread dumps 58s apart show the SAME thread (tid 0x0000000104499ad0) parked on the SAME CountDownLatch object at the SAME stack frame (DefaultMvcResult.awaitAsyncDispatch, called from WikiReloadControllerTest.java:339), with zero log progress between dumps — direct, repeatable observation, not inference."
    - "WikiReloadProgressService.complete()'s own javadoc explicitly documents removing the emitter.complete() call as of the 2026-08-28 fix, for a stated, legitimate production reason (persistent per-userId SSE connection across multiple runs)."
    - "Control comparison: BulkImportControllerTest's structurally-identical sibling test still passes because BulkImportProgressService.complete() legitimately still calls emitter.complete() (per-batch registry, closing on batch completion is correct there) — confirming the wiki-reload case is the outlier, not a universal MockMvc/SSE issue."
  falsification_test: "If WikiReloadProgressService.complete() is changed to call emitter.complete() again (reverting the 2026-08-28 fix) and the CI hang disappears, that would falsify 'complete() no longer closes the emitter' as unrelated — but doing so would reintroduce the wiki-reload-progress-blind-window production bug, so the correct falsification instead is: manually calling emitter.complete() via a new test-only accessor makes asyncDispatch() return promptly (proving the latch mechanism itself works and the ONLY missing piece is the emitter completion signal)."
  fix_rationale: "The fix must NOT revert WikiReloadProgressService.complete()'s production behavior (that would reintroduce the blind-window bug). Instead, add a minimal package-private test-support method that completes the registered emitter directly (simulating a real client disconnect, the only mechanism that still completes this SSE connection in production), and update the test to call that instead of complete() to trigger the async redispatch. complete()'s own state/broadcast logic remains fully covered by the separate pure-unit WikiReloadProgressServiceTest, so no coverage is lost."
  blind_spots: "Have not yet run the fixed test in isolation to 100% confirm asyncDispatch() unblocks with the new method (planned next: run WikiReloadControllerTest alone, then full suite). Have not confirmed whether any OTHER test in the suite has the same now-stale assumption about complete() closing an emitter (grep already shows this is the only MockMvc-level caller of wikiReloadProgressService.complete() outside pure-unit WikiReloadProgressServiceTest, so risk is low but not zero)."
  candidate_causes:
    - "code: stale test assumption not updated when WikiReloadProgressService.complete()'s production behavior changed (2026-08-28 fix) — test/production behavior drift"
    - "config: no timeout-minutes anywhere in .github/workflows/backend-ci.yml (config/process category) — independently confirmed, means no auto-recovery regardless of the code bug"
  and_gate: "No — these are two independent, additive findings, not a conjunction required to explain the hang. The stale test alone fully explains the hang (sufficient cause). The missing timeout-minutes is a separate, orthogonal defense-in-depth gap that made the hang unbounded rather than fail-fast/bounded — fixing either one prevents CI from being stuck for 1.5+ hours, but only fixing the test prevents the underlying test failure itself. Both will be fixed."
```

- **next_action**: DONE. CI run 33271162681 (commit 502dc2c) confirmed: "Run tests" step completed in 2m 18s, no hang. Build result FAILED (99 pre-existing FK-isolation failures, tracked separately, expected per user's checkpoint response) — this does not block resolution since this session's scope is the hang only. User's checkpoint response pre-authorized marking resolved once the hang was confirmed gone. Proceeding to archive_session.

## Evidence

- timestamp: 2026-08-29T19:14Z
  checked: `gh run view 33266244052 --json status,jobs` (live query, not stale)
  found: Job 99136703154 is still `status: in_progress`. "Run tests" step (step 6) `startedAt: 2026-08-29T17:41:29Z`, still running with no `completedAt`. Current time 19:14:06Z → step has been running **~1h33m**. All prior steps (Set up job, Checkout, Set up Java 25, Make Gradle wrapper executable, Compile lint) completed normally in under 2 minutes total. No later steps (JaCoCo, coverage gate, build JAR) have started.
  implication: Confirms orchestrator's report is still accurate and this is a genuine hang, not a stale/completed run. A full backend suite normally finishes in a few minutes per Symptoms — 1h33m on one step is anomalous by ~30-50x.

- timestamp: 2026-08-29T19:14Z
  checked: `.github/workflows/backend-ci.yml` in full
  found: No `timeout-minutes` key anywhere in the workflow — not at job level, not on the "Run tests" step (`run: ./gradlew test --no-daemon`) or any other step. `runs-on: ubuntu-latest` (standard 2-core runner). Job name is "Lint · Test · Coverage · Build".
  implication: Confirms the orchestrator's static-analysis suspicion #1. Nothing will auto-kill this run except GitHub Actions' hard default job timeout (360 minutes / 6 hours). This is a process gap regardless of root cause — should be fixed with a `timeout-minutes` safety net independent of what's causing the hang.

- timestamp: 2026-08-29T19:20Z
  checked: grep for `Thread.sleep`, `SseEmitter`, pacing config in `src/main` and `src/test`; `application-test.properties` pacing overrides; whether all `@SpringBootTest` classes touching `WikipediaClient`/`WikiReloadService`/`BulkImportService` override `wikipedia.base-url`/`wikidata.sparql-base-url` via WireMock
  found: `application-test.properties` sets all pacing delays to 0-1ms globally (deliberate, documented "RESEARCH.md Pitfall 5" fast-suite default). The 4 test classes that reference Wikipedia-related services but do NOT override `wikipedia.base-url` (`BulkImportProgressServiceTest`, `BulkImportServiceTest`, `WikiReloadServiceTest`, `EnrichmentServiceTest`) are all pure `@ExtendWith(MockitoExtension.class)` unit tests with `@Mock` dependencies — no real Spring context, no real network path. All 7 `@SpringBootTest`/MockMvc classes that DO need a real `WikipediaClient` bean correctly override the base URL to a WireMock instance.
  implication: Eliminates "un-mocked real network call to real Wikipedia/Wikidata" as the hang mechanism — pacing and WireMock stubbing are correctly wired everywhere they're needed. Orchestrator's suspicion #3 (real network call) is ruled out by direct code inspection.

- timestamp: 2026-08-29T19:16-19:20Z
  checked: attempted local reproduction — `cd backend && ./gradlew test --no-daemon` (local HEAD 77412fb has byte-identical `backend/` tree to the CI commit 1cdaa77, confirmed via `git diff 1cdaa77 HEAD -- backend` = empty)
  found: FIRST attempt failed fast (~7s) with `IllegalStateException: Could not find a valid Docker environment` — a stale `~/.testcontainers.properties` (cached `UnixSocketClientProviderStrategy` pointing at `/var/run/docker.sock`, which doesn't exist locally; this machine uses OrbStack at `~/.orbstack/run/docker.sock`). This is a LOCAL environment-only confound, unrelated to CI (GH Actions ubuntu-latest has a real Docker daemon at the standard socket path).
  implication: local repro attempt #1 is not informative about the CI hang either way — needed to fix local Docker detection first before treating a local result as evidence.

- timestamp: 2026-08-29T19:20-19:20Z
  checked: SECOND local repro attempt — moved `~/.testcontainers.properties` aside, re-ran with `DOCKER_HOST=unix:///Users/simonreich/.orbstack/run/docker.sock ./gradlew test --no-daemon -i`
  found: Docker environment detection now succeeds (Postgres + OpenSearch Testcontainers start fine, `HealthSmokeTest` passes). Suite proceeds normally through ~15 test classes (SearchControllerTest, DashboardControllerTest, ReindexControllerTest, WikiReloadControllerTest's queue/stop tests) over ~2 minutes of real wall-clock progress, matching the "few minutes for full suite" baseline — then stops making log progress entirely partway through `WikiReloadControllerTest`.
  implication: **Local reproduction of the hang, on a 10-core machine (not resource-constrained like CI's 2-core runner)** — rules out "CI runner resource contention / Testcontainers port contention under constrained CPU" (orchestrator's suspicion) as the (sole) cause. This is a genuine, deterministic code bug, not an environment-resource issue.

- timestamp: 2026-08-29T19:18:42Z and 2026-08-29T19:19:40Z (two thread dumps, ~58s apart)
  checked: `jstack <gradle-test-executor-pid>` on the hung JVM, taken twice to confirm the thread is genuinely stuck (not just slow)
  found: Same "Test worker" thread (tid 0x0000000104499ad0, nid 5123), same exact stack both times, `elapsed` grew from 53.50s to 111.13s with ZERO log output in between (line count static at 422 both times). Stack: `TIMED_WAITING (parking)` on a `CountDownLatch$Sync` (`<0x000000047f76fd10>`) inside `org.springframework.test.web.servlet.DefaultMvcResult.awaitAsyncDispatch()` → `getAsyncResult()` → `MockMvcRequestBuilders.asyncDispatch()`, called from `WikiReloadControllerTest.shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete(WikiReloadControllerTest.java:339)`.
  implication: **Root cause location pinpointed with direct, unambiguous, repeatable evidence.** This single test method is where the entire suite hangs indefinitely — no other work is happening (single-threaded `SameThreadHierarchicalTestExecutorService`, confirming Gradle's default sequential test execution means one stuck test blocks the whole `./gradlew test` task forever).

- timestamp: 2026-08-29T19:21Z
  checked: `WikiReloadControllerTest.java:325-341` (`shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete`) and `WikiReloadProgressService.java` (`complete(UUID userId)` method + its javadoc, lines 150-197)
  found: The test calls `wikiReloadProgressService.complete(user.getId())` then `mockMvc.perform(asyncDispatch(mvcResult))`, expecting `complete()` to trigger the servlet's real ASYNC completion (this test was written as a regression test for an earlier "AuthorizationDeniedException-on-SSE-completion" bug, per its own docstring). But `WikiReloadProgressService.complete()`'s javadoc explicitly documents a LATER fix ("wiki-reload-progress-blind-window, 2026-08-28") that intentionally REMOVED the `emitter.complete()` call from this method — for a legitimate production reason (the frontend keeps one persistent SSE connection per userId across multiple future runs; closing it when one run finishes broke all subsequent runs' progress reporting). `complete()` now only broadcasts a "complete" SSE *event* over the still-open connection; it never calls `emitter.complete()`/`completeWithError()` on the underlying `SseEmitter`, so the servlet's async context is NEVER actually completed. `awaitAsyncDispatch()`'s `CountDownLatch` is only counted down when the container's real async completion fires — which now never happens for wiki-reload's `complete()` path — so the test hangs forever.
  implication: **CONFIRMED root cause.** This is a genuine regression: the 2026-08-28 "wiki-reload-progress-blind-window" fix correctly changed `WikiReloadProgressService.complete()`'s production behavior but did not update this now-stale test, which still depends on the OLD behavior. Confirmed by a working control: `BulkImportControllerTest`'s structurally-identical sibling test (`shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete`, lines 318-361) still passes because `BulkImportProgressService.complete()` legitimately still calls `emitter.complete()` (bulk-import's registry is keyed per-batch, so closing on that batch's completion is correct — explicitly contrasted in `WikiReloadProgressService`'s own javadoc, lines 39-46). `WikiReloadProgressService.complete()`'s own business logic (state/broadcast correctness) IS separately covered by the pure-unit `WikiReloadProgressServiceTest`, so fixing this MockMvc-level test to use a different completion-triggering mechanism loses no coverage.
  implication (process gap): No `timeout-minutes` anywhere in `.github/workflows/backend-ci.yml` (confirmed earlier) means nothing auto-kills a hung `./gradlew test` step — this single stuck test blocks CI indefinitely instead of failing fast. This should be fixed as defense-in-depth regardless of the code fix.

- timestamp: 2026-08-29T19:37Z
  checked: `gh run view 33271162681 --json status,conclusion,jobs` (new Backend CI run, headSha 502dc2c, triggered by pushing the fix) + full "Run tests" step log via `gh run view 33271162681 --job=99149765275 --log`
  found: "Run tests" step ran 19:34:58Z → 19:37:16Z (2m 18s) — normal timeframe, no hang. Overall job conclusion: `failure`. Log shows `230 tests completed, 99 failed`, `BUILD FAILED in 2m 17s`, all failures are the same `DataIntegrityViolationException`/`ConstraintViolationException` FK-isolation pattern (SearchControllerTest, SettingsIntegrationTest, UserControllerTest, etc.) as the local verification run — same failure count (99). `grep -i WikiReloadControllerTest` over the full step log returns zero matches (Gradle only prints FAILED tests by default) — confirms this test class had no failures in CI, i.e. the fixed test ran and passed cleanly in the real CI environment, not just locally.
  implication: **CI-level confirmation that the hang is fixed.** The exact failure mode this session investigates (indefinite non-completion of the "Run tests" step) is gone — the step now completes in a normal ~2 minute window. The BUILD FAILED outcome is the separate, pre-existing, already-tracked FK-constraint test-isolation flakiness (`.planning/debug/fullsuite-fk-isolation-flakiness.md`), not a regression from this fix — exactly as anticipated in the user's checkpoint response.

- timestamp: 2026-08-29T19:14Z (independent re-verification, before push)
  checked: `gh run view 33266244052 --json status,conclusion,jobs` (live query, re-confirming user's claim before acting)
  found: Run 33266244052 `status: completed`, `conclusion: cancelled`; job 99136703154 "Run tests" step `conclusion: cancelled`, `completedAt: 2026-08-29T19:31:53Z`.
  implication: Confirms the user's report that the stuck run was already cancelled — safe to proceed with commit/push without needing to cancel it myself.

## Eliminated

- hypothesis: Real, un-mocked network call from a test to the actual Wikipedia/Wikidata API (CLAUDE.md mocking-mandate violation)
  evidence: All `@SpringBootTest`/MockMvc test classes exercising `WikipediaClient` correctly override `wikipedia.base-url`/`wikidata.sparql-base-url` to a WireMock instance; the 4 classes that don't override it are pure Mockito unit tests with no real Spring context or network path.
  timestamp: 2026-08-29T19:20Z

- hypothesis: Testcontainers reuse/port-contention or general resource starvation on GH Actions' 2-core runners
  evidence: The hang was reproduced identically on a local 10-core dev machine (not resource-constrained), at the exact same test method, ruling out CI-runner-resource-contention as the (sole) cause. Also: `AbstractOpenSearchTest`'s `.withReuse(true)` has no effect without `testcontainers.reuse.enable=true` in `~/.testcontainers.properties` (not set in CI) — it silently falls back to non-reuse behavior, not a hang.
  timestamp: 2026-08-29T19:20Z

## Resolution

- root_cause: "`WikiReloadControllerTest.shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete()` (backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java:325-341) calls `mockMvc.perform(asyncDispatch(mvcResult))` after `wikiReloadProgressService.complete(userId)`, but the 2026-08-28 'wiki-reload-progress-blind-window' fix intentionally removed `WikiReloadProgressService.complete()`'s call to `emitter.complete()` (correct production behavior — the SSE connection must survive across multiple future runs) without updating this stale regression test, whose assumption (`complete()` triggers real async completion) is now false. `awaitAsyncDispatch()`'s `CountDownLatch` never counts down, hanging the single-threaded Gradle test executor — and therefore the entire `./gradlew test` CI step — indefinitely; no `timeout-minutes` is set anywhere in `.github/workflows/backend-ci.yml` to auto-recover."
- fix: "Added package-private `WikiReloadProgressService.completeAllEmittersForTest(UUID)` that completes the actual registered `SseEmitter` directly (simulating a real client disconnect — the only mechanism that still completes this SSE connection in production). Updated `WikiReloadControllerTest.shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete()` to call this new method instead of the now-emitter-preserving `complete(userId)`, restoring the test's original async-redispatch regression coverage. Also added `timeout-minutes: 20` to the `ci` job in `.github/workflows/backend-ci.yml` as an independent defense-in-depth safety net (orthogonal config gap — does not fix the bug, prevents future hangs of any cause from blocking CI for hours unbounded)."
- verification: "(1) Isolated run of `WikiReloadControllerTest` alone (`./gradlew test --tests de.moviearchive.admin.WikiReloadControllerTest`): BUILD SUCCESSFUL in 34s, JUnit XML confirms `tests=\"8\" failures=\"0\" errors=\"0\"`, `shouldNotDenyAuthorization_onAsyncRedispatch_afterEmitterComplete()` completed in 0.47s (was: indefinite hang). (2) Full-suite run (`./gradlew test`, same DOCKER_HOST-corrected local env, same commit): suite ran cleanly through and PAST `WikiReloadControllerTest` into `AuthIntegrationTest` and beyond — the exact point that hung forever pre-fix — and the whole build FINISHED (not hung) in 1m 13s. Build result was FAILED with 99 pre-existing failures, but these are unrelated: all failures are `DataIntegrityViolationException: ... violates foreign key constraint \"bulk_import_line_user_id_fkey\"` on FK-ordering test cleanup — this is STATE.md's already-documented, already-acknowledged, out-of-scope deferred tech debt (`Phase 15/deferred-items.md: Full-suite ./gradlew check cross-class test isolation flakiness`, tracked in its own session `.planning/debug/fullsuite-fk-isolation-flakiness.md`), pre-existing and unrelated to this session's fix (no file touched by this fix relates to bulk-import/user cleanup ordering). (3) CI-level confirmation, post-push: run 33271162681 (commit 502dc2c) — 'Run tests' step completed in 2m 18s (19:34:58Z-19:37:16Z), `230 tests completed, 99 failed` (same failure count as local run 2), `WikiReloadControllerTest` had zero failures in the CI log. The HANG itself — the specific bug this session investigates — is confirmed fixed both locally and in the real CI environment: a run that used to never finish (1h33m+ before manual cancellation) now finishes, pass or fail, in ~2-3 minutes."
- files_changed:
  - backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java
  - backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java
  - .github/workflows/backend-ci.yml
