---
phase: 04-opensearch-indexing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/de/moviearchive/AbstractOpenSearchTest.java
  - backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java
  - backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java
  - backend/src/main/resources/opensearch/movies-index.json
  - backend/src/test/resources/application-test.properties
autonomous: true
requirements: [IDX-01, IDX-02, IDX-03, IDX-04]
must_haves:
  truths:
    - "An OpenSearch Testcontainer starts once per JVM run and Spring context binds to it"
    - "IndexingIntegrationTest and ReindexControllerTest compile and run as @Disabled stubs"
    - "movies-index.json skeleton exists on the classpath"
  artifacts:
    - path: "backend/src/test/java/de/moviearchive/AbstractOpenSearchTest.java"
      provides: "OpenSearch Testcontainers base class (GenericContainer, no new dependency)"
      contains: "GenericContainer"
    - path: "backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java"
      provides: "@Disabled stubs for IDX-01, IDX-02, IDX-03"
      contains: "@Disabled"
    - path: "backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java"
      provides: "@Disabled stubs for IDX-04"
      contains: "@Disabled"
    - path: "backend/src/main/resources/opensearch/movies-index.json"
      provides: "Index definition skeleton (filled in plan 04-02)"
      contains: "custom_english_analyzer"
  key_links:
    - from: "AbstractOpenSearchTest"
      to: "application-test.properties opensearch.host/port"
      via: "@DynamicPropertySource registry override"
      pattern: "opensearch.host"
---

<objective>
Create the OpenSearch test infrastructure and @Disabled test scaffolding so that Wave 2 and Wave 3 plans have failing tests to make green (Nyquist compliance).

Purpose: The validation strategy (04-VALIDATION.md) requires every Phase 4 task to have an automated verify command. This plan creates those test files first, as @Disabled stubs, plus the OpenSearch Testcontainers base class that all integration tests extend. No production code is written here.

Output: AbstractOpenSearchTest.java, IndexingIntegrationTest.java (@Disabled), ReindexControllerTest.java (@Disabled), movies-index.json skeleton, application-test.properties stub properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-opensearch-indexing/04-CONTEXT.md
@.planning/phases/04-opensearch-indexing/04-PATTERNS.md
@.planning/phases/04-opensearch-indexing/04-VALIDATION.md

<interfaces>
<!-- Existing test base class the new OpenSearch base extends. Use directly — no exploration needed. -->

AbstractIntegrationTest (backend/src/test/java/de/moviearchive/AbstractIntegrationTest.java):
- @SpringBootTest @ActiveProfiles("test") abstract class
- static PostgreSQLContainer<?> postgres started in a static{} block
- @DynamicPropertySource overrideDataSourceProperties(DynamicPropertyRegistry) sets spring.datasource.* from the postgres container

Container image (from docker-compose.yml, must match): opensearchproject/opensearch:2.19.0
GenericContainer env required: discovery.type=single-node, DISABLE_SECURITY_PLUGIN=true
Wait strategy: Wait.forHttp("/_cluster/health").forStatusCodeMatching(c -> c == 200 || c == 401)
Spring properties bound by OpenSearchConfig (plan 04-02): opensearch.host, opensearch.port (application.properties lines 18-19)

Test data creation pattern (EnrichmentIntegrationTest analog): new User(email, "$2a$10$placeholderHash"); setStatus(UserStatus.ACTIVE); userRepository.save(user)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbstractOpenSearchTest base class + test-profile properties</name>
  <files>backend/src/test/java/de/moviearchive/AbstractOpenSearchTest.java, backend/src/test/resources/application-test.properties</files>
  <read_first>
    - backend/src/test/java/de/moviearchive/AbstractIntegrationTest.java (exact structure to extend — static container block + @DynamicPropertySource)
    - backend/src/test/resources/application-test.properties (current test properties; add opensearch stubs without removing existing keys)
    - .planning/phases/04-opensearch-indexing/04-PATTERNS.md (AbstractOpenSearchTest section + Testcontainers Static Singleton Pattern section)
    - C:\Users\simon\git\private\movie-archive\docker-compose.yml (confirm OpenSearch image tag is opensearchproject/opensearch:2.19.0)
  </read_first>
  <action>
    Create `AbstractOpenSearchTest` in package `de.moviearchive` that `extends AbstractIntegrationTest`. Add a `static final GenericContainer<?> opensearch` field initialised in a `static {}` block (same singleton pattern as the existing Postgres container — NOT `@Testcontainers`/`@Container`). Configure the container with image string `"opensearchproject/opensearch:2.19.0"`, `.withExposedPorts(9200)`, `.withEnv("discovery.type", "single-node")`, `.withEnv("DISABLE_SECURITY_PLUGIN", "true")`, and `.waitingFor(Wait.forHttp("/_cluster/health").forStatusCodeMatching(c -> c == 200 || c == 401))`, then `.start()`. Add a `@DynamicPropertySource` static method `overrideOpenSearchProperties(DynamicPropertyRegistry registry)` that registers `opensearch.host` -> `opensearch::getHost` and `opensearch.port` -> `() -> opensearch.getMappedPort(9200)`. Use `org.testcontainers.containers.GenericContainer`, `org.testcontainers.containers.wait.strategy.Wait` (no new Gradle dependency — GenericContainer is already on the test classpath via spring-boot-testcontainers). Then append `opensearch.host=localhost` and `opensearch.port=9200` to `application-test.properties` (placeholder values overridden at runtime by the @DynamicPropertySource) so the Spring context can start.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - AbstractOpenSearchTest.java contains `extends AbstractIntegrationTest`
    - AbstractOpenSearchTest.java contains `new GenericContainer` with literal `opensearchproject/opensearch:2.19.0`
    - AbstractOpenSearchTest.java contains `DISABLE_SECURITY_PLUGIN` and `discovery.type`
    - AbstractOpenSearchTest.java contains a `@DynamicPropertySource` method registering both `opensearch.host` and `opensearch.port`
    - application-test.properties contains a line `opensearch.host=localhost` and a line `opensearch.port=9200`
    - `./gradlew compileTestJava` exits 0
    - File does NOT contain `client.ping(` or any startup connectivity check
  </acceptance_criteria>
  <done>AbstractOpenSearchTest compiles, starts a singleton OpenSearch GenericContainer, and binds opensearch.host/port via @DynamicPropertySource; test properties stub added.</done>
</task>

<task type="auto">
  <name>Task 2: @Disabled test stubs for IDX-01..04 + index JSON skeleton</name>
  <files>backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java, backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java, backend/src/main/resources/opensearch/movies-index.json</files>
  <read_first>
    - backend/src/test/java/de/moviearchive/movie/EnrichmentIntegrationTest.java (analog for IndexingIntegrationTest — class declaration, @BeforeEach cleanDb, test user creation)
    - backend/src/test/java/de/moviearchive/movie/MovieControllerTest.java (analog for ReindexControllerTest — @AutoConfigureMockMvc, loginAndGetToken helper, two-user 403 pattern)
    - .planning/phases/04-opensearch-indexing/04-VALIDATION.md (Per-Task Verification Map — exact test method names required)
    - .planning/phases/04-opensearch-indexing/04-RESEARCH.md (Pattern 2 — movies-index.json structure)
    - .claude/data-model.md (authoritative field list — skeleton only references it; full mapping filled in plan 04-02)
  </read_first>
  <action>
    Create `de.moviearchive.indexing.IndexingIntegrationTest` extending `AbstractOpenSearchTest`, with `@Disabled("Wave 2 — plan 04-02")` on the class. Declare empty `@Test` methods (each body may be empty or `// TODO 04-02`) with these EXACT names so the Nyquist verify commands resolve: `shouldIndexFilm_afterPostgresPersist`, `shouldLeaveIndexedAtNull_whenOsFails`, `shouldCreateIndex_whenNotExists`, `shouldNotThrow_whenIndexAlreadyExists`, `shouldNormalizeAccents`, `shouldStemEnglishWords`. Add `@Autowired MovieRepository`, `@Autowired UserRepository` fields and a `@BeforeEach` stub (signature only; cleanup logic filled in 04-02). Create `de.moviearchive.admin.ReindexControllerTest` extending `AbstractOpenSearchTest` with `@AutoConfigureMockMvc` and `@Disabled("Wave 3 — plan 04-03")` on the class, with empty `@Test` methods named EXACTLY: `shouldReturn403_whenUserMismatch`, `shouldFullReindex`, `shouldIndexOnlyPending`, `shouldReturnIndexedCount`. Create `backend/src/main/resources/opensearch/movies-index.json` as a minimal valid skeleton: a JSON object with `settings.analysis.filter.stop_english` (type `stop`, stopwords `_english_`), `settings.analysis.analyzer.custom_english_analyzer` (type `custom`, tokenizer `standard`, filter array `["asciifolding","lowercase","elision","stop_english","kstem"]`), and a `mappings.properties` object containing ONLY `title`, `original_title`, and `personal_notes` as placeholders. Add a top-level JSON comment-equivalent is not valid JSON — instead add a `_skeleton` boolean field set to true at the root so plan 04-02 can detect/replace it. The remaining 40+ fields are intentionally deferred to plan 04-02.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.indexing.*" --tests "de.moviearchive.admin.*"</automated>
  </verify>
  <acceptance_criteria>
    - IndexingIntegrationTest.java contains `extends AbstractOpenSearchTest` and class-level `@Disabled`
    - IndexingIntegrationTest.java contains all six method names: shouldIndexFilm_afterPostgresPersist, shouldLeaveIndexedAtNull_whenOsFails, shouldCreateIndex_whenNotExists, shouldNotThrow_whenIndexAlreadyExists, shouldNormalizeAccents, shouldStemEnglishWords
    - ReindexControllerTest.java contains `extends AbstractOpenSearchTest`, `@AutoConfigureMockMvc`, class-level `@Disabled`, and all four method names: shouldReturn403_whenUserMismatch, shouldFullReindex, shouldIndexOnlyPending, shouldReturnIndexedCount
    - movies-index.json is valid JSON (parseable) and contains the literal strings `custom_english_analyzer`, `asciifolding`, `kstem`, `stop_english`
    - movies-index.json contains a root field `"_skeleton": true`
    - `./gradlew test --tests "de.moviearchive.indexing.*" --tests "de.moviearchive.admin.*"` exits 0 with all new tests reported skipped (none failing)
  </acceptance_criteria>
  <done>Both test classes compile, run as skipped (@Disabled), expose the exact method names the validation map requires; movies-index.json skeleton is valid JSON with the analyzer block and a _skeleton marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runtime → Docker daemon | Testcontainers pulls and runs the OpenSearch image; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | OpenSearch test container image | accept | Image tag pinned to `opensearchproject/opensearch:2.19.0` matching docker-compose.yml; official OpenSearch image; test-scope only |
| T-04-01-SC | Tampering | Gradle dependency installs | accept | No new dependency added — GenericContainer is already on the classpath via spring-boot-testcontainers (RESEARCH.md zero-dependency path); no package-manager install task in this plan |
</threat_model>

<verification>
- `./gradlew compileTestJava` exits 0 (Task 1)
- `./gradlew test --tests "de.moviearchive.indexing.*" --tests "de.moviearchive.admin.*"` exits 0 with new tests skipped (Task 2)
- No new entry in `backend/build.gradle.kts` (zero-dependency Testcontainers path)
</verification>

<success_criteria>
- AbstractOpenSearchTest starts a singleton OpenSearch GenericContainer and binds opensearch.host/port
- IndexingIntegrationTest and ReindexControllerTest exist with the exact method names from 04-VALIDATION.md, all @Disabled
- movies-index.json skeleton is valid JSON on the classpath with the custom analyzer block and a _skeleton marker
- Full backend test suite still green (no regressions, new tests skipped)
</success_criteria>

<output>
Create `.planning/phases/04-opensearch-indexing/04-01-SUMMARY.md` when done
</output>
