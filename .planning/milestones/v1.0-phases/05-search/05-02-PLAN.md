---
phase: 05-search
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - backend/src/main/java/de/moviearchive/search/dto/SearchRequest.java
  - backend/src/main/java/de/moviearchive/search/dto/FilterCriteria.java
  - backend/src/main/java/de/moviearchive/search/dto/SearchResultItem.java
  - backend/src/main/java/de/moviearchive/search/dto/SearchResponse.java
  - backend/src/main/java/de/moviearchive/search/dto/AutocompleteResponse.java
  - backend/src/main/java/de/moviearchive/search/dto/DashboardMovieItem.java
  - backend/src/main/java/de/moviearchive/search/dto/HistogramBucket.java
  - backend/src/main/java/de/moviearchive/search/dto/DashboardResponse.java
  - backend/src/main/java/de/moviearchive/search/SearchService.java
  - backend/src/main/java/de/moviearchive/search/SearchController.java
  - backend/src/main/java/de/moviearchive/search/DashboardService.java
  - backend/src/main/java/de/moviearchive/search/DashboardController.java
  - backend/src/main/java/de/moviearchive/movie/MovieRepository.java
  - backend/src/test/java/de/moviearchive/search/SearchControllerTest.java
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04]

must_haves:
  truths:
    - "POST /api/search returns relevant films for a free-text query across all indexed text fields, scoped to the caller's movies-{userId} index"
    - "POST /api/search applies advanced filters with OR-within-group and AND-across-group logic"
    - "POST /api/search returns results sorted by title A-Z, year desc, personal_rating desc, or imdb_rating desc, with nulls last on numeric sorts"
    - "GET /api/dashboard returns archive stats, movie of the day, and recently-added films for the caller"
    - "GET /api/search/autocomplete returns name suggestions for director/actor prefixes"
    - "userId is always derived from the JWT (auth.getName() -> email -> userId), never from request body or path"
  artifacts:
    - path: "backend/src/main/java/de/moviearchive/search/SearchController.java"
      provides: "POST /search, GET /search/autocomplete endpoints"
      contains: "@RestController"
      exports: ["search", "autocomplete"]
    - path: "backend/src/main/java/de/moviearchive/search/SearchService.java"
      provides: "OpenSearch bool/should + bool.filter query construction and execution"
      contains: "@Service"
    - path: "backend/src/main/java/de/moviearchive/search/DashboardController.java"
      provides: "GET /dashboard endpoint"
      contains: "@RestController"
    - path: "backend/src/main/java/de/moviearchive/search/DashboardService.java"
      provides: "Aggregations + movie-of-the-day + recently-added"
      contains: "@Service"
    - path: "backend/src/main/java/de/moviearchive/movie/MovieRepository.java"
      provides: "findRecentlyIndexedByUserId query for recently-added"
      contains: "findRecentlyIndexedByUserId"
  key_links:
    - from: "backend/src/main/java/de/moviearchive/search/SearchController.java"
      to: "backend/src/main/java/de/moviearchive/user/UserRepository.java"
      via: "resolveUserId(auth) -> userRepository.findByEmail(auth.getName()).getId()"
      pattern: "findByEmail"
    - from: "backend/src/main/java/de/moviearchive/search/SearchService.java"
      to: "OpenSearch movies-{userId} index"
      via: "client.search(SearchRequest.of(...), Map.class)"
      pattern: "client\\.search"
    - from: "backend/src/main/java/de/moviearchive/search/DashboardService.java"
      to: "backend/src/main/java/de/moviearchive/movie/MovieRepository.java"
      via: "findRecentlyIndexedByUserId(userId, PageRequest.of(0, 10))"
      pattern: "findRecentlyIndexedByUserId"
---

<objective>
Implement the Phase 5 backend: search query API, dashboard data API, and autocomplete. All OpenSearch queries are scoped to the caller's `movies-{userId}` index resolved server-side from the JWT. Enables the 14 disabled SearchControllerTest stubs from plan 05-01.

Purpose: SRCH-01..04 backend behavior — free-text search, advanced filters, sort, plus dashboard aggregations.
Output: search + dashboard DTOs, SearchService, SearchController, DashboardService, DashboardController, one MovieRepository query, and 14 enabled passing integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search/05-RESEARCH.md
@.planning/phases/05-search/05-PATTERNS.md
@.planning/phases/05-search/05-CONTEXT.md
@.claude/data-model.md

<interfaces>
<!-- Field names below are VERIFIED against .claude/data-model.md. Use these exact OpenSearch field names. -->

OpenSearch text fields (analyzed, custom_english_analyzer):
  title (sort via title.raw keyword), original_title (sort via original_title.raw), tagline,
  overview, wikipedia_summary, wikipedia_plot, wikipedia_critics, personal_notes
OpenSearch keyword fields with .text analyzed sub-field:
  genre_list, director_list, full_cast_names, full_crew_names, keyword_list,
  country_list (ISO-3166-1), language_list (ISO-639-1)
OpenSearch scalar fields:
  tmdb_id (integer), year (integer), runtime (integer),
  imdb_rating (float, OMDB-nullable), content_rating (keyword, OMDB-nullable),
  watched (boolean, null until Phase 6), personal_rating (float, null until Phase 6),
  poster_path (keyword, not searchable)

Existing auth pattern (ReindexController.java lines 68-75):
  private UUID resolveUserId(Authentication auth) {
      return userRepository.findByEmail(auth.getName())
          .orElseThrow(() -> new UsernameNotFoundException("User not found: " + auth.getName()))
          .getId();
  }

Existing OpenSearch client call (IndexingService.java):
  SearchResponse<Map> response = client.search(searchReq, Map.class);
  response.hits().total().value(); response.hits().hits() -> Hit::source

Existing repository pattern (MovieRepository.java):
  @Query("SELECT m FROM Movie m WHERE m.user.id = :userId AND m.indexedAt IS NULL")
  List<Movie> findByUserIdAndIndexedAtIsNull(@Param("userId") UUID userId);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search + dashboard DTOs and MovieRepository recently-added query</name>
  <files>backend/src/main/java/de/moviearchive/search/dto/SearchRequest.java, backend/src/main/java/de/moviearchive/search/dto/FilterCriteria.java, backend/src/main/java/de/moviearchive/search/dto/SearchResultItem.java, backend/src/main/java/de/moviearchive/search/dto/SearchResponse.java, backend/src/main/java/de/moviearchive/search/dto/AutocompleteResponse.java, backend/src/main/java/de/moviearchive/search/dto/DashboardMovieItem.java, backend/src/main/java/de/moviearchive/search/dto/HistogramBucket.java, backend/src/main/java/de/moviearchive/search/dto/DashboardResponse.java, backend/src/main/java/de/moviearchive/movie/MovieRepository.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/movie/dto/SaveMovieRequest.java (DTO + validation analog)
    - backend/src/main/java/de/moviearchive/movie/MovieRepository.java (existing @Query pattern, Pageable import)
    - .planning/phases/05-search/05-PATTERNS.md lines 225-292 (DTO + repository patterns, exact field lists)
    - .planning/phases/05-search/05-CONTEXT.md (D-10 filter set, D-12 sort options, D-02 dashboard sections)
  </read_first>
  <action>
    Create package `de.moviearchive.search.dto` with these types:

    `SearchRequest` — `@Data @Builder` class (not record, allows nulls): `String query` (null/blank = match_all), `@Valid FilterCriteria filters`, `String sort` (one of `title_asc`,`year_desc`,`rating_desc`,`imdb_desc`; default `title_asc`), `@Min(0) int page`.

    `FilterCriteria` — `@Data @Builder` class with all D-10 fields: `List<String> genres`, `String director`, `String actors`, `Integer yearFrom`, `Integer yearTo`, `Double imdbRatingFrom`, `Double imdbRatingTo`, `List<String> contentRatings`, `Integer runtimeMax`, `Boolean notYetWatched`, `List<String> languages`, `List<String> countries`.

    `SearchResultItem` — record `(String id, Integer tmdbId, String title, Integer year, String posterPath, List<String> directorList, List<String> genreList, Double imdbRating, Integer runtime)`.

    `SearchResponse` — record `(List<SearchResultItem> results, long total, int page, int totalPages, boolean hasMore)`.

    `AutocompleteResponse` — record `(List<String> suggestions)`.

    `DashboardMovieItem` — record `(String id, String title, Integer year, String posterPath)`.

    `HistogramBucket` — record `(String label, long count)`.

    `DashboardResponse` — record `(long totalFilms, List<Map<String,Object>> topGenres, List<Map<String,Object>> languageBreakdown, List<HistogramBucket> imdbHistogram, DashboardMovieItem movieOfTheDay, List<DashboardMovieItem> recentlyAdded)`. Use `record GenreCount(String name, long count)` and `record LanguageCount(String code, long count)` instead of raw `Map` for topGenres/languageBreakdown so the JSON keys match the MSW contract (`name`/`count`, `code`/`count`); reference these nested records in DashboardResponse.

    Modify `backend/src/main/java/de/moviearchive/movie/MovieRepository.java`: add method `@Query("SELECT m FROM Movie m WHERE m.user.id = :userId AND m.indexedAt IS NOT NULL ORDER BY m.indexedAt DESC") List<Movie> findRecentlyIndexedByUserId(@Param("userId") UUID userId, Pageable pageable);` — add the `org.springframework.data.domain.Pageable` import if absent. Follow the exact existing `@Query` style at MovieRepository.java lines 36-43.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava 2>&1 | grep -E "BUILD SUCCESSFUL|error:"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 DTO files exist under `backend/src/main/java/de/moviearchive/search/dto/`
    - `MovieRepository.java` contains `findRecentlyIndexedByUserId` with `Pageable` parameter and `ORDER BY m.indexedAt DESC`
    - `SearchRequest` is annotated `@Data` and `@Builder` and contains field `page` with `@Min(0)`
    - `FilterCriteria` contains all 12 fields named exactly: genres, director, actors, yearFrom, yearTo, imdbRatingFrom, imdbRatingTo, contentRatings, runtimeMax, notYetWatched, languages, countries
    - `cd backend && ./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>All search/dashboard DTOs compile; MovieRepository exposes findRecentlyIndexedByUserId.</done>
</task>

<task type="auto">
  <name>Task 2: SearchService + SearchController (free-text, filters, sort, autocomplete, pagination)</name>
  <files>backend/src/main/java/de/moviearchive/search/SearchService.java, backend/src/main/java/de/moviearchive/search/SearchController.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/indexing/IndexingService.java (OpenSearchClient injection, client.search, generic client patterns)
    - backend/src/main/java/de/moviearchive/admin/ReindexController.java (resolveUserId, index name derivation, error handler)
    - backend/src/main/java/de/moviearchive/user/UserRepository.java (findByEmail signature)
    - .planning/phases/05-search/05-RESEARCH.md lines 218-497 (Patterns 1-4, 8, 9 — query construction, sort, pagination, autocomplete)
    - .planning/phases/05-search/05-PATTERNS.md lines 51-194, 620-658 (controller/service analog patterns)
    - .claude/data-model.md (field names + .raw / .text sub-fields)
  </read_first>
  <action>
    Create `SearchService` (`@Service @Slf4j`, constructor-inject `OpenSearchClient client`). Implement `public SearchResponse search(String indexName, SearchRequest request) throws IOException`:

    - Base query: if `request.getQuery()` is null/blank use `Query.of(q -> q.matchAll(m -> m))`; else a `bool.should` over fields with boosts per 05-RESEARCH.md Pattern 1: title 4.0, original_title 3.0, director_list.text 3.0, full_cast_names.text 2.5, genre_list.text 2.0, tagline 2.0, overview 1.5, keyword_list.text 1.5, full_crew_names.text 1.0, wikipedia_summary 1.0, wikipedia_plot 0.8, wikipedia_critics 0.6, personal_notes 1.0; `minimumShouldMatch("1")`.
    - Build a `BoolQuery.Builder`: `.must(baseQuery)`, then add `.filter(...)` clauses ONLY for non-null/non-empty FilterCriteria fields, exactly per 05-RESEARCH.md Pattern 2: genres -> `terms` on `genre_list`; director -> `match` on `director_list.text`; actors -> `match` on `full_cast_names.text`; year range -> `range` on `year`; imdb range -> `range` on `imdb_rating`; contentRatings -> `terms` on `content_rating`; runtimeMax -> `range.lte` on `runtime`; notYetWatched==TRUE -> `term` `watched=false`; languages -> `terms` on `language_list`; countries -> `terms` on `country_list`. Filters go in `bool.filter` (filter context — no score, cached). Add a code comment that null imdb_rating docs are intentionally excluded by the range filter (05-RESEARCH.md Pitfall 2).
    - Sort: switch on `request.getSort()` per 05-RESEARCH.md Pattern 3 — `title_asc` -> `title.raw` Asc; `year_desc` -> `year` Desc `missing _last`; `rating_desc` -> `personal_rating` Desc `missing _last`; `imdb_desc` -> `imdb_rating` Desc `missing _last`; default -> `title.raw` Asc.
    - Pagination: constant `PAGE_SIZE = 20`; `from = page * PAGE_SIZE`; set `.from(from).size(PAGE_SIZE)`; source-filter to includes: `tmdb_id, title, year, poster_path, director_list, genre_list, imdb_rating, runtime`.
    - Execute `client.search(searchReq, Map.class)`. Map each hit `source` Map to `SearchResultItem` (id = hit.id(); read fields from the source Map with null-safe casts). Compute `total = response.hits().total().value()`, `totalPages = (int) Math.ceil(total / (double) PAGE_SIZE)`, `hasMore = (from + PAGE_SIZE) < total`.

    Implement `public AutocompleteResponse autocomplete(String indexName, String field, String prefix) throws IOException` per 05-RESEARCH.md Pattern 8: validate `field` is one of `director` or `actors` (reject others with `IllegalArgumentException`); map `director` -> `director_list` / `director_list.text`, `actors` -> `full_cast_names` / `full_cast_names.text`. Use `match_phrase_prefix` on the `.text` sub-field, `size 20`, source-filter to the keyword field; flatten + dedupe + cap at 10 distinct suggestions.

    Create `SearchController` (`@RestController @RequestMapping("/search") @Slf4j`, constructor-inject `SearchService`, `UserRepository`). Endpoints: `@PostMapping public ResponseEntity<SearchResponse> search(@RequestBody @Valid SearchRequest request, Authentication auth) throws IOException` and `@GetMapping("/autocomplete") public ResponseEntity<AutocompleteResponse> autocomplete(@RequestParam String field, @RequestParam String prefix, Authentication auth) throws IOException`. Both derive `indexName = "movies-" + resolveUserId(auth)` using the EXACT `resolveUserId` pattern from ReindexController.java lines 68-75 (`auth.getName()` is the email — NEVER `UUID.fromString(auth.getName())`). The userId is never read from the request. Add the same `@ExceptionHandler(MethodArgumentNotValidException.class)` 400 handler used by MovieController (returns `{"message": "..."}`), plus a handler for `IllegalArgumentException` returning 400 `{"message": ...}`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest" 2>&1 | grep -E "BUILD SUCCESSFUL|tests completed|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `SearchController.java` contains `@RestController` and `@RequestMapping("/search")`
    - `SearchController` contains a `resolveUserId` method calling `userRepository.findByEmail` (no `UUID.fromString(auth.getName())` anywhere)
    - `SearchService.java` contains `@Service`, `client.search(`, `bool.filter` (filter context), and `PAGE_SIZE = 20`
    - `grep -n "UUID.fromString" backend/src/main/java/de/moviearchive/search/SearchController.java` returns no match
    - The SRCH-01/02/03 SearchControllerTest methods that exercise search (`shouldReturnAllFilms_whenQueryIsEmpty`, `shouldFindFilmByTitle`, `shouldFindFilmByOverview`, `shouldNormalizeAccentsInSearch`, `shouldFilterBySingleGenre`, `shouldFilterByMultipleGenresOR`, `shouldFilterByDirector`, `shouldFilterByYearRange`, `shouldFilterByImdbRating`, `shouldCombineGenreAndDirectorFilters`, `shouldReturnEmpty_whenWatchedFilterApplied`, `shouldSortByTitleAscending`, `shouldSortByImdbRatingDescending`, `shouldSortByPersonalRating_nullsLast`) pass (see Task 3 which enables them)
  </acceptance_criteria>
  <done>SearchService + SearchController implemented; all queries scoped to movies-{userId} from JWT; compiles and the search/filter/sort tests pass once enabled in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: DashboardService + DashboardController, enable + implement SearchControllerTest stubs</name>
  <files>backend/src/main/java/de/moviearchive/search/DashboardService.java, backend/src/main/java/de/moviearchive/search/DashboardController.java, backend/src/test/java/de/moviearchive/search/SearchControllerTest.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/movie/MovieService.java (service + @Transactional + repository injection analog)
    - backend/src/main/java/de/moviearchive/movie/MovieController.java (GET endpoint + validation handler analog)
    - backend/src/main/java/de/moviearchive/indexing/DocumentBuilder.java (poster_path extraction from rawTmdbJson — reuse the same logic for recently-added)
    - backend/src/test/java/de/moviearchive/search/SearchControllerTest.java (the 14 disabled stubs from plan 05-01 — enable + implement)
    - backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java (persistMovie, indexTestMovie, refreshIndex helper bodies)
    - .planning/phases/05-search/05-RESEARCH.md lines 403-471, 660-721 (Patterns 5-7, integration test skeleton)
  </read_first>
  <action>
    Create `DashboardService` (`@Service @Transactional @Slf4j`, constructor-inject `OpenSearchClient client`, `MovieRepository movieRepository`). Implement `public DashboardResponse getDashboard(String indexName, UUID userId) throws IOException`:

    - Single OS request with `size(0)` and aggregations per 05-RESEARCH.md Pattern 5: `total_count` valueCount on `tmdb_id`; `genres` terms on `genre_list` size 10; `languages` terms on `language_list` size 20; `imdb_histogram` range agg on `imdb_rating` with buckets `1-2`(1.0–3.0), `3-4`(3.0–5.0), `5-6`(5.0–7.0), `7-8`(7.0–9.0), `9-10`(9.0–10.1). Null-check every bucket list before iterating (05-RESEARCH.md Pitfall 7 — empty archive must not NPE; return totalFilms=0 with empty lists and movieOfTheDay=null).
    - Movie of the day per Pattern 6: separate OS request, `function_score` wrapping `matchAll`, `random_score` with `seed = String.valueOf(LocalDate.now().toString().hashCode())` and field `_seq_no`, `boostMode Replace`, `size 1`, source-filter `title, year, poster_path, tmdb_id`. If zero hits, movieOfTheDay = null.
    - Recently added per Pattern 7: `movieRepository.findRecentlyIndexedByUserId(userId, PageRequest.of(0, 10))`. For each Movie, extract `poster_path` from `movie.getRawTmdbJson()` using the SAME extraction logic as DocumentBuilder, derive `year` from release_date, map to `DashboardMovieItem(movie.getId().toString(), title, year, posterPath)`.
    - Assemble and return `DashboardResponse`.

    Create `DashboardController` (`@RestController @RequestMapping("/dashboard") @Slf4j`, constructor-inject `DashboardService`, `UserRepository`). `@GetMapping public ResponseEntity<DashboardResponse> getDashboard(Authentication auth) throws IOException` -> `UUID userId = resolveUserId(auth); return ResponseEntity.ok(dashboardService.getDashboard("movies-" + userId, userId));`. Reuse the EXACT `resolveUserId` pattern from ReindexController.

    Modify `SearchControllerTest.java`: remove the `@Disabled` annotation from all 14 stub methods and replace each `fail(...)` body with a real integration test per 05-RESEARCH.md lines 660-721 and 05-VALIDATION.md behavior column. Add `indexTestMovie(...)` helper(s) that persist a Movie then call `indexingService.index(...)` and then `refreshIndex("movies-" + testUser.getId())` BEFORE asserting (mandatory — OpenSearch is near-real-time, 05-RESEARCH.md Pitfall 1). Each test logs in via `loginAndGetToken`, performs `mockMvc.perform(post("/search")...)` or asserts the documented behavior (e.g., `shouldReturnAllFilms_whenQueryIsEmpty` indexes "Zorro" + "Amelie", asserts results[0].title=Amelie, results[1].title=Zorro, total=2; `shouldReturnEmpty_whenWatchedFilterApplied` asserts total=0 because watched is null until Phase 6). Keep all 14 names unchanged.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest" 2>&1 | grep -E "BUILD SUCCESSFUL|tests completed|FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardController.java` contains `@RestController` and `@RequestMapping("/dashboard")`
    - `DashboardService.java` contains `@Service`, the `imdb_histogram` range aggregation, and a null-check guard so an empty index returns totalFilms=0 (no NPE)
    - `SearchControllerTest.java` contains zero `@Disabled` annotations and zero `Assertions.fail(` calls
    - `cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest"` exits 0 with all 14 tests passing
    - `cd backend && ./gradlew test` exits 0 (full backend suite green)
  </acceptance_criteria>
  <done>Dashboard API implemented; all 14 SearchControllerTest stubs enabled and passing; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → SearchController/DashboardController | Untrusted JSON request body + query params cross here; authenticated via JwtAuthFilter |
| backend → OpenSearch movies-{userId} | Index name derived server-side from JWT; filter values passed as parameterized FieldValue |
| backend → PostgreSQL (recently-added) | Parameterized JPQL via @Query with @Param binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege (IDOR) | SearchController / DashboardController userId resolution | mitigate | userId derived ONLY via `resolveUserId(auth)` = `userRepository.findByEmail(auth.getName()).getId()`. Index name is `"movies-" + userId`. No userId in request body or path. Acceptance criterion greps for absence of `UUID.fromString(auth.getName())`. |
| T-05-02-02 | Tampering (query injection) | SearchService filter/query construction | mitigate | All filter values passed through opensearch-java typed builders (`FieldValue.of`, `match.query(fv -> fv.stringValue(...))`, `terms`). No string interpolation into raw query JSON. (05-RESEARCH.md Security Domain V5.) |
| T-05-02-03 | Denial of Service (resource exhaustion) | SearchService pagination + DashboardService aggregations | mitigate | `PAGE_SIZE = 20` hard cap on from/size; aggregation `size` capped at 20 (languages) / 10 (genres); autocomplete capped at 10 results; movie-of-day `size 1`. No unbounded query. |
| T-05-02-04 | Information Disclosure | SearchController autocomplete `field` param | mitigate | `field` whitelisted to `director`/`actors` only; any other value throws IllegalArgumentException -> 400, preventing arbitrary-field enumeration of another mapping. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages (05-RESEARCH.md Package Legitimacy Audit: zero new dependencies). No install task. |
</threat_model>

<verification>
- `cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest"` → all 14 pass
- `cd backend && ./gradlew test` → full backend suite green
- `grep -n "UUID.fromString" backend/src/main/java/de/moviearchive/search/*.java` → no matches (IDOR mitigation proof)
- Empty-archive dashboard request → 200 with totalFilms=0, no 500/NPE
</verification>

<success_criteria>
- POST /api/search returns correctly scored, filtered, sorted results scoped to movies-{userId}
- GET /api/dashboard returns stats + movie of the day + recently-added; empty archive returns a valid empty-state body
- GET /api/search/autocomplete returns deduped name suggestions for director/actors only
- All 14 SearchControllerTest integration tests pass; full backend suite green
- No IDOR: userId never sourced from request; query values never string-interpolated
</success_criteria>

<output>
Create `.planning/phases/05-search/05-02-SUMMARY.md` when done
</output>
