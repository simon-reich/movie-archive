---
phase: 10-bulk-import-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/composables/useMovies.ts
  - frontend/pages/add.vue
  - frontend/test/unit/composables/useMovies.spec.ts
  - frontend/test/unit/pages/add.spec.ts
autonomous: true
requirements: [IMPORT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A file input and Import button render in the Add Film page's new 'Bulk Import' section, calling uploadBulkImport(file) via useMovies() on submit (IMPORT-01)."
    - "On a successful 202 response, an inline acknowledgement message is shown; no progress bar or per-line results list is rendered here — those are explicitly Phase 11 scope, this phase only builds the trigger (D-13/deferred)."
    - "On a 422 response, the same 'No TMDB key configured. Add your key in Settings.' message already used by the existing search flow is shown, reusing the established error-message convention rather than inventing a new one."
    - "On any other error response, a generic 'Import failed. Please try again.' message is shown — the upload never fails silently."
  artifacts:
    - frontend/composables/useMovies.ts
    - frontend/pages/add.vue
    - frontend/test/unit/composables/useMovies.spec.ts
    - frontend/test/unit/pages/add.spec.ts
  key_links:
    - "add.vue handleBulkImport() -> useMovies().uploadBulkImport(file) -> $fetch POST /api/movies/bulk-import (multipart FormData) -> backend BulkImportController.uploadBulkImport (Plan 10-01, same-phase contract)"
  prohibitions:
    - "Must not manually set a Content-Type header on the uploadBulkImport FormData request — ofetch auto-generates the multipart boundary; an explicit override breaks the multipart encoding."
    - "Must not omit the Authorization header on the upload request — authHeaders() must be included exactly as every other useMovies composable function does, since /movies/bulk-import sits behind the same JWT-authenticated boundary as /movies/save."
---

<objective>
Add the upload trigger for bulk import to the existing Add Film page: a file input and Import
button that call the new POST /movies/bulk-import endpoint (Plan 10-01) and show a simple
inline acknowledgement or error message.

Purpose: Completes IMPORT-01's frontend half — the user-facing entry point for bulk import.
Live progress and the per-line results overview are explicitly Phase 11 scope; this phase's
UI only needs to trigger the upload and confirm it started.
Output: `uploadBulkImport` composable function and a new "Bulk Import" section on `add.vue`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/10-bulk-import-engine/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add uploadBulkImport to useMovies composable</name>
  <read_first>
    frontend/composables/useMovies.ts (saveMovie/searchTmdb pattern to extend — authHeaders(), credentials:'include', return object shape)
    frontend/test/unit/composables/useMovies.spec.ts (vi.stubGlobal('$fetch', mockFetch) test convention — no MSW, direct $fetch mock)
  </read_first>
  <files>
    frontend/composables/useMovies.ts,
    frontend/test/unit/composables/useMovies.spec.ts
  </files>
  <action>
    In `useMovies.ts`, add `async function uploadBulkImport(file: File): Promise&lt;{ status: string }&gt;`:
    build `const formData = new FormData()`, `formData.append('file', file)`, then `return await
    $fetch&lt;{ status: string }&gt;('/api/movies/bulk-import', { method: 'POST', body: formData,
    credentials: 'include', headers: authHeaders() })` — do NOT set a `Content-Type` header (ofetch
    auto-generates the multipart boundary from the `FormData` body; an explicit override breaks it). Add
    `uploadBulkImport` to the composable's `return { searchTmdb, saveMovie, getStatus, getSavedTmdbIds,
    uploadBulkImport }` object.

    In `useMovies.spec.ts`, add two tests following the file's existing `mockFetch`/`vi.stubGlobal` pattern:
    `uploadBulkImport sends POST multipart to /api/movies/bulk-import and returns status` — mock
    `mockFetch.mockResolvedValueOnce({ status: 'started' })`, call `uploadBulkImport(new File(['Inception;;2010'],
    'films.txt', { type: 'text/plain' }))`, assert `mockFetch` was called with
    `'/api/movies/bulk-import'` and `expect.objectContaining({ method: 'POST', credentials: 'include' })`,
    assert the result's `status` is `'started'`. `uploadBulkImport throws on 422 when no TMDB key configured`
    — mock `mockFetch.mockRejectedValueOnce({ status: 422, data: { message: 'No TMDB key configured. Add
    your key in Settings.' } })`, assert the call rejects with `expect.objectContaining({ status: 422 })`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run test/unit/composables/useMovies.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    Source: `useMovies.ts` exports `uploadBulkImport` from its return object; the function's `$fetch`
    call omits any `Content-Type` header.
    Behavior: `uploadBulkImport` posts a `FormData` body to `/api/movies/bulk-import` with the
    `Authorization` header from `authHeaders()`.
    Test command passes.
  </acceptance_criteria>
  <done>
    useMovies.spec.ts's two new tests pass; uploadBulkImport is available from useMovies() and
    matches the backend contract defined in Plan 10-01 (multipart POST /movies/bulk-import,
    202 { status: "started" } on success, 422 on missing TMDB key).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Bulk Import section to the Add Film page</name>
  <read_first>
    frontend/pages/add.vue (page structure, handleSearch()/handlePosterClick() error-handling pattern, existing Search button classes to mirror)
    frontend/components/FormErrorBanner.vue (reused error component — role="alert", border-l-2 border-[#7A3520] styling)
    frontend/pages/settings.vue (new-section-on-existing-page convention: text-xl font-semibold tracking-wide mb-6 heading, hr separator before the section — see line ~204/297/372 for the pattern this mirrors)
    frontend/test/unit/pages/add.spec.ts (existing test convention: composable-level assertions via vi.stubGlobal, not full DOM mount — established precedent for this file, not a new deviation)
  </read_first>
  <files>
    frontend/pages/add.vue,
    frontend/test/unit/pages/add.spec.ts
  </files>
  <action>
    In `add.vue`'s `&lt;script setup&gt;`, destructure `uploadBulkImport` from `useMovies()` alongside the
    existing functions. Add reactive state: `const selectedFile = ref&lt;File | null&gt;(null)`, `const
    bulkImporting = ref(false)`, `const bulkImportError = ref&lt;string | null&gt;(null)`, `const
    bulkImportMessage = ref&lt;string | null&gt;(null)`. Add `function handleFileSelect(event: Event)`:
    cast `event.target as HTMLInputElement`, set `selectedFile.value = input.files?.[0] ?? null`, clear
    `bulkImportError.value` and `bulkImportMessage.value`. Add `async function handleBulkImport()`: return
    early if `!selectedFile.value`; set `bulkImporting.value = true`, clear both message refs; in a
    try/catch, call `await uploadBulkImport(selectedFile.value)`, on success set `bulkImportMessage.value =
    'Import started — this runs in the background.'` and `selectedFile.value = null`; in the catch block,
    cast the error to `{ status?: number }` and set `bulkImportError.value` to `'No TMDB key configured. Add
    your key in Settings.'` when `status === 422`, else `'Import failed. Please try again.'`; in `finally`,
    set `bulkImporting.value = false`.

    In the template, after the closing `&lt;/div&gt;` of the existing poster grid (end of the `&lt;main&gt;`
    content, before `&lt;/main&gt;`), add: an `&lt;hr class="border-border my-8"&gt;` separator, then a
    `&lt;section id="bulk-import"&gt;` containing an `&lt;h1 class="text-xl font-semibold tracking-wide
    mb-6"&gt;Bulk Import&lt;/h1&gt;`, a `&lt;form class="flex items-center gap-3"
    @submit.prevent="handleBulkImport"&gt;` with a native `&lt;input id="bulk-import-file" type="file"
    accept=".txt,.csv" class="text-sm text-foreground file:mr-3 file:h-10 file:px-4 file:border-0
    file:bg-primary file:text-primary-foreground file:text-sm file:font-semibold file:rounded-none
    file:cursor-pointer file:hover:opacity-90" @change="handleFileSelect"&gt;` and a submit
    `&lt;button&gt;` reusing the exact class string of the existing Search button (`shrink-0 px-5 h-10
    bg-primary text-primary-foreground text-sm font-semibold rounded-none hover:opacity-90 disabled:opacity-50
    disabled:cursor-not-allowed`) with `:disabled="!selectedFile || bulkImporting"`, text `{{ bulkImporting ?
    'Uploading...' : 'Import' }}`. After the `&lt;/form&gt;`, add `&lt;FormErrorBanner v-if="bulkImportError"
    :message="bulkImportError" /&gt;` and `&lt;p v-if="bulkImportMessage" class="text-sm text-foreground
    mt-2"&gt;{{ bulkImportMessage }}&lt;/p&gt;`, closing `&lt;/section&gt;`. No `rounded-*` classes other than
    `rounded-none` anywhere (UI-SPEC.md design system: no rounded corners).

    In `add.spec.ts`, following the file's existing composable-level assertion convention (not full DOM
    mount — matches the file's own documented precedent), add two tests: one mocking `mockFetch` to resolve
    `{ status: 'started' }` for a call to `uploadBulkImport` (imported from `useMovies`) and asserting the
    resolved `status` is `'started'`; one mocking a `422` rejection and asserting the call rejects with
    `expect.objectContaining({ status: 422 })` — mirrors the file's existing `shows 422 error when no TMDB
    key configured` test for `searchTmdb`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run test/unit/pages/add.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    Source: `add.vue` contains a `&lt;section id="bulk-import"&gt;` with a file input and submit
    button; the button's disabled binding is `!selectedFile || bulkImporting`.
    Behavior: submitting with a selected file calls `uploadBulkImport` and sets
    `bulkImportMessage` on success, `bulkImportError` on 422/generic failure.
    Test command passes.
  </acceptance_criteria>
  <done>
    add.spec.ts's two new tests pass; the Add Film page has a working Bulk Import upload
    trigger following the established design system (no rounded corners, reused
    FormErrorBanner, reused button/heading conventions from settings.vue).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser file picker -> uploadBulkImport | The selected File object's content is sent as-is to the backend; the frontend performs no content validation of its own |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Spoofing / Tampering | `accept=".txt,.csv"` file input attribute | low | accept | Client-side file-type hinting is UX-only, not a security boundary — a malicious user can trivially bypass it; actual validation (size limits, content handling) happens server-side in Plan 10-01 (T-10-01/T-10-04). No sensitive operation is authorized client-side. |
| T-10-07 | Information Disclosure | `bulkImportError` message display | low | mitigate | Only static, pre-defined strings are ever assigned to `bulkImportError` (the 422 message and a generic failure message) — no raw server error body, stack trace, or unfiltered `e.message` is ever rendered, consistent with `add.vue`'s existing `searchError` handling. |
</threat_model>

<verification>
Run `cd frontend && npx vitest run test/unit/composables/useMovies.spec.ts test/unit/pages/add.spec.ts`
after both tasks — all tests pass. Run `cd frontend && npm run test` (full suite) to confirm no
regression in existing `useMovies`/`add.vue` tests.
</verification>

<success_criteria>
The Add Film page has a visible file input and Import button. Selecting a file and clicking Import
calls POST /movies/bulk-import via uploadBulkImport and shows either a success acknowledgement or an
error message — never fails silently.
</success_criteria>

<output>
Create `.planning/phases/10-bulk-import-engine/10-02-SUMMARY.md` when done
</output>
