---
phase: 01-authentication
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/test/resources/application-test.properties
  - backend/src/test/java/de/moviearchive/auth/AuthControllerTest.java
  - backend/src/test/java/de/moviearchive/auth/AuthIntegrationTest.java
  - backend/src/test/java/de/moviearchive/auth/AuthServiceTest.java
  - backend/src/test/java/de/moviearchive/security/JwtServiceTest.java
  - backend/src/test/java/de/moviearchive/mail/MailServiceTest.java
  - backend/src/test/java/de/moviearchive/token/RefreshTokenServiceTest.java
  - frontend/test/unit/stores/auth.spec.ts
  - frontend/test/unit/composables/useAuth.spec.ts
  - frontend/test/unit/middleware/auth.spec.ts
  - frontend/test/unit/plugins/auth.spec.ts
  - frontend/test/unit/pages/login.spec.ts
  - frontend/test/unit/pages/signup.spec.ts
  - frontend/test/unit/pages/forgot-password.spec.ts
  - frontend/test/unit/pages/reset-password.spec.ts
  - frontend/test/unit/pages/verify-email.spec.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08

must_haves:
  truths:
    - "All backend test stub files exist and compile (no production code written yet — stubs use @Disabled)"
    - "All frontend test stub files exist and run without errors (stubs use test.skip)"
    - "application-test.properties has GreenMail SMTP on port 3025 and a JWT secret >= 32 chars"
    - "Running ./gradlew test produces a build that compiles with zero errors"
    - "Running pnpm test --run exits 0 (skipped stubs count as passing)"
  artifacts:
    - path: "backend/src/test/resources/application-test.properties"
      provides: "Test configuration — GreenMail SMTP, test JWT secret"
      contains: "spring.mail.port=3025"
    - path: "backend/src/test/java/de/moviearchive/auth/AuthControllerTest.java"
      provides: "Controller test stub for AUTH-01/03/06/07, D-06"
    - path: "backend/src/test/java/de/moviearchive/auth/AuthIntegrationTest.java"
      provides: "Integration test stub for AUTH-01/02/04/05/06/08"
    - path: "backend/src/test/java/de/moviearchive/auth/AuthServiceTest.java"
      provides: "Unit test stub for AUTH-02/08 (Mockito)"
    - path: "backend/src/test/java/de/moviearchive/security/JwtServiceTest.java"
      provides: "JWT creation + validation unit stubs"
    - path: "backend/src/test/java/de/moviearchive/mail/MailServiceTest.java"
      provides: "Mail test stub (GreenMail)"
    - path: "backend/src/test/java/de/moviearchive/token/RefreshTokenServiceTest.java"
      provides: "grace_until race condition unit stubs"
    - path: "frontend/test/unit/stores/auth.spec.ts"
      provides: "Pinia auth store unit stubs"
    - path: "frontend/test/unit/middleware/auth.spec.ts"
      provides: "Middleware redirect logic stubs"
    - path: "frontend/test/unit/plugins/auth.spec.ts"
      provides: "Plugin refresh-on-init stubs"
    - path: "frontend/test/unit/pages/login.spec.ts"
      provides: "Login page integration stubs (MSW)"
    - path: "frontend/test/unit/pages/signup.spec.ts"
      provides: "Signup page integration stubs"
    - path: "frontend/test/unit/pages/forgot-password.spec.ts"
      provides: "Forgot-password page stubs"
    - path: "frontend/test/unit/pages/reset-password.spec.ts"
      provides: "Reset-password page stubs"
    - path: "frontend/test/unit/pages/verify-email.spec.ts"
      provides: "Verify-email page stubs"
    - path: "frontend/test/unit/composables/useAuth.spec.ts"
      provides: "useAuth composable stubs"
  key_links:
    - from: "AbstractIntegrationTest.java"
      to: "AuthIntegrationTest.java"
      via: "extends AbstractIntegrationTest"
      pattern: "extends AbstractIntegrationTest"
    - from: "application-test.properties"
      to: "GreenMail SMTP port"
      via: "spring.mail.port=3025"
      pattern: "spring\\.mail\\.port=3025"
---

<objective>
Create all test stub files required before any production code is written. Stubs compile but contain @Disabled (BE) or test.skip (FE) placeholder test methods. This is the Wave 0 gate — nothing in Plan 02 or Plan 03 executes until this plan is complete.

Purpose: The Nyquist sampling rule requires every automated verify command to reference a real file. Without stubs, plans 02 and 03 cannot be verified during execution.
Output: 16 test files (6 BE + 1 config + 9 FE) that compile and execute cleanly with zero errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-authentication/01-CONTEXT.md
@.planning/phases/01-authentication/01-RESEARCH.md
@.planning/phases/01-authentication/01-VALIDATION.md

<interfaces>
<!-- Existing test base classes the stubs must extend -->

From backend/src/test/java/de/moviearchive/AbstractIntegrationTest.java:
```java
// @SpringBootTest + @ActiveProfiles("test") + @Testcontainers
// Contains: static PostgreSQLContainer<?> postgres with @ServiceConnection
public abstract class AbstractIntegrationTest { ... }
```

From backend/src/test/resources/application-test.properties (already exists — verify then update if needed):
```
spring.flyway.enabled=true
spring.mail.host=localhost
spring.mail.port=3025
jwt.secret=test-secret-key-that-is-long-enough-32c
jwt.access-token-expiration-ms=900000
jwt.refresh-token-expiration-ms=604800000
encryption.master-key=test-encryption-key-32-characters!
app.base-url=http://localhost
```

From frontend/test/mocks/handlers.ts:
```typescript
import { http, HttpResponse } from 'msw'
export const handlers = [
  http.get('/api/actuator/health', () => HttpResponse.json({ status: 'UP' })),
]
```

From frontend/test/mocks/server.ts:
```typescript
import { setupServer } from 'msw/node'
import { handlers } from './handlers'
export const server = setupServer(...handlers)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify application-test.properties and create backend test stubs</name>
  <files>
    backend/src/test/resources/application-test.properties
    backend/src/test/java/de/moviearchive/auth/AuthControllerTest.java
    backend/src/test/java/de/moviearchive/auth/AuthIntegrationTest.java
    backend/src/test/java/de/moviearchive/auth/AuthServiceTest.java
    backend/src/test/java/de/moviearchive/security/JwtServiceTest.java
    backend/src/test/java/de/moviearchive/mail/MailServiceTest.java
    backend/src/test/java/de/moviearchive/token/RefreshTokenServiceTest.java
  </files>
  <read_first>
    - backend/src/test/resources/application-test.properties (verify spring.mail.port=3025 and jwt.secret length >= 32 chars — already exists, confirm then leave unchanged)
    - backend/src/test/java/de/moviearchive/AbstractIntegrationTest.java (base class to extend)
    - backend/src/main/java/de/moviearchive/token/RefreshToken.java (entity structure for stub method names)
    - backend/src/main/java/de/moviearchive/user/User.java (entity structure)
    - .planning/phases/01-authentication/01-VALIDATION.md (exact test method names required)
  </read_first>
  <action>
Verify application-test.properties already contains the required values (it does — confirmed). No changes needed to that file.

Create the following 6 backend test stub files. Each stub compiles but all test methods are annotated @Disabled("Wave 0 stub — implement in Plan 02"). Use @Disabled from org.junit.jupiter.api.Disabled.

**File 1: backend/src/test/java/de/moviearchive/auth/AuthControllerTest.java**
```java
package de.moviearchive.auth;

import de.moviearchive.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.test.web.servlet.MockMvc;

@AutoConfigureMockMvc
class AuthControllerTest extends AbstractIntegrationTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldReturn409_whenEmailAlreadyRegistered() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectUnverified_whenAccountPendingVerification() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectBadPassword_whenPasswordIncorrect() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldReturn200ForUnknownEmail_forgotPassword() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRateLimitLogin_afterTenRequests() {}
}
```

**File 2: backend/src/test/java/de/moviearchive/auth/AuthIntegrationTest.java**
```java
package de.moviearchive.auth;

import de.moviearchive.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.test.web.servlet.MockMvc;

@AutoConfigureMockMvc
class AuthIntegrationTest extends AbstractIntegrationTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldCreateUser_withPendingVerificationStatus() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldVerifyEmail_setsStatusActiveAndConsumesToken() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldLogin_returnsAccessTokenAndHttpOnlyCookie() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRotateRefreshToken_revokesOldAndIssuesNew() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldLogout_setsTokenRevoked() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldResetPassword_revokesAllRefreshTokens() {}
}
```

**File 3: backend/src/test/java/de/moviearchive/auth/AuthServiceTest.java**
```java
package de.moviearchive.auth;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class AuthServiceTest {

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectExpiredToken_whenVerificationTokenExpired() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectConsumedToken_whenVerificationTokenAlreadyUsed() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectExpiredResetToken_whenPasswordResetTokenExpired() {}
}
```

**File 4: backend/src/test/java/de/moviearchive/security/JwtServiceTest.java**
```java
package de.moviearchive.security;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

class JwtServiceTest {

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldGenerateAccessToken_withSubjectAndEmailClaim() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldValidateToken_returnsClaimsForValidToken() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectToken_whenSignatureInvalid() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectToken_whenTokenExpired() {}
}
```

**File 5: backend/src/test/java/de/moviearchive/mail/MailServiceTest.java**
```java
package de.moviearchive.mail;

import de.moviearchive.AbstractIntegrationTest;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

class MailServiceTest extends AbstractIntegrationTest {

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldSendVerificationEmail_withTokenLinkInBody() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldSendPasswordResetEmail_withResetLinkInBody() {}
}
```

**File 6: backend/src/test/java/de/moviearchive/token/RefreshTokenServiceTest.java**
```java
package de.moviearchive.token;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
class RefreshTokenServiceTest {

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldHandleGracePeriod_whenConcurrentRefreshWithinWindow() {}

    @Test
    @Disabled("Wave 0 stub — implement in Plan 02")
    void shouldRejectToken_whenGracePeriodExpired() {}
}
```

After creating all 6 files, verify they compile:
```
cd /path/to/backend && ./gradlew compileTestJava
```
  </action>
  <verify>
    <automated>cd /Users/simonreich/git/private/movie-archive/backend && ./gradlew compileTestJava --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileTestJava` exits 0 with no errors
    - `grep -r "@Disabled" backend/src/test/java/de/moviearchive/auth/` shows @Disabled on every test method in AuthControllerTest, AuthIntegrationTest, AuthServiceTest
    - `grep "extends AbstractIntegrationTest" backend/src/test/java/de/moviearchive/auth/AuthControllerTest.java` returns a match
    - `grep "extends AbstractIntegrationTest" backend/src/test/java/de/moviearchive/auth/AuthIntegrationTest.java` returns a match
    - `grep "spring.mail.port=3025" backend/src/test/resources/application-test.properties` returns a match
    - `grep "jwt.secret=test-secret-key-that-is-long-enough-32c" backend/src/test/resources/application-test.properties` returns a match (length >= 32)
  </acceptance_criteria>
  <done>6 backend stub files exist and compile; application-test.properties verified with GreenMail port 3025 and a 36-char JWT test secret.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend test stub files</name>
  <files>
    frontend/test/unit/stores/auth.spec.ts
    frontend/test/unit/composables/useAuth.spec.ts
    frontend/test/unit/middleware/auth.spec.ts
    frontend/test/unit/plugins/auth.spec.ts
    frontend/test/unit/pages/login.spec.ts
    frontend/test/unit/pages/signup.spec.ts
    frontend/test/unit/pages/forgot-password.spec.ts
    frontend/test/unit/pages/reset-password.spec.ts
    frontend/test/unit/pages/verify-email.spec.ts
  </files>
  <read_first>
    - frontend/vitest.config.ts (include pattern: test/unit/**/*.spec.ts — verify new files are in scope)
    - frontend/test/mocks/handlers.ts (MSW handler pattern — stubs reference these imports)
    - frontend/test/mocks/server.ts (server setup pattern)
    - frontend/test/unit/components/AppHome.spec.ts (existing passing spec — use as model for imports and structure)
    - .planning/phases/01-authentication/01-VALIDATION.md (exact behavior descriptions for stub test names)
  </read_first>
  <action>
Create 9 frontend test stub files. Each uses `test.skip(...)` so Vitest treats them as skipped (not failed). Create any missing intermediate directories (test/unit/stores, test/unit/composables, test/unit/middleware, test/unit/plugins, test/unit/pages).

**File 1: frontend/test/unit/stores/auth.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('auth store', () => {
  test.skip('setAuth populates accessToken and userEmail in-memory only', () => {})
  test.skip('clearAuth nullifies accessToken and userEmail', () => {})
  test.skip('isAuthenticated is true when accessToken is set', () => {})
  test.skip('refresh calls POST /api/auth/refresh and populates store', () => {})
  test.skip('accessToken is not persisted to localStorage', () => {})
})
```

**File 2: frontend/test/unit/composables/useAuth.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('useAuth composable', () => {
  test.skip('login calls POST /api/auth/login and populates store', () => {})
  test.skip('signup calls POST /api/auth/signup and redirects to /verify-email-sent', () => {})
  test.skip('logout calls POST /api/auth/logout and clears store', () => {})
  test.skip('verifyEmail calls POST /api/auth/verify-email with token from query', () => {})
  test.skip('forgotPassword calls POST /api/auth/forgot-password', () => {})
  test.skip('resetPassword calls POST /api/auth/reset-password with token and newPassword', () => {})
})
```

**File 3: frontend/test/unit/middleware/auth.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('auth.global middleware', () => {
  test.skip('redirects to /login when refresh_token cookie is absent on protected route', () => {})
  test.skip('allows navigation when refresh_token cookie is present', () => {})
  test.skip('allows navigation to /login without cookie', () => {})
  test.skip('allows navigation to /signup without cookie', () => {})
  test.skip('allows navigation to /forgot-password without cookie', () => {})
  test.skip('allows navigation to /reset-password without cookie', () => {})
  test.skip('allows navigation to /verify-email without cookie', () => {})
  test.skip('allows navigation to /verify-email-sent without cookie', () => {})
})
```

**File 4: frontend/test/unit/plugins/auth.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('auth.client plugin', () => {
  test.skip('calls /auth/refresh on app init when refresh cookie present', () => {})
  test.skip('does not redirect when refresh succeeds', () => {})
  test.skip('clears store silently when refresh fails (cookie expired)', () => {})
})
```

**File 5: frontend/test/unit/pages/login.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('/login page', () => {
  test.skip('renders email field, password field, and Sign in button', () => {})
  test.skip('shows inline error "Invalid email or password." on 401 response', () => {})
  test.skip('shows "Please verify your email before signing in." on 403 response', () => {})
  test.skip('shows "Too many attempts. Try again in X seconds." on 429 with Retry-After header', () => {})
  test.skip('redirects to / after successful login', () => {})
  test.skip('populates auth store after successful login', () => {})
  test.skip('shows spinner and disables button while request is in-flight', () => {})
})
```

**File 6: frontend/test/unit/pages/signup.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('/signup page', () => {
  test.skip('renders email field, password field, and Create account button', () => {})
  test.skip('shows "Enter a valid email address." for invalid email on submit', () => {})
  test.skip('shows "Password must be at least 8 characters." for short password on submit', () => {})
  test.skip('redirects to /verify-email-sent after successful signup', () => {})
  test.skip('shows "An account with this email already exists." on 409 response', () => {})
  test.skip('does not auto-login after signup', () => {})
})
```

**File 7: frontend/test/unit/pages/forgot-password.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('/forgot-password page', () => {
  test.skip('renders email field and Send reset link button', () => {})
  test.skip('shows success message after any 200 response regardless of email existence', () => {})
  test.skip('shows "Too many attempts. Try again in X seconds." on 429', () => {})
  test.skip('disables submit button during in-flight request', () => {})
})
```

**File 8: frontend/test/unit/pages/reset-password.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('/reset-password page', () => {
  test.skip('renders new password and confirm password fields', () => {})
  test.skip('shows "Passwords do not match." when confirm does not match', () => {})
  test.skip('shows "Password must be at least 8 characters." for short password', () => {})
  test.skip('shows success state and Sign in CTA after successful reset', () => {})
  test.skip('shows "This reset link has expired." on 400 with expired token message', () => {})
})
```

**File 9: frontend/test/unit/pages/verify-email.spec.ts**
```typescript
import { describe, test } from 'vitest'

describe('/verify-email page', () => {
  test.skip('shows "Verifying your email..." loading state on mount', () => {})
  test.skip('shows "Email verified" heading and Sign in CTA after successful verification', () => {})
  test.skip('shows "Verification failed" and expired message on 400 expired error', () => {})
  test.skip('shows already-used message on 400 consumed token error', () => {})
})
```

After creating all 9 files, run the frontend tests to confirm they pass (skipped stubs are passing):
```
cd /path/to/frontend && pnpm test --run
```
  </action>
  <verify>
    <automated>cd /Users/simonreich/git/private/movie-archive/frontend && pnpm test --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run` exits 0
    - Output contains "skipped" count >= 9 (one skip group per file at minimum)
    - `ls frontend/test/unit/stores/auth.spec.ts frontend/test/unit/composables/useAuth.spec.ts frontend/test/unit/middleware/auth.spec.ts frontend/test/unit/plugins/auth.spec.ts` — all 4 paths exist
    - `ls frontend/test/unit/pages/login.spec.ts frontend/test/unit/pages/signup.spec.ts frontend/test/unit/pages/forgot-password.spec.ts frontend/test/unit/pages/reset-password.spec.ts frontend/test/unit/pages/verify-email.spec.ts` — all 5 paths exist
    - `grep "test.skip" frontend/test/unit/pages/login.spec.ts` returns at least 5 matches
    - `grep "test.skip" frontend/test/unit/middleware/auth.spec.ts` returns at least 6 matches (one per public route)
  </acceptance_criteria>
  <done>9 frontend stub files exist; pnpm test --run exits 0 with all stubs skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → JVM | Test stubs run in the JVM — no external trust boundary in this plan |
| test → Node | Frontend stubs run in happy-dom — no external trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-STUB-01 | Information Disclosure | application-test.properties | accept | Test secrets are hardcoded fixed values for CI only; file is never deployed to production; no real secrets present |
| T-1-STUB-02 | Tampering | test stub files | accept | Stubs contain no production logic; @Disabled prevents execution; no attack surface |

</threat_model>

<verification>
Both ./gradlew compileTestJava and pnpm test --run must exit 0 before Plan 02 or Plan 03 can execute.

```bash
cd /Users/simonreich/git/private/movie-archive/backend && ./gradlew compileTestJava
cd /Users/simonreich/git/private/movie-archive/frontend && pnpm test --run
```
</verification>

<success_criteria>
- 6 backend test stub Java files exist and compile without errors
- application-test.properties confirmed: spring.mail.port=3025, jwt.secret >= 32 chars
- 9 frontend test stub TypeScript files exist
- pnpm test --run exits 0 (skipped count >= 9)
- ./gradlew compileTestJava exits 0
- No production code written in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-01-SUMMARY.md` with:
- What was created (list of all stub files)
- Verification results (compileTestJava output, pnpm test --run output)
- Any deviations from plan
</output>
