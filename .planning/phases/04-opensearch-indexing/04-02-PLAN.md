---
phase: 04-opensearch-indexing
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/src/main/java/de/moviearchive/config/OpenSearchConfig.java
  - backend/src/main/java/de/moviearchive/indexing/DocumentBuilder.java
  - backend/src/main/java/de/moviearchive/indexing/IndexingService.java
  - backend/src/main/resources/opensearch/movies-index.json
  - backend/src/main/java/de/moviearchive/movie/MovieRepository.java
  - backend/src/main/java/de/moviearchive/enrichment/EnrichmentService.java
  - backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java
autonomous: true
requirements: [IDX-01, IDX-02, IDX-03]
must_haves:
  truths:
    - "A film persisted to Postgres by the enrichment pipeline is indexed into movies-{userId} in OpenSearch"
    - "The index is auto-created on first write with the custom analyzer and the full 40+ field mapping"
    - "The custom analyzer normalizes accented characters and stems English words"
    - "If OpenSearch is unreachable during enrichment, indexed_at stays null and status stays SUCCESS (D-01)"
  artifacts:
    - path: "backend/src/main/java/de/moviearchive/config/OpenSearchConfig.java"
      provides: "OpenSearchClient bean via ApacheHttpClient5TransportBuilder"
      contains: "OpenSearchClient"
    - path: "backend/src/main/java/de/moviearchive/indexing/IndexingService.java"
      provides: "ensureIndexExists + index(Movie) + reindex helpers"
      contains: "ensureIndexExists"
    - path: "backend/src/main/java/de/moviearchive/indexing/DocumentBuilder.java"
      provides: "Movie -> Map<String,Object> 40+ field assembly from raw JSON"
      contains: "build"
    - path: "backend/src/main/resources/opensearch/movies-index.json"
      provides: "Full custom analyzer + 40+ field mapping per data-model.md"
      contains: "custom_english_analyzer"
    - path: "backend/src/main/java/de/moviearchive/movie/MovieRepository.java"
      provides: "findAllByUserId + findByUserIdAndIndexedAtIsNull"
      contains: "findByUserIdAndIndexedAtIsNull"
  key_links:
    - from: "EnrichmentService.enrich"
      to: "IndexingService.index"
      via: "Step 5 after Postgres SUCCESS save, silent on failure"
      pattern: "indexingService.index"
    - from: "IndexingService.ensureIndexExists"
      to: "opensearch/movies-index.json"
      via: "getResourceAsStream + CreateIndexRequest.withJson"
      pattern: "movies-index.json"
    - from: "EnrichmentService"
      to: "Movie.indexedAt"
      via: "setIndexedAt(Instant.now()) after successful OS write"
      pattern: "setIndexedAt"
---

<objective>
Build the OpenSearch client bean, the JSON index definition with the full 40+ field mapping and custom analyzer, the document builder that assembles a document from raw TMDB/OMDB JSON, the IndexingService, and wire it as Step 5 of the enrichment pipeline. Enable and implement the IndexingIntegrationTest cases for IDX-01, IDX-02, IDX-03.

Purpose: This is the core of Phase 4 — every saved film becomes a searchable OpenSearch document with the production analyzer. D-01 silent-failure semantics are preserved so an OpenSearch outage never breaks the save flow.

Output: OpenSearchConfig.java, DocumentBuilder.java, IndexingService.java, full movies-index.json, MovieRepository (2 new queries), EnrichmentService Step 5, IndexingIntegrationTest implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-opensearch-indexing/04-CONTEXT.md
@.planning/phases/04-opensearch-indexing/04-RESEARCH.md
@.planning/phases/04-opensearch-indexing/04-PATTERNS.md
@.claude/data-model.md
@backend/src/main/java/de/moviearchive/enrichment/EnrichmentService.java
@backend/src/main/java/de/moviearchive/movie/MovieRepository.java

<interfaces>
<!-- Key contracts the executor needs. Use directly — no codebase exploration needed. -->

application.properties (lines 18-19): opensearch.host=${OPENSEARCH_HOST:localhost}, opensearch.port=${OPENSEARCH_PORT:9200}

opensearch-java 2.19.0 client API (already in build.gradle.kts):
- new OpenSearchClient(ApacheHttpClient5TransportBuilder.builder(new HttpHost("http", host, port)).setMapper(new JacksonJsonpMapper()).build())
- client.indices().exists(ExistsRequest.of(r -> r.index(name))) -> BooleanResponse, .value()
- client.indices().create(CreateIndexRequest.of(r -> r.index(name).withJson(inputStream)))
- client.index(IndexRequest.of(r -> r.index(name).id(id).document(map)))
- OpenSearchException.error().type() == "resource_already_exists_exception" (concurrent-create race — swallow)
- Do NOT use RestClientTransport (deprecated). Do NOT add client.ping() at startup.

Movie entity (de.moviearchive.movie.Movie) relevant getters: getId() UUID, getUser() (User; loaded eagerly via findByIdWithUser JOIN FETCH), getTmdbId() Integer, getImdbId() String, getTitle(), getOriginalTitle(), getReleaseDate() LocalDate, getRuntime() Integer, getRawTmdbJson() JsonNode, getRawOmdbJson() JsonNode (nullable), getWikiUrl(), getWikiSummary(), getWikiPlot(), getWikiCritics(); setter setIndexedAt(Instant).
User: getId() UUID.

EnrichmentService injection point (line 109-112): after `movie.setStatus(MovieStatus.SUCCESS); movieRepository.save(movie); log.info(...)`. Movie here is loaded via findByIdWithUser (user eagerly fetched — no LazyInitializationException calling movie.getUser().getId()).

JsonNode field extraction pattern (established in EnrichmentService lines 59-73):
node.path("field").asText(null) for strings, .asInt(0) for ints, .asDouble(0) for doubles.

MovieRepository existing @Query pattern uses m.user.id (not m.userId): @Query("SELECT m FROM Movie m WHERE m.user.id = :userId").

data-model.md OpenSearch Field Mapping table is the AUTHORITATIVE 40+ field list — do not deviate. Fields marked "Indexed: no" use JSON `"index": false`. nested fields use `"type": "nested"`. flattened fields use `"type": "flattened"`. text fields use `"analyzer": "custom_english_analyzer"`. title/original_title add `"fields": {"raw": {"type": "keyword"}}`. keyword fields with a `.text` sub-field add `"fields": {"text": {"type": "text", "analyzer": "custom_english_analyzer"}}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OpenSearchConfig bean + full movies-index.json + MovieRepository queries</name>
  <files>backend/src/main/java/de/moviearchive/config/OpenSearchConfig.java, backend/src/main/resources/opensearch/movies-index.json, backend/src/main/java/de/moviearchive/movie/MovieRepository.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/config/AsyncConfig.java (analog for @Configuration + @Bean structure)
    - backend/src/main/java/de/moviearchive/settings/SettingsService.java (analog for @Value injection pattern)
    - backend/src/main/resources/opensearch/movies-index.json (skeleton from 04-01 — replace, must remove the _skeleton marker)
    - backend/src/main/java/de/moviearchive/movie/MovieRepository.java (file being modified — preserve existing 5 methods)
    - .claude/data-model.md (AUTHORITATIVE OpenSearch Field Mapping table — every field, type, indexed flag, sub-field)
    - .planning/phases/04-opensearch-indexing/04-RESEARCH.md (Pattern 1 client bean; Pattern 2 JSON index creation; Code Examples §Complete Index Definition JSON)
  </read_first>
  <behavior>
    - movies-index.json deserializes via OpenSearch withJson() and a fresh index can be created from it (verified indirectly in Task 3 test shouldCreateIndex_whenNotExists)
    - The custom analyzer applies asciifolding + lowercase + elision + stop_english + kstem (verified in Task 3 shouldNormalizeAccents / shouldStemEnglishWords)
    - findByUserIdAndIndexedAtIsNull returns only movies with null indexed_at for the given user
    - findAllByUserId returns all movies for the given user
  </behavior>
  <action>
    Create `de.moviearchive.config.OpenSearchConfig` annotated `@Configuration`, with `@Value("${opensearch.host}") String host` and `@Value("${opensearch.port}") int port` fields, and a `@Bean OpenSearchClient openSearchClient()` method that builds `new HttpHost("http", host, port)`, an `ApacheHttpClient5TransportBuilder.builder(httpHost).setMapper(new JacksonJsonpMapper()).build()` transport, and returns `new OpenSearchClient(transport)`. Scheme MUST be the literal `"http"` (DISABLE_SECURITY_PLUGIN). Do NOT call `client.ping()` or add any startup connectivity check. Imports: `org.apache.hc.core5.http.HttpHost`, `org.opensearch.client.opensearch.OpenSearchClient`, `org.opensearch.client.transport.httpclient5.ApacheHttpClient5TransportBuilder`, `org.opensearch.client.json.jackson.JacksonJsonpMapper`.

    Replace `backend/src/main/resources/opensearch/movies-index.json` with the FULL index definition (remove the `_skeleton` marker entirely). `settings.analysis.filter.stop_english` = `{"type":"stop","stopwords":"_english_"}`. `settings.analysis.analyzer.custom_english_analyzer` = `{"type":"custom","tokenizer":"standard","filter":["asciifolding","lowercase","elision","stop_english","kstem"]}`. `mappings.properties` MUST contain every field from the `.claude/data-model.md` "Field Mapping" table with the exact type from that table: `tmdb_id` integer; `imdb_id` keyword; `title`/`original_title` text + analyzer custom_english_analyzer + `fields.raw` keyword; `tagline`/`overview`/`wikipedia_summary`/`wikipedia_plot`/`wikipedia_critics`/`personal_notes` text + analyzer; `year`/`runtime`/`vote_count`/`imdb_votes`/`box_office` integer; `release_date` date; `vote_average`/`imdb_rating`/`personal_rating` float; `content_rating`/`imdb_link`/`trailer_key`/`wikipedia_url` keyword; `genre_list`/`director_list`/`writer_list`/`main_cast`/`full_cast_names`/`full_crew_names`/`country_list`/`language_list`/`company_list`/`keyword_list` keyword + `fields.text` text+analyzer; `full_cast`/`full_crew` nested; `rating_list`/`poster_list`/`backdrop_list`/`video_list` flattened; `watched` boolean; `poster_path`/`backdrop_path` keyword. Every field whose data-model.md "Indexed" column is `no` (poster_path, backdrop_path, box_office, poster_list, backdrop_list, video_list, trailer_key, wikipedia_url, wikipedia_plot_html, wikipedia_full_html) MUST include `"index": false`. Include `wikipedia_plot_html` and `wikipedia_full_html` as text with `"index": false`.

    In `MovieRepository.java` add two methods following the existing explicit `@Query` pattern (not Spring Data name derivation, per RESEARCH open question 2): `@Query("SELECT m FROM Movie m WHERE m.user.id = :userId") List<Movie> findAllByUserId(@Param("userId") UUID userId);` and `@Query("SELECT m FROM Movie m WHERE m.user.id = :userId AND m.indexedAt IS NULL") List<Movie> findByUserIdAndIndexedAtIsNull(@Param("userId") UUID userId);`. Preserve all 5 existing methods unchanged.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - OpenSearchConfig.java contains `@Configuration`, `ApacheHttpClient5TransportBuilder`, `new OpenSearchClient`, and the literal `"http"`
    - OpenSearchConfig.java does NOT contain `RestClientTransport` or `.ping(`
    - movies-index.json is valid JSON, does NOT contain `_skeleton`, contains `custom_english_analyzer` with filter array `asciifolding`, `lowercase`, `elision`, `stop_english`, `kstem`
    - movies-index.json mappings.properties contains every field name from data-model.md Field Mapping table (spot-check: full_cast type nested, rating_list type flattened, poster_path "index": false, personal_notes analyzer custom_english_analyzer)
    - MovieRepository.java contains `findAllByUserId` and `findByUserIdAndIndexedAtIsNull` each with an `@Query` annotation referencing `m.user.id`
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>OpenSearchClient bean compiles, movies-index.json has the full data-model.md mapping + analyzer with no skeleton marker, MovieRepository exposes the two new queries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DocumentBuilder + IndexingService + EnrichmentService Step 5</name>
  <files>backend/src/main/java/de/moviearchive/indexing/DocumentBuilder.java, backend/src/main/java/de/moviearchive/indexing/IndexingService.java, backend/src/main/java/de/moviearchive/enrichment/EnrichmentService.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/enrichment/EnrichmentService.java (file being modified — Step 5 inserted after line 112; field-extraction pattern lines 59-73; constructor lines 27-37)
    - backend/src/main/java/de/moviearchive/settings/SettingsService.java (analog for @Service + constructor injection)
    - backend/src/main/java/de/moviearchive/movie/Movie.java (entity getters/setters DocumentBuilder reads and setIndexedAt)
    - .claude/data-model.md (which fields come from TMDB raw json vs OMDB raw json vs Wikipedia vs computed)
    - .planning/phases/04-opensearch-indexing/04-RESEARCH.md (Pattern 3 index document; Pattern 4 EnrichmentService injection; Pitfall 3 lazy-load; Pitfall 4 concurrent create; Code Examples §DocumentBuilder)
    - .planning/phases/04-opensearch-indexing/04-PATTERNS.md (DocumentBuilder, IndexingService, EnrichmentService modified sections)
  </read_first>
  <behavior>
    - DocumentBuilder.build(movie) returns a Map<String,Object> with title/original_title/release_date/year/runtime/imdb_link populated from Movie scalars + computed values
    - OMDB-sourced keys (imdb_rating, content_rating, etc.) are present-but-null when getRawOmdbJson() is null (no NPE)
    - watched, personal_rating, personal_notes are always null in Phase 4 (D-05)
    - IndexingService.index(movie) creates index movies-{userId} on first call, writes the document, is a no-op create on the second call (idempotent), and swallows resource_already_exists_exception
    - EnrichmentService.enrich: after SUCCESS save, calls indexingService.index(movie); on success sets indexed_at and saves; on any exception logs warn and leaves status SUCCESS + indexed_at null
  </behavior>
  <action>
    Create `de.moviearchive.indexing.DocumentBuilder` annotated `@Component @Slf4j` with `public Map<String,Object> build(Movie movie)`. Read `JsonNode tmdb = movie.getRawTmdbJson()` (non-null after enrichment) and `JsonNode omdb = movie.getRawOmdbJson()` (nullable). Populate a `HashMap` keyed by EVERY field name in the data-model.md mapping. Scalars from Movie getters: tmdb_id, imdb_id, title, original_title, release_date (toString or null), runtime. Computed: `year` = release year or null; `imdb_link` = `"https://www.imdb.com/title/" + imdbId` or null. From `tmdb` JsonNode using `.path(...).asText(null)`/`.asInt(0)`/`.asDouble(0)`: tagline, overview, vote_average, vote_count, poster_path, backdrop_path, and the list fields (genre_list, director_list, writer_list, country_list, language_list, company_list, keyword_list, full_cast, full_cast_names, full_crew, full_crew_names, poster_list, backdrop_list, video_list, trailer_key) — extract from the appropriate TMDB JSON nodes per data-model.md Source column; use the established null-safe path() pattern; arrays map to `List<...>`. From `omdb` guarded by `if (omdb != null && !omdb.isNull())`: imdb_rating, imdb_votes, content_rating, box_office, rating_list, main_cast (all null when omdb absent). Wikipedia from Movie getters: wikipedia_url, wikipedia_summary, wikipedia_plot, wikipedia_critics (and wikipedia_plot_html/wikipedia_full_html if present on the entity, else null). Personal fields ALWAYS null (D-05): `doc.put("watched", null); doc.put("personal_rating", null); doc.put("personal_notes", null);`. Add a private helper for safe Double parsing of OMDB string ratings.

    Create `de.moviearchive.indexing.IndexingService` annotated `@Service @Slf4j` with constructor-injected `OpenSearchClient client`, `MovieRepository movieRepository`, `DocumentBuilder documentBuilder`. Methods: `void ensureIndexExists(String indexName)` — call `client.indices().exists(ExistsRequest.of(r -> r.index(indexName)))`; if `.value()` return; else load `getClass().getClassLoader().getResourceAsStream("opensearch/movies-index.json")` and `client.indices().create(CreateIndexRequest.of(r -> r.index(indexName).withJson(stream)))`; wrap the create in try/catch and if `OpenSearchException` with `e.error().type()` equal to `"resource_already_exists_exception"` swallow it (concurrent-create race, Pitfall 4), else rethrow; `log.info("Created OpenSearch index: {}", indexName)`. `void index(Movie movie)` — compute `String indexName = "movies-" + movie.getUser().getId()` (user is eagerly fetched via findByIdWithUser — see Pitfall 3; do NOT re-fetch without JOIN FETCH), call `ensureIndexExists(indexName)`, build the doc via `documentBuilder.build(movie)`, and `client.index(IndexRequest.of(r -> r.index(indexName).id(movie.getId().toString()).document(doc)))`. Do NOT annotate `index()` or the class with `@Async`. Do NOT add `@Retryable` (D-01 silent fail is the recovery contract; admin reindex is the recovery path). Do NOT put `@Transactional` at class level.

    Modify `EnrichmentService`: add `IndexingService indexingService` as a constructor parameter and field (constructor injection, no `@Autowired`). After the existing Step 4 block (`movie.setStatus(MovieStatus.SUCCESS); movieRepository.save(movie); log.info("Enrichment complete...")` at line ~112), insert Step 5: a `try { indexingService.index(movie); movie.setIndexedAt(Instant.now()); movieRepository.save(movie); log.info("OpenSearch indexed movieId={}", movieId); } catch (Exception e) { log.warn("OpenSearch indexing failed for movieId={} — indexed_at stays null: {}", movieId, e.getMessage()); }` — no rethrow, status stays SUCCESS (D-01). Add `import java.time.Instant;` if not present.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - DocumentBuilder.java contains `@Component`, method `build(Movie`, and `doc.put("watched", null)`, `doc.put("personal_rating", null)`, `doc.put("personal_notes", null)`
    - DocumentBuilder.java guards OMDB extraction with a null check on getRawOmdbJson()
    - IndexingService.java contains `ensureIndexExists`, `client.indices().exists`, `withJson`, `IndexRequest.of`, `"movies-"`, and the `resource_already_exists_exception` swallow branch
    - IndexingService.java does NOT contain `@Async` or `@Retryable`
    - EnrichmentService.java constructor includes an `IndexingService` parameter and the class contains `indexingService.index(movie)` and `setIndexedAt(Instant.now())` inside a try/catch that logs `log.warn` without rethrow
    - `./gradlew compileJava` exits 0
  </acceptance_criteria>
  <done>DocumentBuilder assembles the full document map (personal fields null), IndexingService creates the index and writes documents idempotently, EnrichmentService Step 5 indexes with D-01 silent-failure semantics — all compiling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement IndexingIntegrationTest (IDX-01, IDX-02, IDX-03)</name>
  <files>backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java</files>
  <read_first>
    - backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java (the @Disabled stub from 04-01 — remove class-level @Disabled, fill method bodies)
    - backend/src/test/java/de/moviearchive/movie/EnrichmentIntegrationTest.java (analog — @BeforeEach cleanDb, test user creation, fixture loading helper)
    - backend/src/test/java/de/moviearchive/AbstractOpenSearchTest.java (base class providing the OpenSearch container)
    - backend/src/test/resources/fixtures/tmdb/inception-detail.json (existing TMDB fixture for raw json; reuse the loadFixture pattern)
    - .planning/phases/04-opensearch-indexing/04-VALIDATION.md (exact test names + required behaviors per IDX requirement)
    - .planning/phases/04-opensearch-indexing/04-RESEARCH.md (Pitfall 4 concurrent create; analyzer filter behaviors)
  </read_first>
  <behavior>
    - shouldIndexFilm_afterPostgresPersist: a Movie with rawTmdbJson set, after indexingService.index(movie), is retrievable from OpenSearch index movies-{userId} by movie id; indexed_at is set after EnrichmentService path
    - shouldLeaveIndexedAtNull_whenOsFails: when the OpenSearch write fails (e.g., point client at unreachable host via a constructed IndexingService, or assert behavior when index op throws), movie status stays SUCCESS and indexed_at stays null
    - shouldCreateIndex_whenNotExists: after index(movie) for a fresh user, client.indices().exists(movies-{userId}) returns true
    - shouldNotThrow_whenIndexAlreadyExists: calling ensureIndexExists twice for the same index does not throw
    - shouldNormalizeAccents: indexing a title with accented chars and querying via _analyze with custom_english_analyzer folds diacritics (e.g. "Amélie" -> token "amelie")
    - shouldStemEnglishWords: _analyze with custom_english_analyzer stems an English word via kstem (e.g. "running" -> "run" or "runs" -> "run")
  </behavior>
  <action>
    Remove the class-level `@Disabled` from `IndexingIntegrationTest` (it `extends AbstractOpenSearchTest`). Autowire `IndexingService`, `OpenSearchClient`, `MovieRepository`, `UserRepository`. Add `@BeforeEach` that deletes all movies + users (EnrichmentIntegrationTest pattern) and deletes the test user's OpenSearch index if it exists (catch index_not_found). Create a test User (ACTIVE, placeholder bcrypt hash) and persist; create a Movie with `setUser`, `setTmdbId`, `setRawTmdbJson` (parse a small JSON fixture or build a minimal ObjectNode with title/original_title/release_date), `setTitle`, status SUCCESS, persist. Implement the six methods exactly:
    - `shouldIndexFilm_afterPostgresPersist`: call `indexingService.index(movie)`, then `client.get(...)` (or search) on `movies-{userId}` with the movie id and assert the document title matches.
    - `shouldLeaveIndexedAtNull_whenOsFails`: construct an `IndexingService` with an `OpenSearchClient` pointed at an unreachable host (HttpHost "http","localhost",1) OR spy/stub so `index()` throws; drive the EnrichmentService Step 5 contract by asserting that after the failure the movie's `getIndexedAt()` is null and `getStatus()` is SUCCESS. (If wiring EnrichmentService is heavy, assert IndexingService.index throws and that a try/catch around it leaves indexed_at null — mirror the D-01 semantics directly.)
    - `shouldCreateIndex_whenNotExists`: assert `client.indices().exists` is false before, call `indexingService.index(movie)`, assert exists true after.
    - `shouldNotThrow_whenIndexAlreadyExists`: call `indexingService.ensureIndexExists("movies-" + userId)` twice; `assertDoesNotThrow`.
    - `shouldNormalizeAccents`: after index creation, call `client.indices().analyze(...)` (AnalyzeRequest) with analyzer `custom_english_analyzer` and text containing an accented word; assert the token list contains the ascii-folded form.
    - `shouldStemEnglishWords`: same `analyze` call with an English inflected word; assert the kstem-stemmed token is produced.
    Use AssertJ assertions consistent with EnrichmentIntegrationTest.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.indexing.IndexingIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - IndexingIntegrationTest.java no longer has a class-level `@Disabled`
    - All six methods have real assertion bodies (no empty/TODO bodies; each contains at least one `assert`)
    - `./gradlew test --tests "de.moviearchive.indexing.IndexingIntegrationTest"` exits 0 with 6 tests run, 0 failures, 0 skipped
    - shouldLeaveIndexedAtNull_whenOsFails asserts both `getStatus()` SUCCESS and `getIndexedAt()` null
    - shouldNormalizeAccents and shouldStemEnglishWords assert on tokens returned by an OpenSearch `analyze` call against `custom_english_analyzer`
  </acceptance_criteria>
  <done>IndexingIntegrationTest is enabled and all six IDX-01/02/03 tests pass against the real OpenSearch container.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichment thread → OpenSearch | Movie data (from external TMDB/OMDB) is written as a document |
| Movie raw JSON → document | Untrusted external API content assembled into the index document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | OS document assembly from raw TMDB/OMDB JSON | mitigate | Document built as `Map<String,Object>` serialized by JacksonJsonpMapper — no string interpolation into queries or index names |
| T-04-02-02 | Tampering | Index name construction `"movies-" + userId` | mitigate | userId is a UUID (no path-traversal characters); name is server-derived, never client-supplied |
| T-04-02-03 | Denial of Service | OpenSearch unreachable during enrichment | accept | D-01: failure is silent, status stays SUCCESS, indexed_at null; admin reindex is the recovery path (plan 04-03) |
| T-04-02-04 | Tampering | Concurrent ensureIndexExists race | mitigate | Catch `resource_already_exists_exception` and treat as success (Pitfall 4) |
| T-04-02-SC | Tampering | Gradle dependency installs | accept | No new dependency — opensearch-java 2.19.0 and httpclient5 5.4.4 already in build.gradle.kts (RESEARCH.md VERIFIED); no install task in this plan |
</threat_model>

<verification>
- `./gradlew compileJava` exits 0 (Tasks 1, 2)
- `./gradlew test --tests "de.moviearchive.indexing.IndexingIntegrationTest"` exits 0, 6 tests pass (Task 3)
- `./gradlew test` full suite green before phase gate (no regression in enrichment tests after EnrichmentService change)
- D-01 preserved: shouldLeaveIndexedAtNull_whenOsFails proves status SUCCESS + indexed_at null on OS failure
- D-05 preserved: DocumentBuilder writes watched/personal_rating/personal_notes as null
</verification>

<success_criteria>
- OpenSearchClient bean created via ApacheHttpClient5TransportBuilder (no RestClientTransport, no startup ping)
- movies-index.json contains the full 40+ field mapping per data-model.md and the custom analyzer (asciifolding, lowercase, elision, stop_english, kstem)
- Every saved film is indexed into movies-{userId} as Step 5 of EnrichmentService
- OpenSearch failure leaves status=SUCCESS and indexed_at=null (D-01)
- MovieRepository exposes findAllByUserId + findByUserIdAndIndexedAtIsNull for plan 04-03
- IndexingIntegrationTest green for IDX-01, IDX-02, IDX-03
</success_criteria>

<output>
Create `.planning/phases/04-opensearch-indexing/04-02-SUMMARY.md` when done

Note for downstream: D-06 (Phase 6 integration requirement) — when Phase 6 saves personal fields (watched/personal_rating/personal_notes), that service MUST call IndexingService.index(movie) to upsert the OpenSearch document. personal_notes must be searchable per DETAIL-03. This is NOT Phase 4 scope; the Phase 6 planner must pick it up.
</output>
