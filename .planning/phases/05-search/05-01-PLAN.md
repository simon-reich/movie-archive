---
phase: 05-search
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/src/test/java/de/moviearchive/search/SearchControllerTest.java
  - frontend/test/mocks/handlers/search.ts
  - frontend/test/mocks/handlers.ts
  - frontend/test/unit/composables/useSearch.spec.ts
  - frontend/test/unit/pages/search.spec.ts
  - frontend/test/unit/pages/index.spec.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04]

must_haves:
  truths:
    - "Backend test class SearchControllerTest exists with disabled stubs for every SRCH-01..04 behavior"
    - "Frontend MSW handlers for POST /api/search, GET /api/dashboard, GET /api/search/autocomplete exist and are registered"
    - "Frontend spec files for useSearch, search page, and dashboard page exist with todo stubs"
    - "Full backend test suite stays green (disabled stubs do not fail); full frontend suite stays green (todo stubs do not fail)"
  artifacts:
    - path: "backend/src/test/java/de/moviearchive/search/SearchControllerTest.java"
      provides: "Disabled integration test stubs for all Phase 5 backend behaviors"
      contains: "class SearchControllerTest"
    - path: "frontend/test/mocks/handlers/search.ts"
      provides: "MSW handlers for search/dashboard/autocomplete endpoints"
      exports: ["searchHandlers"]
    - path: "frontend/test/unit/composables/useSearch.spec.ts"
      provides: "Todo stubs for URL-param-driven search behaviors"
    - path: "frontend/test/unit/pages/search.spec.ts"
      provides: "Todo stubs for search page component behaviors"
    - path: "frontend/test/unit/pages/index.spec.ts"
      provides: "Todo stubs for dashboard page component behaviors"
  key_links:
    - from: "frontend/test/mocks/handlers.ts"
      to: "frontend/test/mocks/handlers/search.ts"
      via: "import { searchHandlers } and spread into handlers array"
      pattern: "searchHandlers"
---

<objective>
Create the Wave 0 test scaffolding for Phase 5 (Search). This plan produces failing-by-design (disabled / todo) test stubs and the MSW mock layer so Waves 1 and 2 have a verification target before any production code exists.

Purpose: Nyquist compliance — every later task has an `<automated>` command that resolves to a real file. No production code in this plan; only test infrastructure.
Output: One backend disabled test class, one MSW handler module + registration, three frontend todo spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-search/05-VALIDATION.md
@.planning/phases/05-search/05-PATTERNS.md
@.planning/phases/05-search/05-RESEARCH.md

<interfaces>
<!-- The MSW response shapes below are the contract that Wave 1/2 backend DTOs and frontend composables must honor. -->

POST /api/search response body:
  { results: SearchResultItem[], total: number, page: number, totalPages: number, hasMore: boolean }
SearchResultItem:
  { id: string, tmdbId: number, title: string, year: number, posterPath: string,
    directorList: string[], genreList: string[], imdbRating: number, runtime: number }

GET /api/dashboard response body:
  { totalFilms: number,
    topGenres: { name: string, count: number }[],
    languageBreakdown: { code: string, count: number }[],
    imdbHistogram: { label: string, count: number }[],
    movieOfTheDay: { id: string, title: string, year: number, posterPath: string } | null,
    recentlyAdded: { id: string, title: string, year: number, posterPath: string }[] }

GET /api/search/autocomplete?field=&prefix= response body:
  { suggestions: string[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend SearchControllerTest disabled stubs</name>
  <files>backend/src/test/java/de/moviearchive/search/SearchControllerTest.java</files>
  <read_first>
    - backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java (exact analog: base class, MockMvc, createActiveUser, loginAndGetToken, persistMovie, deleteIndexIfExists, refreshIndex helpers)
    - backend/src/test/java/de/moviearchive/AbstractOpenSearchTest.java (base class SearchControllerTest extends)
    - backend/src/test/java/de/moviearchive/indexing/IndexingIntegrationTest.java (refreshIndex pattern, indexing fixture pattern)
    - .planning/phases/05-search/05-VALIDATION.md (Per-Task Verification Map — exact test method names required)
    - .planning/phases/05-search/05-PATTERNS.md (SearchControllerTest section lines 296-412 — copy helper patterns verbatim)
  </read_first>
  <action>
    Create `backend/src/test/java/de/moviearchive/search/SearchControllerTest.java` in package `de.moviearchive.search`. Annotate the class with `@AutoConfigureMockMvc` and `extends AbstractOpenSearchTest`. Autowire `MockMvc`, `UserRepository`, `MovieRepository`, `IndexingService`, `OpenSearchClient`. Add a private `ObjectMapper objectMapper = new ObjectMapper()`.

    Copy these helper methods verbatim from ReindexControllerTest (per 05-PATTERNS.md lines 296-412): `cleanDb()` `@BeforeEach`, `createActiveUser(String email)`, `loginAndGetToken(String email)`, `persistMovie(User, String title, Instant indexedAt)` with the `tmdbIdSeq` counter starting at 1000, `deleteIndexIfExists(String indexName)`, `refreshIndex(String indexName)`.

    Add one `@Test @org.junit.jupiter.api.Disabled("Wave 1: SearchController not yet implemented")` stub method PER ROW in 05-VALIDATION.md Per-Task Verification Map for plan 02 (Wave 1). Required method names exactly: `shouldReturnAllFilms_whenQueryIsEmpty`, `shouldFindFilmByTitle`, `shouldFindFilmByOverview`, `shouldNormalizeAccentsInSearch`, `shouldFilterBySingleGenre`, `shouldFilterByMultipleGenresOR`, `shouldFilterByDirector`, `shouldFilterByYearRange`, `shouldFilterByImdbRating`, `shouldCombineGenreAndDirectorFilters`, `shouldReturnEmpty_whenWatchedFilterApplied`, `shouldSortByTitleAscending`, `shouldSortByImdbRatingDescending`, `shouldSortByPersonalRating_nullsLast`. Each stub body: a single `// Wave 1 implements: <one-line behavior summary>` comment plus `org.junit.jupiter.api.Assertions.fail("not implemented")` so an accidentally-enabled stub fails loudly. Do NOT add `throws Exception` semantics that require unused imports; keep stub bodies minimal. Each method declares `throws Exception`.

    Do NOT implement SearchController, SearchService, or any production class — this is scaffolding only. Disabled tests must not block the suite.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest" 2>&1 | grep -E "BUILD SUCCESSFUL|0 tests|skipped"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/src/test/java/de/moviearchive/search/SearchControllerTest.java` exists with `class SearchControllerTest extends AbstractOpenSearchTest`
    - File contains all 14 method names listed in the action, each annotated `@Disabled`
    - `cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest"` exits 0 (BUILD SUCCESSFUL; all tests skipped, none failed)
    - `cd backend && ./gradlew compileTestJava` exits 0 (file compiles)
  </acceptance_criteria>
  <done>SearchControllerTest compiles, all 14 stubs disabled, gradle test for the class is green with zero executed tests.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend MSW handlers + registration</name>
  <files>frontend/test/mocks/handlers/search.ts, frontend/test/mocks/handlers.ts</files>
  <read_first>
    - frontend/test/mocks/handlers/movies.ts (exact analog for handler module structure and HttpResponse.json usage)
    - frontend/test/mocks/handlers.ts (registration barrel — must add searchHandlers import + spread)
    - .planning/phases/05-search/05-RESEARCH.md lines 796-845 (MSW handler example with exact response shapes)
    - .planning/phases/05-search/05-PATTERNS.md lines 640-684 (handler module + registration pattern)
  </read_first>
  <action>
    Create `frontend/test/mocks/handlers/search.ts` exporting `export const searchHandlers = [ ... ]`. Import `{ http, HttpResponse } from 'msw'`. Define three handlers matching the `<interfaces>` contract in this plan's context:

    1. `http.post('/api/search', async ({ request }) => ...)` — returns `{ results: [ { id: 'test-uuid-1', tmdbId: 27205, title: 'Inception', year: 2010, posterPath: '/oYuLEt3zVCKq57qu2F8dT7NIa6f.jpg', directorList: ['Christopher Nolan'], genreList: ['Sci-Fi','Thriller'], imdbRating: 8.8, runtime: 148 } ], total: 1, page: 0, totalPages: 1, hasMore: false }`.
    2. `http.get('/api/dashboard', () => ...)` — returns `{ totalFilms: 42, topGenres: [{ name:'Drama', count:12 },{ name:'Thriller', count:8 }], languageBreakdown: [{ code:'en', count:30 }], imdbHistogram: [{ label:'7-8', count:15 },{ label:'9-10', count:5 }], movieOfTheDay: { id:'test-uuid-2', title:'The Godfather', year:1972, posterPath:'/xxx.jpg' }, recentlyAdded: [{ id:'test-uuid-1', title:'Inception', year:2010, posterPath:'/yyy.jpg' }] }`.
    3. `http.get('/api/search/autocomplete', () => ...)` — returns `{ suggestions: ['Christopher Nolan'] }`.

    Modify `frontend/test/mocks/handlers.ts`: add `import { searchHandlers } from './handlers/search'` after the moviesHandlers import, and add `...searchHandlers,` as the last spread entry in the `handlers` array (after `...moviesHandlers,`).
  </action>
  <verify>
    <automated>cd frontend && pnpm test 2>&1 | grep -E "passed|Test Files" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/test/mocks/handlers/search.ts` exists and exports `searchHandlers` as an array of 3 handlers
    - `frontend/test/mocks/handlers.ts` contains `import { searchHandlers }` and `...searchHandlers` inside the handlers array
    - `cd frontend && pnpm test` exits 0 with no new failing tests (existing suite stays green)
  </acceptance_criteria>
  <done>searchHandlers registered in the barrel, frontend suite green, response shapes match the interfaces contract.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend todo spec stubs (useSearch, search page, dashboard page)</name>
  <files>frontend/test/unit/composables/useSearch.spec.ts, frontend/test/unit/pages/search.spec.ts, frontend/test/unit/pages/index.spec.ts</files>
  <read_first>
    - frontend/test/unit/composables/useMovies.spec.ts (exact analog: vi.stubGlobal pattern, dynamic import)
    - frontend/test/unit/pages/add.spec.ts (exact analog: module import test, mockFetch pattern)
    - .planning/phases/05-search/05-VALIDATION.md (Per-Task Verification Map rows 05-03-01..05-04-01 — exact behaviors to stub)
    - .planning/phases/05-search/05-PATTERNS.md lines 688-746 (spec file patterns)
  </read_first>
  <action>
    Create three Vitest spec files using `it.todo(...)` (NOT `it(...)`) so the suite stays green while signposting Wave 2 work.

    `frontend/test/unit/composables/useSearch.spec.ts`: `describe('useSearch composable', ...)` with `it.todo('reads q param from URL on mount')`, `it.todo('reads genre param (single and array) from URL and normalizes to array')`, `it.todo('reads director param from URL')`, `it.todo('debounces query changes by ~300ms before updating URL')`, `it.todo('appends results on load-more (page > 0), replaces on page 0')`, `it.todo('re-executes search when route query changes (clickable attribute navigation)')`.

    `frontend/test/unit/pages/search.spec.ts`: `describe('/search page', ...)` with `it.todo('search page module exports a valid Vue component')`, `it.todo('executes search on mount')`, `it.todo('clicking a genre chip pushes /search?genre=X to the router')`, `it.todo('renders MovieGrid when viewMode is grid, MovieList when list')`.

    `frontend/test/unit/pages/index.spec.ts`: `describe('/ dashboard page', ...)` with `it.todo('dashboard page module exports a valid Vue component')`, `it.todo('fetches dashboard data on mount')`, `it.todo('renders movie of the day and recently added when films exist')`, `it.todo('shows Add your first film CTA when totalFilms is 0')`.

    Each file imports `{ describe, it } from 'vitest'`. Do NOT add real assertions or dynamic component imports yet — todo stubs only. Wave 2 plans (05-03, 05-04) convert `.todo` to real `it` blocks.
  </action>
  <verify>
    <automated>cd frontend && pnpm test 2>&1 | grep -E "todo|passed|Test Files" | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `frontend/test/unit/composables/` and `frontend/test/unit/pages/`
    - Each file uses `it.todo(` (verified by grep: `grep -c "it.todo" frontend/test/unit/composables/useSearch.spec.ts` returns >= 6)
    - `cd frontend && pnpm test` exits 0; todo tests reported as todo, zero failures
  </acceptance_criteria>
  <done>Three todo spec files exist, frontend suite green, Wave 2 behaviors signposted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked HTTP | MSW intercepts; no real network. No untrusted input crosses any production boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Test scaffolding only — no production code path | accept | This plan creates only test stubs and mocks; no runtime attack surface introduced. Production threats are mitigated in plans 05-02/05-03/05-04. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (05-RESEARCH.md Package Legitimacy Audit: zero new dependencies). No install task in this plan. |
</threat_model>

<verification>
- `cd backend && ./gradlew test --tests "de.moviearchive.search.SearchControllerTest"` → BUILD SUCCESSFUL, all skipped
- `cd backend && ./gradlew test` → full suite stays green (disabled stubs do not run)
- `cd frontend && pnpm test` → full suite green, todo tests reported as todo
</verification>

<success_criteria>
- SearchControllerTest compiles with 14 disabled stubs covering SRCH-01..04 backend behaviors
- searchHandlers exported and registered in the MSW barrel with the exact response contract
- Three frontend todo spec files exist; no test fails anywhere
- Wave 1 and Wave 2 plans now have concrete `<automated>` targets that resolve to real files
</success_criteria>

<output>
Create `.planning/phases/05-search/05-01-SUMMARY.md` when done
</output>
