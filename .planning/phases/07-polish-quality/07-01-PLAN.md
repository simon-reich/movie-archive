---
phase: 07-polish-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/AppNav.vue
  - frontend/pages/movies/[id].vue
  - frontend/pages/add.vue
  - frontend/components/MovieCard.vue
autonomous: true
requirements:
  - QLTY-01

must_haves:
  truths:
    - "On a Pixel 5 (393px wide), AppNav shows a hamburger icon instead of nav links"
    - "Tapping the hamburger opens a solid off-white drawer sliding in from the right"
    - "Tapping any drawer link or the X button closes the drawer"
    - "Desktop nav (md: and above) remains unchanged — no hamburger visible"
    - "Detail page two-column grid collapses to single column on mobile; sidebar stacks below main content"
    - "Hero poster and title remain readable at 393px wide"
    - "Cast & Crew multi-column does not produce columns narrower than readable"
    - "data-testid attributes exist on add-page poster cards, search result cards (MovieCard), and the detail page h1"
  artifacts:
    - path: "frontend/components/AppNav.vue"
      provides: "Mobile hamburger + slide-in drawer + desktop nav unchanged"
    - path: "frontend/pages/movies/[id].vue"
      provides: "Responsive detail page — mobile single-column reflow, hero flex-col on mobile"
    - path: "frontend/pages/add.vue"
      provides: "data-testid=poster-card on each poster div, data-testid=save-status on state overlays"
    - path: "frontend/components/MovieCard.vue"
      provides: "data-testid=movie-card on the root element"
  key_links:
    - from: "AppNav hamburger button"
      to: "drawerOpen ref"
      via: "@click toggle"
      pattern: "drawerOpen"
    - from: "detail page grid"
      to: "mobile single column"
      via: "grid-cols-1 md:grid-cols-3"
      pattern: "grid-cols-1 md:grid-cols-3"
---

<objective>
Make all core pages fully usable on a Pixel 5 (393×851px) viewport.

Purpose: QLTY-01 — the app must be responsive and fully usable on mobile devices.
Output: Updated AppNav (hamburger + drawer), responsive detail page, and data-testid attributes that the E2E Playwright spec will use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/UI-SPEC.md
@.planning/phases/07-polish-quality/07-CONTEXT.md
@.planning/phases/07-polish-quality/07-RESEARCH.md

<interfaces>
<!-- AppNav.vue current structure (executor reads file before editing) -->
<!-- Key: nav has a single flex row with logo left + links right. No mobile treatment. -->
<!-- Prop: overlay?: boolean — controls textClass (white vs foreground). -->
<!-- Imports currently: { Search, Settings, LogOut } from 'lucide-vue-next' -->

<!-- movies/[id].vue key layout lines (executor reads file before editing) -->
<!-- Line 133: hero div class="relative w-full h-72 overflow-hidden -mt-12" -->
<!-- Line 136: hero content flex items-end justify-between pb-6 -->
<!-- Line 138: inner div class="flex items-end gap-6" (poster + text block) -->
<!-- Line 144: poster img class="w-32 aspect-[2/3] ..." -->
<!-- Line 179: body grid class="max-w-7xl mx-auto px-4 py-8 grid grid-cols-3 gap-8" -->
<!-- Line 182: left col class="col-span-2 space-y-8" -->
<!-- Line 285: right col class="col-span-1 space-y-6" -->
<!-- Line 341: cast section class="columns-3 gap-8" -->

<!-- add.vue key layout (executor reads file before editing) -->
<!-- Each poster result is a div.relative.cursor-pointer.group at ~line 128 -->
<!-- State overlays (pending/success/error/saved) are absolute divs inside it -->

<!-- MovieCard.vue root element (executor reads file before editing) -->
<!-- Root div class="overflow-hidden" at line ~25 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppNav mobile hamburger and slide-in drawer (D-01, D-02, D-03)</name>
  <files>frontend/components/AppNav.vue</files>
  <read_first>
    - frontend/components/AppNav.vue (read current full content before editing)
    - .planning/UI-SPEC.md (editorial aesthetic: no rounded corners, terracotta accent #C84B31, bg-background = #FAF7F2, tracking-widest uppercase on nav links)
    - .planning/phases/07-polish-quality/07-CONTEXT.md (D-01, D-02, D-03 decisions)
  </read_first>
  <action>
Edit `frontend/components/AppNav.vue` to add mobile hamburger + slide-in drawer, leaving desktop nav unchanged.

**Imports to add** (extend the existing lucide import line):
```
import { Search, Settings, LogOut, Menu, X } from 'lucide-vue-next'
```

**Script additions** (add after existing `const userInitial` computed):
```typescript
const drawerOpen = ref(false)
```

**Template changes:**

1. Wrap the existing `<div class="flex items-center gap-4">` in a `<div class="hidden md:flex items-center gap-4">` — this hides all current nav links below `md` breakpoint (768px).

2. Add a hamburger button AFTER the hidden div (sibling, inside the outer `flex items-center justify-between` container):
```html
<!-- Mobile hamburger: visible only below md -->
<button
  class="md:hidden"
  :class="textClass"
  aria-label="Open navigation"
  @click="drawerOpen = true"
>
  <Menu class="w-5 h-5" />
</button>
```

3. Add the drawer AFTER the closing `</nav>` tag (sibling of nav, at root of template using a fragment or wrapping `<template>`):
```html
<!-- Slide-in drawer: only on mobile, solid off-white panel, no backdrop, no rounded corners -->
<Transition name="slide">
  <div
    v-if="drawerOpen"
    class="fixed top-0 right-0 h-full w-64 bg-background border-l border-border z-[60] flex flex-col pt-16 px-6 gap-6 md:hidden"
  >
    <!-- Close button -->
    <button
      class="absolute top-4 right-4 text-foreground"
      aria-label="Close navigation"
      @click="drawerOpen = false"
    >
      <X class="w-5 h-5" />
    </button>

    <!-- Nav links: uppercase tracking-widest, terracotta on active route -->
    <NuxtLink
      to="/add"
      class="text-sm font-medium tracking-widest uppercase text-foreground hover:text-primary"
      active-class="text-primary"
      @click="drawerOpen = false"
    >Add Film</NuxtLink>

    <NuxtLink
      to="/search"
      class="text-sm font-medium tracking-widest uppercase text-foreground hover:text-primary flex items-center gap-2"
      active-class="text-primary"
      @click="drawerOpen = false"
    >
      <Search class="w-4 h-4" />
      Search
    </NuxtLink>

    <NuxtLink
      to="/settings"
      class="text-sm font-medium tracking-widest uppercase text-foreground hover:text-primary flex items-center gap-2"
      active-class="text-primary"
      @click="drawerOpen = false"
    >
      <Settings class="w-4 h-4" />
      Settings
    </NuxtLink>

    <button
      :disabled="loading"
      class="text-sm font-medium tracking-widest uppercase text-foreground hover:text-primary flex items-center gap-2 disabled:opacity-50 disabled:cursor-not-allowed text-left"
      @click="handleLogout(); drawerOpen = false"
    >
      <LogOut class="w-4 h-4" />
      Sign out
    </button>
  </div>
</Transition>
```

4. Add slide transition styles using `<style scoped>` at the bottom:
```css
<style scoped>
.slide-enter-active,
.slide-leave-active {
  transition: transform 0.3s ease;
}
.slide-enter-from,
.slide-leave-to {
  transform: translateX(100%);
}
</style>
```

**Constraints per D-02:** NO backdrop overlay. NO `backdrop-blur`. NO rounded corners (`rounded-*` classes must not appear in the drawer). Solid `bg-background` (which maps to `#FAF7F2`). The `border-l border-border` provides a clear visual edge. Width `w-64` (256px) — leaves 129px of the 393px Pixel 5 viewport visible (editorial feel showing page behind).

**Per D-03:** The hamburger `md:hidden` class ensures it does not appear at md (768px) and above. The desktop nav div gets `hidden md:flex` to restore it at md and above.

The template must use Vue 3 `<template>` fragment to have both `<nav>` and `<Transition>` as siblings at the template root.
  </action>
  <verify>
    <automated>cd /Users/simonreich/git/private/movie-archive/frontend && grep -n "drawerOpen" components/AppNav.vue && grep -n "md:hidden" components/AppNav.vue && grep -n "hidden md:flex" components/AppNav.vue && grep -n "translate-x" components/AppNav.vue | head -5 || grep -n "slide-enter" components/AppNav.vue</automated>
  </verify>
  <acceptance_criteria>
    - `components/AppNav.vue` contains `drawerOpen` ref
    - `components/AppNav.vue` contains `md:hidden` class on hamburger button
    - `components/AppNav.vue` contains `hidden md:flex` on the desktop nav links wrapper
    - `components/AppNav.vue` contains `Menu` and `X` in the lucide import
    - `components/AppNav.vue` contains `bg-background` on the drawer div (not `bg-white` or `bg-card`)
    - `components/AppNav.vue` does NOT contain `backdrop` (no backdrop overlay per D-02)
    - `components/AppNav.vue` does NOT contain `rounded-` on any drawer element
    - `components/AppNav.vue` contains `slide-enter-active` in `<style scoped>`
  </acceptance_criteria>
  <done>AppNav shows hamburger on mobile, opens a solid off-white slide-in drawer with all nav links, desktop nav unchanged at md and above.</done>
</task>

<task type="auto">
  <name>Task 2: Detail page mobile reflow + data-testid attributes (D-04, D-05, D-06)</name>
  <files>frontend/pages/movies/[id].vue, frontend/pages/add.vue, frontend/components/MovieCard.vue</files>
  <read_first>
    - frontend/pages/movies/[id].vue (read full file before editing — especially lines 133–175 hero, line 179 body grid, line 285 sidebar col, line 341 cast columns)
    - frontend/pages/add.vue (read full file before editing — especially the poster grid div ~line 128)
    - frontend/components/MovieCard.vue (read full file before editing — root div ~line 25)
    - .planning/phases/07-polish-quality/07-RESEARCH.md (Pitfall 1 — hero height on mobile, Pitfall 6 — columns-3)
  </read_first>
  <action>
**Part A — movies/[id].vue mobile reflow:**

1. **Body grid (line 179):** Change:
   ```
   class="max-w-7xl mx-auto px-4 py-8 grid grid-cols-3 gap-8"
   ```
   To:
   ```
   class="max-w-7xl mx-auto px-4 py-8 grid grid-cols-1 md:grid-cols-3 gap-8"
   ```

2. **Left column (line 182):** Change:
   ```
   class="col-span-2 space-y-8"
   ```
   To:
   ```
   class="col-span-1 md:col-span-2 space-y-8"
   ```

3. **Right sidebar column (line 285):** Change:
   ```
   class="col-span-1 space-y-6"
   ```
   To (no change needed — `col-span-1` already works as full-width on mobile):
   ```
   class="col-span-1 space-y-6"
   ```
   (leave unchanged — col-span-1 in a single-column grid is full width)

4. **Cast/Crew multi-column (line 341):** Change:
   ```
   class="columns-3 gap-8"
   ```
   To:
   ```
   class="columns-1 md:columns-3 gap-8"
   ```

5. **Hero poster (line 144):** Change the poster `<img>` class from:
   ```
   class="w-32 aspect-[2/3] object-cover border border-white/20 flex-shrink-0"
   ```
   To:
   ```
   class="w-20 sm:w-32 aspect-[2/3] object-cover border border-white/20 flex-shrink-0"
   ```
   Also change the poster placeholder `<div>` class from:
   ```
   class="w-32 aspect-[2/3] bg-card flex-shrink-0 ..."
   ```
   To:
   ```
   class="w-20 sm:w-32 aspect-[2/3] bg-card flex-shrink-0 ..."
   ```

6. **Hero content row (line 138):** Change:
   ```
   class="flex items-end gap-6"
   ```
   To:
   ```
   class="flex items-end gap-3 sm:gap-6"
   ```
   This prevents the poster + title block from overflowing at 393px.

7. **Add data-testid to the detail page h1** (the movie title heading, around line 158):
   Change:
   ```html
   <h1 class="text-2xl font-semibold tracking-wide text-white">{{ movie.title }}</h1>
   ```
   To:
   ```html
   <h1 data-testid="movie-title" class="text-2xl font-semibold tracking-wide text-white">{{ movie.title }}</h1>
   ```

**Part B — add.vue data-testid attributes:**

Each poster card in the results grid is a `<div ... @click="handlePosterClick(item)">`. Add `data-testid="poster-card"` to that div:
```html
<div
  v-for="item in results"
  :key="item.tmdbId"
  data-testid="poster-card"
  class="relative cursor-pointer group overflow-hidden"
  @click="handlePosterClick(item)"
>
```

The save status indicator overlays already have conditional rendering. Add `data-testid="save-status"` to the pending overlay (the first state overlay, approximately line 143 in add.vue):
```html
<div
  v-if="item.state === 'pending'"
  data-testid="save-status"
  class="absolute inset-0 bg-background/70 flex items-center justify-center"
>
```

**Part C — MovieCard.vue data-testid:**

Add `data-testid="movie-card"` to the root `<div class="overflow-hidden">`:
```html
<div data-testid="movie-card" class="overflow-hidden">
```
  </action>
  <verify>
    <automated>cd /Users/simonreich/git/private/movie-archive/frontend && grep -n "grid-cols-1 md:grid-cols-3" pages/movies/\[id\].vue && grep -n "columns-1 md:columns-3" pages/movies/\[id\].vue && grep -n "data-testid=\"movie-title\"" pages/movies/\[id\].vue && grep -n "data-testid=\"poster-card\"" pages/add.vue && grep -n "data-testid=\"movie-card\"" components/MovieCard.vue</automated>
  </verify>
  <acceptance_criteria>
    - `pages/movies/[id].vue` contains `grid-cols-1 md:grid-cols-3` on the body grid wrapper
    - `pages/movies/[id].vue` contains `col-span-1 md:col-span-2` on the left content column
    - `pages/movies/[id].vue` contains `columns-1 md:columns-3` on the cast/crew section
    - `pages/movies/[id].vue` contains `w-20 sm:w-32` on the hero poster img
    - `pages/movies/[id].vue` contains `data-testid="movie-title"` on the h1
    - `pages/add.vue` contains `data-testid="poster-card"` on the poster result div
    - `pages/add.vue` contains `data-testid="save-status"` on the pending state overlay
    - `components/MovieCard.vue` contains `data-testid="movie-card"` on the root div
  </acceptance_criteria>
  <done>Detail page reflows to single column on mobile. Hero poster readable at 393px. Cast/crew columns readable. data-testid attributes in place for E2E tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI | No server-side changes in this plan — purely frontend UI/layout changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01-01 | Information Disclosure | Mobile drawer (AppNav.vue) | accept | The drawer exposes the same links as the desktop nav — no new information is exposed. All routes behind these links already require JWT auth. |
| T-7-01-02 | Tampering | data-testid attributes | accept | data-testid attributes are read-only HTML attributes used for test selection. They expose no functionality, secrets, or attack surface beyond what is already visible in the DOM. |
</threat_model>

<verification>
After both tasks:

```bash
cd /Users/simonreich/git/private/movie-archive/frontend
# Verify no TypeScript errors in changed files
pnpm exec vue-tsc --noEmit 2>&1 | grep -E "AppNav|movies/\[id\]|add\.vue|MovieCard" | head -10 || echo "No TypeScript errors in changed files"
```

Manual spot-check (optional): Open browser at 393px width, navigate to /add → AppNav shows hamburger, tapping opens drawer. Navigate to /movies/[any-id] → layout is single column.
</verification>

<success_criteria>
- `frontend/components/AppNav.vue` has hamburger on mobile (md:hidden), desktop nav unchanged (hidden md:flex), solid white drawer with slide transition, no backdrop overlay, no rounded corners, close button works
- `frontend/pages/movies/[id].vue` body uses `grid-cols-1 md:grid-cols-3`; hero poster `w-20 sm:w-32`; cast uses `columns-1 md:columns-3`; h1 has `data-testid="movie-title"`
- `frontend/pages/add.vue` poster cards have `data-testid="poster-card"`, pending overlay has `data-testid="save-status"`
- `frontend/components/MovieCard.vue` root div has `data-testid="movie-card"`
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-quality/07-01-SUMMARY.md`
</output>
