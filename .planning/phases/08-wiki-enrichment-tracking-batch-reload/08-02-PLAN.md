---
phase: 08-wiki-enrichment-tracking-batch-reload
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/src/main/java/de/moviearchive/movie/MovieRepository.java
  - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java
  - backend/src/main/java/de/moviearchive/config/AsyncConfig.java
  - backend/src/main/resources/application.properties
  - backend/src/test/resources/application-test.properties
  - backend/src/main/java/de/moviearchive/admin/WikiReloadController.java
  - backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java
  - backend/src/test/java/de/moviearchive/movie/WikiReloadServiceIntegrationTest.java
autonomous: true
requirements: [ENRICH-02, ENRICH-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`MovieRepository.findEligibleForWikiReload` returns only movies for the given user with `wikiUrl IS NULL` AND `status = SUCCESS` AND (`wikiLastAttemptedAt IS NULL` OR `wikiLastAttemptedAt < cutoff`); a film last attempted exactly at the cutoff instant is excluded (not `< cutoff`), and becomes eligible only strictly after that instant (ENRICH-02 boundary contract)."
    - "`WikiReloadService.batchReload()` runs on the dedicated `wikiReloadExecutor` bean (core=1/max=1/queue=1) via `@Async`, inserting `Thread.sleep(pacingDelayMs)` between consecutive Wikipedia calls but never after the last eligible film, and performs zero sleeps when 0 or 1 films are eligible (ENRICH-03 boundary contract)."
    - "A second `POST /admin/wiki-reload/{userId}` trigger while a run is already in-flight is queued (via `wikiReloadExecutor`'s `queueCapacity=1`) and processed after the first completes; a third concurrent trigger while one run is active and one is queued is rejected with a 503, not an unhandled 500 (ENRICH-02 concurrency contract)."
    - "`wiki.retry.cooldown-days` and `wiki.retry.pacing-delay-ms` are configurable via `application.properties` with ENV overrides (`WIKI_RETRY_COOLDOWN_DAYS`/`WIKI_RETRY_PACING_DELAY_MS`), defaulting to 30 and 1000 respectively (D-04/D-08)."
    - "A single per-movie failure inside the batch loop never aborts processing of the remaining eligible movies in that run (regression check — already established by Plan 08-01, must survive the async/pacing conversion)."
  artifacts:
    - backend/src/main/java/de/moviearchive/movie/MovieRepository.java
    - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java
    - backend/src/main/java/de/moviearchive/config/AsyncConfig.java
    - backend/src/main/resources/application.properties
    - backend/src/test/resources/application-test.properties
    - backend/src/main/java/de/moviearchive/admin/WikiReloadController.java
    - backend/src/test/java/de/moviearchive/movie/WikiReloadServiceIntegrationTest.java
  key_links:
    - "AsyncConfig.wikiReloadExecutor bean -> @Async(\"wikiReloadExecutor\") on WikiReloadService.batchReload"
    - "WikiReloadController.triggerReload -> catch TaskRejectedException -> 503"
    - "WikiReloadService.batchReload -> MovieRepository.findEligibleForWikiReload(userId, cutoff) where cutoff = Instant.now().minus(cooldownDays, ChronoUnit.DAYS)"
  prohibitions:
    - "WikiReloadService.batchReload() must never be annotated `@Transactional` after the async/pacing conversion — a long-lived transaction held across `Thread.sleep` calls for the full batch duration risks connection-pool exhaustion; only the per-movie `retryWikipedia()` method may be `@Transactional`."
    - "WikiReloadService.retryWikipedia() and batchReload() must never be annotated `@Retryable` — WikipediaClient.fetch() already exhausts a 10-candidate fallback internally; wrapping the caller in Spring-managed retry would multiply Wikipedia request volume during exactly the rate-limiting scenario this phase exists to prevent."
    - "The `wikiReloadExecutor` bean must never be sized above core=1/max=1 — a larger pool would allow two batch runs to pace Wikipedia calls simultaneously from different threads, defeating the point of sequential pacing (D-07)."
---

<objective>
Complete the batch-reload feature from Plan 08-01's synchronous tracer into its final, safe form: cooldown-window eligibility filtering (D-03/D-04), fire-and-forget `@Async` execution on a dedicated bounded executor (D-05), a paced delay between Wikipedia calls (D-07/D-08), and a bounded-queue concurrency contract that degrades to 503 instead of an unhandled failure when overlapping triggers exceed capacity.

Purpose: Deliver ENRICH-02's cooldown filtering and ENRICH-03's pacing — the two knobs that directly address the 630-film rate-limiting incident this milestone exists to fix.
Output: Cooldown-filtered eligibility query, `wikiReloadExecutor` bean, paced async `batchReload`, two new config properties, and timing-based integration tests proving both the cooldown boundary and the pacing behavior.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-wiki-enrichment-tracking-batch-reload/08-CONTEXT.md
@.planning/phases/08-wiki-enrichment-tracking-batch-reload/08-RESEARCH.md
@.planning/phases/08-wiki-enrichment-tracking-batch-reload/08-PATTERNS.md
@.planning/phases/08-wiki-enrichment-tracking-batch-reload/08-01-SUMMARY.md
@backend/src/main/java/de/moviearchive/config/AsyncConfig.java
</context>

<artifacts_produced>
## Artifacts this plan produces

- `MovieRepository.findEligibleForWikiReload(UUID userId, Instant cutoff)` — replaces Plan 08-01's tracer query with the final cooldown + status-filtered version
- `wiki.retry.cooldown-days` / `wiki.retry.pacing-delay-ms` — new `application.properties` config keys (ENV: `WIKI_RETRY_COOLDOWN_DAYS` / `WIKI_RETRY_PACING_DELAY_MS`)
- `AsyncConfig.wikiReloadExecutor` — new dedicated `ThreadPoolTaskExecutor` bean (core=1/max=1/queue=1)
- `WikiReloadService.batchReload` (modified) — now `@Async("wikiReloadExecutor")`, cooldown-aware, paced
- `WikiReloadController` (modified) — 202 Accepted response, new `TaskRejectedException` -> 503 handler
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Cooldown-window eligibility query + async execution + pacing delay</name>
  <files>backend/src/main/java/de/moviearchive/movie/MovieRepository.java, backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java, backend/src/main/java/de/moviearchive/config/AsyncConfig.java, backend/src/main/resources/application.properties, backend/src/test/resources/application-test.properties, backend/src/main/java/de/moviearchive/admin/WikiReloadController.java, backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/movie/MovieRepository.java (current state, after Plan 08-01's `findByUserIdAndWikiUrlIsNull` addition — this is the exact method being replaced)
    - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java (current state from Plan 08-01)
    - backend/src/main/java/de/moviearchive/admin/WikiReloadController.java (current state from Plan 08-01)
    - backend/src/main/java/de/moviearchive/config/AsyncConfig.java (current — `enrichmentExecutor` bean, the style to mirror)
    - backend/src/main/resources/application.properties (current — ENV-override convention, `tmdb.base-url`/`omdb.base-url`/`wikipedia.base-url` block)
    - backend/src/test/resources/application-test.properties (current)
    - backend/src/main/java/de/moviearchive/movie/MovieStatus.java (`PENDING`/`SUCCESS`/`ERROR`)
    - backend/src/test/java/de/moviearchive/movie/EnrichmentIntegrationTest.java lines 90-100 (`pollForCompletion` pattern to mirror when updating the existing controller test)
    - .planning/phases/08-wiki-enrichment-tracking-batch-reload/08-RESEARCH.md Pitfall 2 (dedicated executor rationale), Pitfall 4 (`@Transactional` placement), Pitfall 5 (fast-test pacing override), Open Question 1 (status=SUCCESS filter recommendation)
  </read_first>
  <action>
    1. In `MovieRepository.java`, replace `findByUserIdAndWikiUrlIsNull` with the final `List<Movie> findEligibleForWikiReload(@Param("userId") UUID userId, @Param("cutoff") Instant cutoff)`, JPQL: `SELECT m FROM Movie m WHERE m.user.id = :userId AND m.wikiUrl IS NULL AND m.status = de.moviearchive.movie.MovieStatus.SUCCESS AND (m.wikiLastAttemptedAt IS NULL OR m.wikiLastAttemptedAt < :cutoff)`. The `status = SUCCESS` clause is per RESEARCH.md Open Question 1's recommendation — `ERROR`-status movies never reached the Wikipedia step and have no reliable title/year to look up, so including them would waste a paced Wikipedia call on incomplete data.
    2. In `WikiReloadService.java`: add `@Value("${wiki.retry.cooldown-days:30}") private long cooldownDays;` and `@Value("${wiki.retry.pacing-delay-ms:1000}") private long pacingDelayMs;` (imports: `org.springframework.beans.factory.annotation.Value`, `java.time.temporal.ChronoUnit`). Change `batchReload` to `@Async("wikiReloadExecutor")` (this is now the D-05 fire-and-forget path). Inside `batchReload`, compute `Instant cutoff = Instant.now().minus(cooldownDays, ChronoUnit.DAYS);` and replace the Plan 08-01 call with `movieRepository.findEligibleForWikiReload(userId, cutoff)`. Add pacing: after each `retryWikipedia` call except the last element (`if (i < eligible.size() - 1)`), call `Thread.sleep(pacingDelayMs)` inside its own try/catch for `InterruptedException` (`Thread.currentThread().interrupt(); log.warn("Wiki batch-reload interrupted for userId={{}} at index={{}}", userId, i); return;`). Confirm `retryWikipedia` stays `@Transactional` and `batchReload` stays free of `@Transactional` (Pitfall 4).
    3. In `AsyncConfig.java`, add a sibling bean `wikiReloadExecutor` — same `ThreadPoolTaskExecutor` style as the existing `enrichmentExecutor` bean but sized `core=1`, `max=1`, `queueCapacity=1`, thread name prefix `"wiki-reload-"`. Do not modify `enrichmentExecutor`.
    4. In `application.properties`, add two lines directly under the existing `wikipedia.base-url` line: `wiki.retry.cooldown-days=${WIKI_RETRY_COOLDOWN_DAYS:30}` and `wiki.retry.pacing-delay-ms=${WIKI_RETRY_PACING_DELAY_MS:1000}`.
    5. In `application-test.properties`, add `wiki.retry.pacing-delay-ms=1` — a fast global default for the whole test suite (RESEARCH.md Pitfall 5). Individual tests that need to observe real pacing timing override this per-class via `@DynamicPropertySource` (Task 2).
    6. In `WikiReloadController.java`: since `batchReload` is now truly fire-and-forget, change the response to `ResponseEntity.accepted().body(Map.of("status", "started"))` (202, matching D-05's "returns immediately"). Add `@ExceptionHandler(org.springframework.core.task.TaskRejectedException.class) public ResponseEntity<Map<String,String>> handleTaskRejected(org.springframework.core.task.TaskRejectedException ex)` returning `ResponseEntity.status(503).body(Map.of("message", "A wiki-reload is already in progress for this user; try again shortly."))` — this fires when a third overlapping trigger exceeds `wikiReloadExecutor`'s bounded queue (1 running + 1 queued is the max in flight; a third submission is rejected synchronously by `ThreadPoolTaskExecutor`).
    7. In `WikiReloadControllerTest.java`, update `shouldRetryWikipediaAndReindex_forMovieMissingWikiData` (written synchronously in Plan 08-01) to poll for completion instead of asserting immediately — mirror `EnrichmentIntegrationTest`'s `pollForCompletion` helper (poll `movieRepository.findById` until `wikiLastAttemptedAt` is non-null or a timeout elapses, e.g. 5000ms), since the endpoint now returns before the batch finishes. Change the HTTP status assertion from `isOk()` to `isAccepted()`.
  </action>
  <verify>
    <automated>./gradlew test --tests "de.moviearchive.admin.WikiReloadControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `findEligibleForWikiReload`'s JPQL contains `m.status = de.moviearchive.movie.MovieStatus.SUCCESS` and the `wikiLastAttemptedAt < :cutoff` comparison.
    - `WikiReloadService.batchReload` is annotated `@Async("wikiReloadExecutor")` and is NOT `@Transactional`.
    - `AsyncConfig` has a `wikiReloadExecutor` bean with `core=1/max=1/queue=1`.
    - `application.properties` has both new `wiki.retry.*` keys with the documented defaults and ENV var names; `application-test.properties` sets `wiki.retry.pacing-delay-ms=1`.
    - `WikiReloadController.triggerReload` returns 202 Accepted; a `TaskRejectedException` handler returning 503 exists.
    - `WikiReloadControllerTest` (updated to poll, expecting 202) passes.
  </acceptance_criteria>
  <done>Batch-reload is fire-and-forget, paced 1s (configurable) between Wikipedia calls, filters eligible films by a 30-day (configurable) cooldown window plus SUCCESS status, and a saturated executor queue degrades to a 503 instead of an unhandled 500.</done>
  <reversibility rating="reversible">D-05 already documents async-vs-sync as an internal implementation detail with no published contract depending on it; both new config properties are ENV-overridable without a redeploy.</reversibility>
</task>

<task type="auto">
  <name>Task 2: WikiReloadServiceIntegrationTest — cooldown boundary + pacing verification</name>
  <files>backend/src/test/java/de/moviearchive/movie/WikiReloadServiceIntegrationTest.java</files>
  <read_first>
    - backend/src/test/java/de/moviearchive/movie/EnrichmentIntegrationTest.java (full file — WireMock + Testcontainers conventions, `pollForCompletion`)
    - backend/src/test/java/de/moviearchive/admin/ReindexControllerTest.java `persistMovie` helper (lines 84-109)
    - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java (final form from Task 1)
    - backend/src/main/java/de/moviearchive/movie/MovieRepository.java (final form from Task 1)
    - backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java (Plan 08-01's WireMock+OpenSearch composition pattern to reuse — locally-declared `WireMockExtension`, not inherited)
  </read_first>
  <action>
    Create `WikiReloadServiceIntegrationTest.java` in package `de.moviearchive.movie`, extending `AbstractOpenSearchTest` with a locally-declared `WireMockExtension` (same composition pattern as `WikiReloadControllerTest`). `@Autowired` `WikiReloadService`, `MovieRepository`, `UserRepository`. Write two tests: (a) `shouldRespectCooldownWindow_excludingRecentAttempts` — persist 3 movies for one user, all `wikiUrl=null`/`status=SUCCESS`: one with `wikiLastAttemptedAt=null` (never attempted), one with `wikiLastAttemptedAt=Instant.now().minus(29, ChronoUnit.DAYS)` (inside the 30-day cooldown — must stay untouched), one with `wikiLastAttemptedAt=Instant.now().minus(31, ChronoUnit.DAYS)` (outside cooldown — must be retried). Stub WireMock to return a found Wikipedia page for all requests. Call `wikiReloadService.batchReload(userId)` directly (invoking the `@Async` method through the injected Spring proxy is safe and runs on `wikiReloadExecutor`). Poll (mirroring `EnrichmentIntegrationTest.pollForCompletion`) until the two eligible movies have a fresh `wikiUrl`, then assert: the never-attempted and 31-day movies now have `wikiUrl` set, while the 29-day movie's `wikiLastAttemptedAt` is UNCHANGED from its persisted value (it was skipped). (b) `shouldPaceRequestsBetweenEligibleMovies` — override `wiki.retry.pacing-delay-ms=500` via `@DynamicPropertySource` for this test class (the global `1`ms test default from Task 1 is too fast to observe timing). Persist 3 eligible movies, record `Instant start`, call `batchReload`, poll for completion, then assert the elapsed wall-clock time is `>= 1000ms` (2 sleeps x 500ms for 3 movies) — proving pacing occurred. Separately, in the same test or a second method, persist exactly 1 eligible movie only, call `batchReload`, poll for completion, and assert elapsed time is well under 500ms — covering the "zero sleeps when <= 1 eligible film" boundary.
  </action>
  <verify>
    <automated>./gradlew test --tests "de.moviearchive.movie.WikiReloadServiceIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - Cooldown-boundary test proves the 29-day movie is untouched while the never-attempted and 31-day movies are retried.
    - Pacing test proves a 3-movie batch takes `>= 1000ms` (with 500ms pacing) while a 1-movie batch completes in well under 500ms.
  </acceptance_criteria>
  <done>ENRICH-02's cooldown-window eligibility contract and ENRICH-03's pacing contract both have real, timing-based integration test coverage — not just unit-mocked assumptions.</done>
  <reversibility rating="reversible">Test-only file.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Concurrency test — queued second trigger, rejected third trigger</name>
  <files>backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/admin/WikiReloadController.java (final form from Task 1, including the `TaskRejectedException` handler)
    - backend/src/main/java/de/moviearchive/config/AsyncConfig.java (final form from Task 1 — `wikiReloadExecutor` sizing)
    - backend/src/test/java/de/moviearchive/admin/WikiReloadControllerTest.java (current state, after Task 1's poll-based update)
  </read_first>
  <action>
    Add a `@DynamicPropertySource` override on this test class setting `wiki.retry.pacing-delay-ms=2000` so a multi-movie batch stays "in flight" long enough to deterministically observe queuing (document in a code comment why this is set well above the global 1ms test default). Add test `shouldQueueSecondTrigger_whileFirstRunInProgress`: persist >= 2 eligible movies for one user, `POST /admin/wiki-reload/{userId}` once (expect 202), then immediately `POST` again for the SAME user before the first run completes — assert the second call ALSO returns 202 (queued, not rejected — `wikiReloadExecutor`'s `queueCapacity=1` holds it). Add test `shouldReject_whenThirdTriggerExceedsQueueCapacity`: with the first run and the queued second run both still pending (same setup), issue a THIRD `POST` immediately — assert `503` with a `message` field present in the JSON body. Use the generous 2000ms pacing margin to avoid flakiness.
  </action>
  <verify>
    <automated>./gradlew test --tests "de.moviearchive.admin.WikiReloadControllerTest"</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests pass; the second trigger returns 202, the third returns 503 with a non-empty `message` field.
    - No flakiness across repeated local runs (generous timing margin documented in a code comment).
  </acceptance_criteria>
  <done>The wikiReloadExecutor's bounded queue behavior (1 running + 1 queued, reject beyond that) is verified end-to-end through the real HTTP endpoint, closing the ENRICH-02 concurrency contract.</done>
  <reversibility rating="reversible">Test-only changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser/curl, JWT-authenticated) -> `WikiReloadController` | untrusted request crosses here; the path `userId` is attacker-controllable |
| Repeated/overlapping requests -> `wikiReloadExecutor` | resource-exhaustion surface — a bounded queue is the mitigation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | `WikiReloadController.triggerReload` | high | mitigate | Unchanged from Plan 08-01 — `assertOwnership()` 403 check remains in force; no changes to ownership logic in this plan |
| T-08-02 | Denial of Service | `wikiReloadExecutor` / repeated batch-reload triggers | medium | mitigate | Dedicated `core=1/max=1/queue=1` executor bounds in-flight runs to 2 (1 running + 1 queued); a third overlapping trigger is rejected with `TaskRejectedException` -> 503, not an unbounded queue or crash; tested in `WikiReloadControllerTest.shouldReject_whenThirdTriggerExceedsQueueCapacity` (Task 3) |
| T-08-03 | Tampering (SSRF) | `WikipediaClient` (unchanged) | low | accept | `wikipedia.base-url` remains a fixed `application.properties` value, not user-controllable per request |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package-manager dependencies added this phase |
</threat_model>

<verification>
- `./gradlew test --tests "de.moviearchive.admin.WikiReloadControllerTest"` — updated poll-based success test + new concurrency tests
- `./gradlew test --tests "de.moviearchive.movie.WikiReloadServiceIntegrationTest"` — cooldown boundary + pacing timing
- `./gradlew test` — full suite, before `/gsd-verify-work`
</verification>

<success_criteria>
- `findEligibleForWikiReload` correctly excludes recently-attempted films and includes never-attempted/stale-attempted films (cooldown boundary proven by test, not just code review).
- `batchReload` runs async on a dedicated executor, paces Wikipedia calls, and never sleeps after the last item or when 0-1 films are eligible.
- Overlapping triggers degrade gracefully: queued (2nd) then rejected-with-503 (3rd+), never an unhandled 500.
- All new and modified tests are green; no regressions in Plan 08-01's tests.
</success_criteria>

<output>
Create `.planning/phases/08-wiki-enrichment-tracking-batch-reload/08-02-SUMMARY.md` when done
</output>
