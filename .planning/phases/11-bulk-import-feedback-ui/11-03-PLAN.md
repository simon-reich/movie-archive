---
phase: 11-bulk-import-feedback-ui
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - backend/src/main/java/de/moviearchive/bulkimport/BulkImportLineRepository.java
  - backend/src/main/java/de/moviearchive/bulkimport/BulkImportBatchRepository.java
  - backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchSummary.java
  - backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchDetail.java
  - backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportLineResult.java
  - backend/src/main/java/de/moviearchive/bulkimport/BulkImportController.java
  - backend/src/test/java/de/moviearchive/bulkimport/BulkImportControllerTest.java
autonomous: true
requirements: [IMPORT-06]

estimate:
  tokens: 8500
  raw_tokens: 8500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A user can list their own past bulk-import batches with date, line count, and a per-status breakdown"
    - "A user can open one batch and see every line's title, poster (if any), and status"
    - "A user cannot read another user's batch list entries or batch detail (IDOR protection)"
    - "Rows with no TMDB match render without a poster field being fabricated (null, not a broken URL)"
  artifacts:
    - path: backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchDetail.java
      provides: "record BulkImportBatchDetail(UUID batchId, Instant createdAt, int totalLines, List<BulkImportLineResult> lines)"
      contains: "record BulkImportBatchDetail"
    - path: backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchSummary.java
      provides: "record BulkImportBatchSummary(UUID batchId, Instant createdAt, int totalLines, Map<String, Long> statusCounts)"
      contains: "record BulkImportBatchSummary"
    - path: backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportLineResult.java
      provides: "record BulkImportLineResult(String title, String originalTitle, Integer year, String status, String posterPath)"
      contains: "record BulkImportLineResult"
  key_links:
    - from: backend/src/main/java/de/moviearchive/bulkimport/BulkImportController.java
      to: backend/src/main/java/de/moviearchive/bulkimport/BulkImportBatchRepository.java
      via: "GET /movies/bulk-import/batches calls bulkImportBatchRepository.findByUserIdOrderByCreatedAtDesc(userId)"
      pattern: "findByUserIdOrderByCreatedAtDesc"
    - from: backend/src/main/java/de/moviearchive/bulkimport/BulkImportController.java
      to: backend/src/main/java/de/moviearchive/bulkimport/BulkImportLineRepository.java
      via: "GET /movies/bulk-import/batches/{batchId} calls bulkImportLineRepository.findByBatchIdOrderByTitle(batchId), mapped to BulkImportLineResult"
      pattern: "findByBatchIdOrderByTitle"
---

<objective>
Expose the persisted batch/line data built in Plan 11-01 as two read-only GET endpoints
(D-03/D-05/D-06): a batch list (date, line count, status distribution) and a batch detail
(per-line title/poster/status) — the actual "Ergebnisübersicht" IMPORT-06 requires, backed by
Postgres, never a downloadable file (explicit non-goal per D-06).

Purpose: D-05 locks this phase's results view as read-only for now (no inline "pick a
candidate" action) — these two endpoints are exactly the read surface that boundary requires,
nothing more. D-03's batch-list page (Plan 11-05) and D-01's live-progress page (Plan 11-04)
both consume the detail endpoint this plan adds.
Output: `BulkImportBatchSummary`/`BulkImportBatchDetail`/`BulkImportLineResult` DTOs, new
repository queries, `GET /movies/bulk-import/batches` and `GET /movies/bulk-import/
batches/{batchId}`, both scoped to the authenticated user (the second one ownership-checked
via Plan 11-02's `loadOwnedBatch()`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-bulk-import-feedback-ui/11-CONTEXT.md
@.planning/phases/11-bulk-import-feedback-ui/11-RESEARCH.md
@.planning/phases/11-bulk-import-feedback-ui/11-PATTERNS.md
@.planning/phases/11-bulk-import-feedback-ui/11-01-SUMMARY.md
@.planning/phases/11-bulk-import-feedback-ui/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository queries + response DTOs</name>
  <read_first>
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportLineRepository.java (as
    modified by Plan 11-01)
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportBatchRepository.java (as
    created by Plan 11-01)
    backend/src/main/java/de/moviearchive/movie/dto/TmdbSearchResultItem.java (record DTO
    convention this package follows)
    backend/src/main/java/de/moviearchive/movie/MovieRepository.java (the `findByUserId...`
    derived-query naming convention already used against a `@ManyToOne user` field)
  </read_first>
  <files>
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportLineRepository.java,
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportBatchRepository.java,
    backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchSummary.java,
    backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportBatchDetail.java,
    backend/src/main/java/de/moviearchive/bulkimport/dto/BulkImportLineResult.java
  </files>
  <action>
    Modify `BulkImportBatchRepository.java`: add a derived query method
    `List&lt;BulkImportBatch&gt; findByUserIdOrderByCreatedAtDesc(UUID userId);` (same
    nested-property derived-query style already proven in this codebase by
    `MovieRepository.findByUserIdAndWikiUrlIsNull`).

    Modify `BulkImportLineRepository.java`: add `List&lt;BulkImportLine&gt;
    findByBatchIdOrderByTitle(UUID batchId);` (derived query — `batch.id` resolved via nested
    property traversal, same pattern) and a status-count query `@Query("SELECT b.status,
    COUNT(b) FROM BulkImportLine b WHERE b.batch.id = :batchId GROUP BY b.status")
    List&lt;Object[]&gt; countByBatchIdGroupByStatus(@Param("batchId") UUID batchId);` — plain
    `Object[]` rows (element 0 = `BulkImportLineStatus`, element 1 = `Long`), converted to a
    `Map&lt;String, Long&gt;` in the controller (Task 2) — this codebase has no existing
    interface-projection convention, so don't introduce one here.

    Create the three new DTO records in `backend/src/main/java/de/moviearchive/bulkimport/dto/`,
    matching this package's established `record`-not-Lombok-class convention for read-only
    response DTOs:
    `BulkImportLineResult(String title, String originalTitle, Integer year, String status,
    String posterPath)`;
    `BulkImportBatchDetail(UUID batchId, Instant createdAt, int totalLines,
    List&lt;BulkImportLineResult&gt; lines)`;
    `BulkImportBatchSummary(UUID batchId, Instant createdAt, int totalLines,
    Map&lt;String, Long&gt; statusCounts)`.
  </action>
  <verify>
    <automated>cd backend && DOCKER_HOST=unix://$HOME/.orbstack/run/docker.sock ./gradlew compileJava --no-daemon</automated>
  </verify>
  <done>
    All three DTO records and both new repository query methods compile. `./gradlew
    compileJava` succeeds with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Batch list + detail GET endpoints (ownership-checked)</name>
  <read_first>
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportController.java (as modified by
    Plan 11-02 — the `loadOwnedBatch()` helper and existing exception handlers)
    backend/src/test/java/de/moviearchive/bulkimport/BulkImportControllerTest.java
  </read_first>
  <files>
    backend/src/main/java/de/moviearchive/bulkimport/BulkImportController.java,
    backend/src/test/java/de/moviearchive/bulkimport/BulkImportControllerTest.java
  </files>
  <action>
    Modify `BulkImportController.java`: add `@GetMapping("/bulk-import/batches") public
    ResponseEntity&lt;List&lt;BulkImportBatchSummary&gt;&gt; getBatches(Authentication auth)`
    — resolves the authenticated user's id via `userRepository.findByEmail(auth.getName())`
    (never a client-supplied userId, no path variable on this endpoint), calls
    `bulkImportBatchRepository.findByUserIdOrderByCreatedAtDesc(userId)`, and maps each
    `BulkImportBatch` to a `BulkImportBatchSummary` via a private `statusCounts(UUID
    batchId)` helper that converts `bulkImportLineRepository.
    countByBatchIdGroupByStatus(batchId)`'s `List&lt;Object[]&gt;` rows into a `Map&lt;String,
    Long&gt;` (cast row[0] to `BulkImportLineStatus` and call `.name()`, cast row[1] to
    `Long`). No pagination (RESEARCH.md Open Question 2 — single-user-first scope, plain
    `List` ordered by `created_at DESC`).

    Add `@GetMapping("/bulk-import/batches/{batchId}") public
    ResponseEntity&lt;BulkImportBatchDetail&gt; getBatchDetail(@PathVariable UUID batchId,
    Authentication auth)` — calls the existing `loadOwnedBatch(auth, batchId)` helper from
    Plan 11-02 (403 on mismatch, 404 on missing, reused unchanged), then
    `bulkImportLineRepository.findByBatchIdOrderByTitle(batchId)`, maps each `BulkImportLine`
    to a `BulkImportLineResult(line.getTitle(), line.getOriginalTitle(), line.getYear(),
    line.getStatus().name(), line.getPosterPath())`, and returns a `BulkImportBatchDetail`
    built from the batch's `id`/`createdAt`/`totalLines` plus that line list.

    In `BulkImportControllerTest.java`, add tests: (1) after two uploads for the same user,
    `GET /movies/bulk-import/batches` returns both batches ordered newest-first, each with a
    `statusCounts` map whose values sum to the number of persisted lines for that batch; (2)
    `GET /movies/bulk-import/batches/{batchId}` for the owner returns 200 with `lines`
    containing the expected `title`/`status`/`posterPath` (non-null for the SAVED line,
    null for an AMBIGUOUS/NOT_FOUND line — reuse the existing ambiguous-match fixture); (3)
    the same detail request authenticated as a second user returns 403 (mirrors Task 2 of
    Plan 11-02's ownership test); (4) a random nonexistent `batchId` returns 404.
  </action>
  <verify>
    <automated>cd backend && DOCKER_HOST=unix://$HOME/.orbstack/run/docker.sock ./gradlew test --tests "de.moviearchive.bulkimport.BulkImportControllerTest" --no-daemon</automated>
  </verify>
  <done>
    `GET /movies/bulk-import/batches` returns only the authenticated user's batches,
    newest-first, with correct per-status counts. `GET /movies/bulk-import/batches/{batchId}`
    returns the owner's per-line title/originalTitle/year/status/posterPath, 403s for a
    different user, 404s for a nonexistent id.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> GET /movies/bulk-import/batches | No path variable — scoped exclusively by the JWT-resolved user id, never a client-supplied filter |
| Client -> GET /movies/bulk-import/batches/{batchId} | `batchId` is a client-supplied path variable, same IDOR shape as Plan 11-02's progress endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Elevation of Privilege | BulkImportController.getBatchDetail() | high | mitigate | Reuses Plan 11-02's `loadOwnedBatch()` unchanged — 403 on a `batchId` owned by another user, verified by an explicit second-user test (Task 2). |
| T-11-07 | Information Disclosure | BulkImportController.getBatches() | high | mitigate | The batch list query is always scoped by `findByUserIdOrderByCreatedAtDesc(userId)` where `userId` comes exclusively from `userRepository.findByEmail(auth.getName())` — there is no `@RequestParam`/`@PathVariable` userId on this endpoint a client could override. |
</threat_model>

<verification>
Run `cd backend && DOCKER_HOST=unix://$HOME/.orbstack/run/docker.sock ./gradlew test --tests "de.moviearchive.bulkimport.*" --no-daemon` — all tests pass, including the four new list/detail/ownership tests.
</verification>

<success_criteria>
- `GET /movies/bulk-import/batches` returns the authenticated user's batches only, newest-first, with per-status counts summing to that batch's persisted line count.
- `GET /movies/bulk-import/batches/{batchId}` returns full per-line title/poster/status for the owner, 403 for a different user, 404 for a missing batch.
- No inline mutation endpoints added (D-05 boundary respected — this plan is read-only).
</success_criteria>

## Artifacts This Plan Produces

- `de.moviearchive.bulkimport.dto.BulkImportBatchSummary` — new record
- `de.moviearchive.bulkimport.dto.BulkImportBatchDetail` — new record
- `de.moviearchive.bulkimport.dto.BulkImportLineResult` — new record
- `BulkImportBatchRepository.findByUserIdOrderByCreatedAtDesc(UUID): List<BulkImportBatch>` — new method
- `BulkImportLineRepository.findByBatchIdOrderByTitle(UUID): List<BulkImportLine>` — new method
- `BulkImportLineRepository.countByBatchIdGroupByStatus(UUID): List<Object[]>` — new method
- `GET /movies/bulk-import/batches` — new endpoint, returns `List<BulkImportBatchSummary>`
- `GET /movies/bulk-import/batches/{batchId}` — new endpoint, returns `BulkImportBatchDetail`

<output>
Create `.planning/phases/11-bulk-import-feedback-ui/11-03-SUMMARY.md` when done
</output>
