---
phase: 11-bulk-import-feedback-ui
plan: 05
type: execute
wave: 5
depends_on: ["11-03", "11-04"]
files_modified:
  - frontend/pages/imports/index.vue
  - frontend/test/unit/pages/imports-index.spec.ts
  - frontend/components/AppNav.vue
autonomous: true
requirements: [IMPORT-06]

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The user can browse a list of past bulk-import batches (date, line count, status distribution) at any time, not just right after uploading"
    - "Clicking a batch in the list opens its full per-line results (D-03's persisted-report requirement — the core driver behind this whole phase)"
    - "The batch list page is reachable from the app's main navigation, not only via a direct URL"
  artifacts:
    - path: frontend/pages/imports/index.vue
      provides: "Batch list page (date, line count, status distribution per batch)"
      min_lines: 25
  key_links:
    - from: frontend/pages/imports/index.vue
      to: frontend/composables/useBulkImport.ts
      via: "onMounted calls getBatches() and renders each BulkImportBatchSummary as a NuxtLink to /imports/{batchId}"
      pattern: "getBatches"
    - from: frontend/components/AppNav.vue
      to: frontend/pages/imports/index.vue
      via: "New nav link to /imports"
      pattern: "/imports"
---

<objective>
Close the loop on D-03's core requirement — the user's own words in 11-CONTEXT.md were "ich
muss irgendwo die Bulk Imports Reports wiederherstellen können, abrufen und wiederherstellen
können" (I need to be able to retrieve and revisit my bulk import reports somewhere) — with a
batch list page reachable from navigation, not just a link that only exists for a few clicks
right after an upload.

Purpose: without this plan, a user who navigates away from the Add Film page after upload has
no way back to a completed import's results (the exact failure mode D-03 exists to prevent).
Output: `frontend/pages/imports/index.vue` (batch list), a nav entry pointing to it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/UI-SPEC.md
@.planning/phases/11-bulk-import-feedback-ui/11-CONTEXT.md
@.planning/phases/11-bulk-import-feedback-ui/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch list page</name>
  <read_first>
    frontend/pages/index.vue (list/summary page shape: loading/data/error/empty states,
    `onMounted` fetch pattern)
    frontend/composables/useDashboard.ts (the `data`/`isLoading`/`error`/`fetchX()` shape this
    page's loading pattern follows)
    frontend/composables/useBulkImport.ts (as created by Plan 11-04 — `getBatches()`,
    `BulkImportBatchSummary`)
    frontend/test/unit/pages/index.spec.ts (this codebase's test convention for a list/summary
    page)
  </read_first>
  <files>
    frontend/pages/imports/index.vue,
    frontend/test/unit/pages/imports-index.spec.ts
  </files>
  <action>
    Create `frontend/pages/imports/index.vue`: `&lt;script setup&gt;` calling `const {
    getBatches } = useBulkImport()`, local state `batches = ref&lt;BulkImportBatchSummary[]&gt;
    ([])`, `isLoading = ref(true)`, `error = ref&lt;string | null&gt;(null)`. `onMounted`:
    fetch via `getBatches()` into `batches.value`, catch into `error.value = 'Failed to load
    import history. Please refresh.'`, always clear `isLoading` in `finally` — mirrors
    `useDashboard.ts`'s `fetchDashboard()` try/catch/finally shape exactly. Template: heading
    "Bulk Import History"; loading state shows `SpinnerIcon` (matches `index.vue`'s loading
    block); error state shows the error text (matches `index.vue`'s error block); empty state
    (`!isLoading &amp;&amp; !error &amp;&amp; batches.length === 0`) shows "No bulk imports
    yet." plus a `NuxtLink to="/add"`; otherwise render one row per batch as a `NuxtLink
    :to="\`/imports/${batch.batchId}\`"` showing the formatted `createdAt` date, `totalLines`,
    and a compact rendering of `statusCounts` (e.g. "12 saved · 2 ambiguous · 1 not found" —
    only render entries with a non-zero count, using the exact status label wording already
    established for the results grid in Plan 11-04's `[batchId].vue`).

    Write `imports-index.spec.ts` following `index.spec.ts`'s test convention: mock
    `useBulkImport` to return a controllable `getBatches` promise, mount the page, and assert
    the loading/data/error/empty states each render correctly, plus that each batch row links
    to the correct `/imports/{batchId}` path.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run test/unit/pages/imports-index.spec.ts</automated>
  </verify>
  <done>
    `/imports` renders a loading state, then either the batch list (newest first, matching
    the backend's `findByUserIdOrderByCreatedAtDesc` ordering) or the empty state, with each
    row linking to its own `/imports/{batchId}` detail page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Navigation entry for the batch list page</name>
  <read_first>
    frontend/components/AppNav.vue (both the desktop `hidden md:flex` link row and the
    mobile slide-in drawer link list — both need the new entry, matching the existing
    `/add`/`/search`/`/settings` `NuxtLink` markup exactly)
  </read_first>
  <files>
    frontend/components/AppNav.vue
  </files>
  <action>
    Modify `AppNav.vue`: add a new `NuxtLink to="/imports"` entry to BOTH link lists — the
    desktop row (after the existing `/search` link, using the same `:class="[...textClass]"`
    pattern and an appropriately named `lucide-vue-next` icon such as `History` or
    `ListChecks`, imported alongside the existing `Search, Settings, LogOut, Menu, X,
    PlusCircle` icon imports) and the mobile drawer list (same position, matching the
    `active-class="text-primary"` + `@click="drawerOpen = false"` pattern already used by
    every other drawer link). Label it "Imports".
  </action>
  <verify>
    <automated>cd frontend && npx vitest run test/unit/components 2>&1 | tail -30</automated>
    <human-check>
      Start the dev environment (`docker compose up` or the project's existing `npm run dev`
      equivalent) and manually walk the full flow end-to-end:
      1. Upload a bulk-import file on /add, confirm the "Track progress →" link appears.
      2. Click it, confirm /imports/{batchId} shows a live processed/total count that
         updates, then transitions to the results grid (poster cards for saved lines, a
         text-only fallback card for any ambiguous/not-found/parse-error line).
      3. Navigate to /imports via the nav bar, confirm the just-completed batch appears in
         the list with the correct date/line count/status counts.
      4. Click it again, confirm the same results grid re-renders from the persisted data
         (not from SSE this time — reload the page to be sure).
    </human-check>
  </verify>
  <done>
    "Imports" is a visible nav entry on both desktop and the mobile drawer, linking to
    `/imports`. The full upload -> track progress -> results -> revisit-later flow works
    end-to-end in a real browser.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan adds no new server endpoints or trust boundaries — it is a pure client-side rendering of already-authorized data fetched via Plan 11-03's `getBatches()` (itself scoped server-side to the authenticated user). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-10 | Information Disclosure | imports/index.vue | low | accept | Renders only what `GET /movies/bulk-import/batches` already returns for the authenticated user (server-side-scoped in Plan 11-03) — no new client-side filtering logic that could be bypassed, no new server trust boundary introduced. |
</threat_model>

<verification>
Run `cd frontend && npx vitest run test/unit/pages/imports-index.spec.ts` — passes. Complete
the `<human-check>` walkthrough in Task 2 in a real browser before considering this phase
verified.
</verification>

<success_criteria>
- `/imports` lists all of the authenticated user's past batches, newest first, each linking
  to its detail page.
- "Imports" appears in both the desktop nav and the mobile drawer.
- A full upload → live progress → results → revisit-later loop works in a real browser.
</success_criteria>

## Artifacts This Plan Produces

- `frontend/pages/imports/index.vue` — new page (batch list)
- `AppNav.vue` "Imports" nav entry — new UI (desktop + mobile drawer)

<output>
Create `.planning/phases/11-bulk-import-feedback-ui/11-05-SUMMARY.md` when done
</output>
