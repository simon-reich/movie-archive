---
phase: 14-wiki-batch-reload-pacing-cooldown-fix-progress-ui
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java
  - backend/src/test/java/de/moviearchive/admin/WikiReloadProgressServiceTest.java
  - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java
  - backend/src/test/java/de/moviearchive/movie/WikiReloadServiceTest.java
  - frontend/composables/useSettings.ts
  - frontend/pages/settings.vue
  - frontend/test/unit/pages/settings.spec.ts
autonomous: true
requirements: [D-14-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User sees an ETA estimate on the progress UI that adapts to real observed per-movie call durations (including any active 429 backoff time), not a fixed pacing-delay-based guess (D-07)"
  artifacts:
    - backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java (ProgressState.etaSeconds field + rolling-average computation)
    - frontend/pages/settings.vue (ETA label in the progress block)
  key_links:
    - "WikiReloadService.batchReload()'s per-movie wall-clock duration measurement -> WikiReloadProgressService's 5-entry rolling window -> ProgressState.etaSeconds -> settings.vue's wikiEtaLabel"
---

<objective>
Complete D-14-03's progress-UI requirement by adding the ETA estimate 14-01-PLAN.md deferred: a live, per-movie-duration-driven ETA (remaining-count times a rolling average of the last 5 real per-movie call durations, including any active 429-backoff wait), rendered next to the processed/total count on the Settings page.

Purpose: CONTEXT.md D-07 explicitly rejects a fixed pacing-delay-based ETA guess in favor of one that adapts when the reactive 429-backoff fallback (D-02) kicks in — this plan wires that real measurement into the SSE stream 14-01-PLAN.md already established.
Output: `ProgressState` gains an `etaSeconds` field backed by a real rolling-average computation; `settings.vue` renders it as "~X min remaining" / "~Xs remaining".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/UI-SPEC.md
@.planning/phases/14-wiki-batch-reload-pacing-cooldown-fix-progress-ui/14-CONTEXT.md
@.planning/phases/14-wiki-batch-reload-pacing-cooldown-fix-progress-ui/14-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rolling-average ETA calculation wired into batchReload() progress events (D-07)</name>
  <files>backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java, backend/src/test/java/de/moviearchive/admin/WikiReloadProgressServiceTest.java, backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java, backend/src/test/java/de/moviearchive/movie/WikiReloadServiceTest.java</files>
  <read_first>
    - backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java (as delivered by 14-01-PLAN.md — the ProgressState record and publish()/resetRun()/complete() methods)
    - backend/src/main/java/de/moviearchive/enrichment/WikiReloadService.java (as delivered by 14-01-PLAN.md — batchReload()'s per-movie loop, where self.retryWikipedia() is called and progressService.publish() is invoked)
    - backend/src/test/java/de/moviearchive/admin/WikiReloadProgressServiceTest.java (as delivered by 14-01-PLAN.md — the existing lifecycle test structure to extend)
  </read_first>
  <behavior>
    - publish() called three times with durations 1000ms, 2000ms, 3000ms for a 5-movie run: the third call's ProgressState.etaSeconds equals round(average(1000,2000,3000) * (5-3) / 1000)
    - publish() called six times: the sixth call's etaSeconds reflects only the most recent five durations, not all six (the window evicts the oldest entry)
  </behavior>
  <action>
    Extend `WikiReloadProgressService.ProgressState` to add a sixth field: `long etaSeconds` (the record becomes `ProgressState(int processed, int total, boolean complete, String lastMovieTitle, String lastMovieStatus, long etaSeconds)`). Update the two existing "nothing in progress" call sites inside `WikiReloadProgressService` — `register()`'s synthetic-complete fallback and `complete()`'s final-state construction — to pass `0L` for `etaSeconds`. Add a `Map&lt;UUID, Deque&lt;Long&gt;&gt; durationWindowsMs` field and a `private static final int ETA_WINDOW_SIZE = 5` named constant (a fixed-size rolling window of the last 5 per-movie call durations — RESEARCH.md's Open Questions recommendation, balancing responsiveness against a single-outlier-movie skew). Change `publish(...)`'s signature to accept an additional `long durationMs` parameter (the just-completed movie's wall-clock call duration in milliseconds; any time spent inside an active 429 backoff happens synchronously inside that same call, so measuring wall-clock duration around the call captures it automatically per D-07). Inside `publish()`, before constructing the new `ProgressState`: push `durationMs` onto that `userId`'s deque (`computeIfAbsent(userId, id -> new ArrayDeque&lt;&gt;())`), evict from the front once the deque exceeds `ETA_WINDOW_SIZE`, compute the average of the current window's values, and compute `etaSeconds = Math.round(average * (total - processed) / 1000.0)` — pass this into the new `ProgressState`. Evict the `userId`'s duration-window entry from `durationWindowsMs` inside `complete()`, alongside the existing `emitters`/`lastKnown`/`stopFlags` eviction.

    In `WikiReloadService.batchReload()`'s per-movie loop, measure wall-clock duration around the existing `self.retryWikipedia(movie, resolvedTitles)` call using `System.currentTimeMillis()` captured before and after the call, and pass the measured `durationMs` into the now-6-argument `progressService.publish(...)` call.

    Extend `WikiReloadProgressServiceTest` with the two cases described in the behavior list above: `publish_computesEtaSeconds_asRollingAverageTimesRemaining` and `publish_windowCapsAtFiveEntries`. Extend `WikiReloadServiceTest` with a Mockito-mocked `WikiReloadProgressService` case (inject via the constructor in `setUp()`, mirroring how `movieRepository`/`wikipediaClient`/`indexingService` are already mocked there) asserting `publish(...)` is invoked with a `durationMs` argument greater than or equal to `0` for a successfully-processed movie, using `argThat(d -> d >= 0)` or an `ArgumentCaptor&lt;Long&gt;`.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "de.moviearchive.admin.WikiReloadProgressServiceTest" --tests "de.moviearchive.movie.WikiReloadServiceTest"</automated>
  </verify>
  <acceptance_criteria>
    - `ProgressState` has 6 fields including `etaSeconds`; `grep -c 'etaSeconds' backend/src/main/java/de/moviearchive/admin/WikiReloadProgressService.java` is at least 3 (record field + publish() computation + the register()/complete() zero-fill call sites).
    - `publish_computesEtaSeconds_asRollingAverageTimesRemaining` and `publish_windowCapsAtFiveEntries` both pass.
    - `batchReload()` measures and passes a real wall-clock `durationMs` per movie into `publish()`.
  </acceptance_criteria>
  <done>Every progress event streamed during a batch-reload carries an etaSeconds value computed from the rolling average of the last 5 real per-movie call durations (including active backoff time), not a fixed pacing-delay-based guess.</done>
  <reversibility rating="reversible">Record-field extension + a new bounded in-memory rolling window, both purely additive to an already-new (14-01-PLAN.md) component with no external consumers beyond this phase's own frontend.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the ETA estimate on the Settings page progress block (D-07)</name>
  <files>frontend/composables/useSettings.ts, frontend/pages/settings.vue, frontend/test/unit/pages/settings.spec.ts</files>
  <read_first>
    - frontend/composables/useSettings.ts (as delivered by 14-01-PLAN.md — the WikiReloadProgress interface and subscribeToWikiReloadProgress)
    - frontend/pages/settings.vue (as delivered by 14-01-PLAN.md — the #wikipedia-data progress block, wikiProgress ref, wikiProgressPercent computed)
    - frontend/test/unit/pages/settings.spec.ts (as delivered by 14-01-PLAN.md — the mount-based progress tests to extend)
  </read_first>
  <behavior>
    - etaSeconds 240 renders "~4 min remaining"
    - etaSeconds 45 renders "~45s remaining"
    - etaSeconds 0 renders neither "min remaining" nor "s remaining" text
  </behavior>
  <action>
    Add `etaSeconds: number` to the `WikiReloadProgress` interface in `useSettings.ts` (matches the backend `ProgressState`'s new 6th field from Task 1). In `settings.vue`, add a computed `wikiEtaLabel` that formats `wikiProgress.value?.etaSeconds`: `null`/`0`/absent formats to an empty string (render nothing); otherwise, when `etaSeconds >= 60`, format as `` `~${Math.ceil(etaSeconds / 60)} min remaining` ``; when `etaSeconds &lt; 60` (and `&gt; 0`), format as `` `~${etaSeconds}s remaining` ``. Render this label as a `&lt;p class="text-sm text-muted-foreground"&gt;` immediately below the existing processed/total `&lt;p&gt;` inside the `data-testid="wiki-reload-progress"` block, gated on `v-if="wikiEtaLabel"`.

    Add three new test cases to `settings.spec.ts` (extending the mount-based progress tests added in 14-01-PLAN.md's Task 3, reusing the same `vi.mock('@/composables/useSettings')` + captured-callback setup): invoke the captured `onProgress` callback with `{ processed: 2, total: 10, complete: false, lastMovieTitle: 'Interstellar', lastMovieStatus: 'SUCCESS', etaSeconds: 240 }` and assert the mounted wrapper's text contains the 4-minutes label from the behavior list above; a second case with `etaSeconds: 45` asserts the sub-minute label; a third case with `etaSeconds: 0` asserts neither label variant appears.
  </action>
  <verify>
    <automated>cd frontend && pnpm test -- settings</automated>
  </verify>
  <acceptance_criteria>
    - `settings.vue` renders an ETA label inside the progress block when `etaSeconds > 0`, formatted in minutes at/above 60 seconds and in seconds below.
    - All three new ETA-rendering test cases in `settings.spec.ts` pass.
  </acceptance_criteria>
  <done>A user watching a live batch-reload sees an ETA estimate next to the processed/total count, sourced entirely from the backend's rolling-average computation.</done>
  <reversibility rating="reversible">Purely additive display formatting on top of an already-streamed field; no state or API contract change.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

- `WikiReloadProgressService.ProgressState.etaSeconds` field (extends the 14-01-PLAN.md record)
- `WikiReloadProgressService`'s `durationWindowsMs` field and `ETA_WINDOW_SIZE` constant
- `settings.vue`'s `wikiEtaLabel` computed

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (inherited from 14-01-PLAN.md) `GET /admin/wiki-reload/{userId}/progress` | No new trust boundary — the ETA field rides the same already-ownership-checked SSE stream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Information Disclosure | `etaSeconds` field added to `ProgressState` | low | accept | A rolling-average call-duration estimate reveals no more than the already-streamed processed/total counts already disclose to the (ownership-checked) subscriber; no new sensitive data is included |
</threat_model>

<verification>
Run the full backend suite (`cd backend && ./gradlew test`) and full frontend suite (`cd frontend && pnpm test`) after both tasks land — both must be green. Manually confirm on a dev environment: trigger a reload, observe the ETA label update as movies complete, and confirm it visibly increases if a real 429 backoff occurs mid-run (proving the rolling average, not a fixed guess, drives the number).
</verification>

<success_criteria>
- D-14-03 (ETA): the progress UI shows an ETA that is computed from a rolling average of real per-movie durations, verified by unit tests asserting the window-of-5 behavior and by mount-based frontend tests asserting the formatted label.
</success_criteria>

<output>
Create `.planning/phases/14-wiki-batch-reload-pacing-cooldown-fix-progress-ui/14-02-SUMMARY.md` when done.
</output>
