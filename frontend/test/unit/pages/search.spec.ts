import { describe, it } from 'vitest'

describe('/search page', () => {
  it.todo('search page module exports a valid Vue component')
  it.todo('executes search on mount')
  it.todo('clicking a genre chip pushes /search?genre=X to the router')
  it.todo('renders MovieGrid when viewMode is grid, MovieList when list')
})
